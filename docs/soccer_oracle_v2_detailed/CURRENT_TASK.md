# CURRENT TASK

## Task corrente
**AGGIORNAMENTO 2026-09-12 (6): random search al posto della grid search esaustiva per Corners/Cards, "per il momento"**. Dopo aver mostrato le metriche prima/dopo la proiezione monotona, l'operatore ha chiesto di procedere col training reale completo ma ha suggerito: "forse e' meglio usare una random search per il momento" - risposta diretta al problema di costo gia' documentato (68 minuti solo per la suite di test su dati sintetici, causato dalla grid search esaustiva introdotta al punto (3)).

`_select_champion_via_model_search` (`train_multi_market.py`, condivisa con h2h/goal_no_goal/under_over_*) estesa con `search_strategy: str = "grid"` (default INVARIATO, zero impatto sui mercati flagship che continuano a passare da `train_market()` senza specificarlo) + `random_search_iter: int = 10`: con `search_strategy="random"` usa `RandomizedSearchCV` invece di `GridSearchCV` (stesso `_model_space`, stesso `cv_splits`, stesso criterio di ranking - cambia SOLO quante combinazioni vengono valutate, `random_state=42` per riproducibilita'). `train_cards_line`/`train_corners_line`/`train_cards_all_lines`/`train_corners_all_lines` ora hanno `search_strategy: str = "random"` come NUOVO default (diverso dal default "grid" della funzione condivisa - la scelta e' scoped a questi due mercati).

**Speedup misurato** (stessa linea, stesso dataset sintetico, stesso split): 133.0s (grid) -> 89.0s (random), **1.49x** - piu' modesto del previsto perche' l'ensemble (voting/stacking) e la valutazione finale di ogni candidato NON dipendono da `search_strategy` (costo fisso in entrambi i casi): la ricerca sui 3 candidati base e' solo una parte del tempo totale. Champion scelto identico in entrambi i casi su questo test (`random_forest`). Suite mirata (`TestTrainCardsAllLines`/`TestTrainCornersAllLines`) riverificata verde col nuovo default.

**AGGIORNAMENTO 2026-09-12 (5): monotonicita' tra linee per Corners/Cards, stesso principio gia' in produzione per i gol**. L'operatore ha chiesto se le linee di Corners/Cards seguono lo stesso principio di annidamento degli Under/Over gol ("un over 2.5 e' sicuramente anche un over 1.5?") - risposta: si', sono eventi ANNIDATI sullo stesso conteggio totale (Over 10.5 corner implica Over 9.5, Over 9.5 implica Over 8.5), esattamente come per i gol - dove il progetto ha gia' una soluzione pronta e in produzione da tempo (MARKET-04, `totals_market.py`: `enforce_monotonic_over_probabilities`, proiezione isotona obbligatoria applicata prima di servire le probabilita', perche' modelli indipendenti per soglia NON garantiscono da soli `P(Over1.5)>=P(Over2.5)>=...`).

Verificato quanto il problema fosse reale per Corners/Cards (dove NON esisteva ancora questa garanzia, essendo ogni linea un modello registry completamente indipendente): sugli stessi OOF gia' usati per il confronto quote, **33.1% delle coppie di linee adiacenti violano l'ordine per Corners, 22.7% per Cards** (metrica pairwise ufficiale, stessa di `totals_market._count_monotonicity_violations`; guardando "almeno una violazione su tutte e 3 le coppie per riga" la percentuale sale al 75-77%/53-58%) - percentuali enormi, es. P(Over 9.5)=0.55 ma P(Over 8.5)=0.50 nella stessa riga, logicamente impossibile.

**Fix ("Si applica lo stesso principio")**: `enforce_monotonic_over_probabilities`/`_count_monotonicity_violations` (`totals_market.py`) generalizzate con un parametro `label_fn` (default INVARIATO, nessun impatto sui gol gia' in produzione) per essere riusate IDENTICHE con la convenzione di etichetta di Corners/Cards (`_line_label`, "line_X_Y") invece di duplicare la logica. Nuove `_line_independent_oof`/`compute_monotonicity_report` (identiche in `cards_market.py`/`corners_market.py`): OOF diagnostico con RandomForest fisso INDIPENDENTE dal champion per linea (troppo costoso da rifittare qui), conta le violazioni PRIMA della proiezione e produce le probabilita' proiettate monotone. Integrato in `train_cards_all_lines`/`train_corners_all_lines` (nuovo campo `CardsBenchmarkReport.monotonicity`/`CornersBenchmarkReport.monotonicity`) e propagato in `run_cards_benchmark`/`run_corners_benchmark` (`details["monotonicity"]`).

**Nota per il deploy**: solo diagnostico per ora - la proiezione monotona non e' ancora applicata a un output SERVITO (nessuna delle due linee e' ancora consumata da un servizio live, a differenza dei gol dove `dashboard_service._apply_monotonic_projection` la applica gia' in produzione); da agganciare quando/se questi modelli verranno promossi e serviti. 6 nuovi test rapidi (`TestComputeMonotonicityReport`, 25.9s, NON richiamano mai la grid search). Suite mirata completa (cards+corners+filter_market_service+train_multi_market_pipeline+totals_market) in corso di riverifica dopo questa modifica.

**AGGIORNAMENTO 2026-09-12 (4): fix quote pooled multi-linea per Corners/Cards + verifica empirica ("Investighiamo" / "Si procedi")**. Dopo aver scoperto ((3) sotto) che la model search completa NON migliorava le performance rispetto al RandomForest fisso, l'operatore ha chiesto di investigare la causa root invece di continuare a cambiare algoritmo ("Investighiamo").

**Causa trovata (a livello di feature, non di algoritmo)**: `switch_bet()` (`download_match_service.py`) separa gia' le quote Under/Over GOL per soglia specifica (`under_over_1_5/_2_5/_3_5/_4_5`), ma per Corners/Cards ritorna sempre la stessa chiave piatta ('corners'/'cards') qualunque sia la linea quotata - `FilterMarketService._extract_market_odds_features` mette quindi in un UNICO paniere le quote di TUTTE le linee (8.5/9.5/10.5/11.5 corners, 3.5/4.5/5.5/6.5 cards) di TUTTI i bookmaker. Confermato empiricamente sui dati reali esportati: `odds_count` pooled fino a 218, `odds_max` fino a 66.0 con `odds_min` fino a 1.0 (impossibile per un singolo evento/linea) - e da una funzione preesistente (`_extract_line_value`, mai usata per queste feature ML) che dimostra che la linea era gia' identificabile dentro la chiave (formato confermato: `"Over 8.5_bet365"`).

**Fix implementato ("Si procedi")**: nuove feature quota SPECIFICHE per linea (`_extract_line_specific_odds_features` in `filter_market_service.py`, riusata da una nuova `_line_specific_odds_features` in `cards_market.py`/`corners_market.py`), ADDITIVE alle colonne pooled esistenti nel frame. `_feature_columns_for` (riscritta) sceglie, per la linea attiva in training, SOLO le sue colonne quota specifiche (mai le altre linee, mai il paniere pooled) tramite un nuovo parametro `use_line_specific_odds: bool = True` propagato da `train_cards_line`/`train_corners_line`/`train_cards_all_lines`/`train_corners_all_lines`. 31 nuovi test (dataset multi-linea realistico in `_make_match`, `TestLineSpecificOddsFeatures`, `TestFeatureColumnsForLineScoping`, `TestExtractLineFromOddsKey`, `TestExtractLineSpecificOddsFeatures`).

**Verifica empirica PRIMA del training completo (costoso)**: nuovo script isolato `scripts/analysis/test_line_specific_odds_vs_pooled.py` (stesso pattern di `test_split_odds_vs_pooled.py`: RandomForest fisso, walk-forward, `selection_score` come metrica di adozione, soglia +0.01) sui dati reali riesportati (`corners.csv`/`cards.csv`, 8804/8355 righe). **Risultato onesto: nessuna delle 8 linee supera la soglia di adozione sul `selection_score` composito** (delta tra -0.0012 e +0.0090) - ma l'AUC puro migliora in 7 linee su 8 (es. cards 6.5: 0.6344 -> 0.6503; cards 5.5: 0.6269 -> 0.6425; corners 8.5: 0.5365 -> 0.5493), l'unica eccezione e' corners 9.5 (lieve peggioramento). Il fix resta comunque corretto concettualmente (elimina un mescolamento di quote incompatibili tra linee diverse) e non peggiora la metrica composita in modo significativo: mantenuto come default (`use_line_specific_odds=True`), nessun rollback. Suite mirata (cards+corners+filter_market_service+train_multi_market_pipeline): **72 passed** (1h11m, tempo dominato dalla grid search introdotta al punto (3), non da questo fix).

**Nota per il deploy**: nessuna migration necessaria. Il training reale con questo fix non e' stato ancora ri-eseguito su tutte le 8 linee dopo la verifica A/B (solo la verifica isolata sopra) - da fare come prossimo passo se l'operatore conferma di voler procedere con un nuovo giro di training completo.

**AGGIORNAMENTO 2026-09-12 (3): model search completa (grid search + SMOTE + voting/stacking) per Corners/Cards**. Richiesto esplicitamente dall'operatore dopo aver chiesto quali algoritmi fossero stati usati per il primo giro di training reale ("hai fatto ensemble, stacking o voting? hai provato piu' opzioni? hai fatto la grid search?") e aver scoperto che NON era stato fatto - solo un `RandomForestClassifier` a iperparametri fissi. Risposta: "assolutamente si... il tuo obbiettivo e provare e trovare la situazione migliore".

`train_cards_line`/`train_corners_line` non addestrano piu' un singolo modello: riusano ORA la STESSA ricerca gia' in produzione per h2h/goal_no_goal/under_over_* (`train_market()` in `train_multi_market.py`) - grid search su 3 candidati (logistic/random_forest/random_forest_smote), ensemble voting+stacking sui 2 migliori, champion scelto per `selection_score`. La logica di ricerca (prima inline dentro `train_market()`) e' stata ESTRATTA (comportamento invariato, verificato dalla suite esistente) in una nuova `_select_champion_via_model_search(X, y, cv_splits, market, season_series, league_series, selection_method)` in `train_multi_market.py`, riusata "cross-module" da `cards_market.py`/`corners_market.py` (stesso pattern gia' in uso per `_build_temporal_cv`/`_filter_valid_splits`). `CardsLineTrainResult`/`CornersLineTrainResult` espongono ora anche `champion_name`/`model_results` (TUTTI i candidati confrontati, non solo il vincitore) - propagati fino al registry (`model_name=f"calibrated_{champion_name}"`, `extra.model_search_results`) per trasparenza completa su cosa e' stato provato.

**Costo computazionale scoperto durante la verifica**: la grid search completa e' MOLTO piu' lenta del RandomForest fisso di prima - la suite di test su dataset SINTETICI (150-260 righe) e' passata da ~3 minuti a **68 minuti** per cards_market_test.py+corners_market_test.py (49 test, tutti verdi). Sui dati REALI (8783 righe corners, 8334 cards, gia' esportati) il training completo (8 linee) e' quindi atteso durare potenzialmente ORE, non minuti - da eseguire come task di sfondo con check-in periodici, non una singola chiamata bloccante. Suite completa rieseguita fuori da questi 2 file per verificare che il refactor di `train_multi_market.py` (file condiviso con la produzione) non avesse rotto nulla: 834 passed.

**AGGIORNAMENTO 2026-09-12 (2): training reale Corners/Cards (MARKET-05/06) + raffinamento feature arbitro + "tutte le metriche possibili"**. Richiesto esplicitamente dall'operatore dopo aver confermato il piano discusso in precedenza ("Si procediamo con l'implementazione e vorrei avere tutte le metriche possibili per ogni contesto"). Nessuna modifica al training/registry per gli altri mercati (scope limitato a Corners/Cards, come discusso).

- **Raffinamento feature arbitro (solo Cards)**: il prior arbitro grezzo (media semplice cartellini storici) applica ora uno SHRINKAGE bayesiano verso un baseline (media di LEGA point-in-time se disponibile, altrimenti globale, altrimenti il prior neutro costante) - un arbitro con 1-2 precedenti non e' piu' preso alla lettera. Nuova feature `referee_severity_index_prior` (prior/baseline, normalizzato per lega: 1.0 = nella media della propria lega) - un arbitro severo in una lega "dura" e uno in una lega "morbida" ora sono comparabili. Un arbitro sconosciuto/assente resta sempre e solo al prior neutro fisso (mai un baseline di lega per un'identita' ignota). Nessuna feature arbitro aggiunta a Corners (confermato: l'arbitro non ha influenza nota sui calci d'angolo).
- **"Tutte le metriche possibili" per ogni linea di ogni mercato**: nuovo modulo condiviso `src/ml/evaluation/classification_report.py` (`compute_full_classification_report`) - confusion matrix, precision/recall/F1 per classe e pesati, curva ROC+AUC, curva Precision-Recall+average precision, soglia ottimale (Youden J) con le stesse metriche ricalcolate a quella soglia - calcolato SIA pre che post calibrazione, per OGNI linea di Corners e Cards, riusando gli array OOF gia' prodotti da `CalibrationService.calibrate_estimator` (esteso con 4 nuovi campi additivi `pre/post_probabilities`/`pre/post_y_true`, nessun ricalcolo del walk-forward). Deliberatamente NON toccata `compute_probability_metrics` (usata da ogni altro mercato del progetto): il nuovo modulo e' complementare e additivo, zero impatto fuori da Corners/Cards.
- **Fix compatibilita' col gate di promozione (OPS-02)**: `_save_line_model` ora registra anche `sample_size`/`post_ece`/`post_auc`/`selection_score` (mancavano — senza `sample_size` il gate avrebbe SEMPRE bloccato la promozione di questi modelli). Report nested completo (confusion matrix/ROC/PR/reliability) salvato in `extra`.
- **Nuovo script CLI** `scripts/analysis/train_corners_cards_benchmark.py` (mai eseguiti prima d'ora `run_corners_benchmark_from_db`/`run_cards_benchmark_from_db`, gia' esistenti ma orfani): addestra+calibra ciascuna linea sul DB reale e stampa/salva il report completo; `--dry-run` per una prova senza registrare modelli. **Non eseguibile da questa sessione cloud** (nessun accesso di rete diretto al Postgres Railway, stesso blocco gia' documentato altrove) - smoke-testato con dati sintetici mockando `MatchRepository`, mai contro dati reali. Da lanciare dall'operatore in locale o da una sessione con accesso DB reale.
- 21 nuovi test (`classification_report_test.py`, estensioni a `calibration_service_test.py`/`cards_market_test.py`/`corners_market_test.py`). Suite completa: **881 passed** (era 870).

**Nota per il deploy**: nessuna migration necessaria. Nessun impatto sul serving finche' l'operatore non esegue lo script E promuove esplicitamente qualche linea a `production` (oggi tutto resta `candidate`, come sempre in questo progetto).

**AGGIORNAMENTO 2026-09-12 (1): sezione "Model Diagnostics" nel frontend web**. Richiesto esplicitamente dall'operatore ("inizia l'implementazione del model diagnostics come nuova pagina per ogni mercato che abbiamo e che man mano aggiungeremo"), chiudendo il prompt dettagliato preparato il 2026-09-08/10 (`PROMPT_model_diagnostics_web_section.md`, ora marcato IMPLEMENTATO). Nuova pagina web (voce di menu top-level, confermata con l'operatore) che mostra, per ogni mercato con un modello registrato, un report di classificazione out-of-fold (walk-forward, stessa garanzia anti-leakage di produzione): grafico ROC comparativo con una linea per mercato + una card per mercato con confusion matrix, precision/recall/F1 per classe e 3 stat tile (Accuracy/ROC AUC/Weighted F1) — porting fedele, ma generalizzato a N mercati (non piu' solo i 4 Under/Over), dell'Artifact "Under/Over Champions" pubblicato il 2026-09-08.

Backend: nuova funzione pura `src/ml/evaluation/model_diagnostics_service.py` (riusata sia da un nuovo endpoint `GET /models/diagnostics` sia dallo script CLI `evaluate_champions_detailed.py`, riscritto perche' rotto — leggeva da un CSV di export non piu' nel repo), cache API a 15 minuti (`src/api/model_diagnostics_service.py`). Frontend: `frontend/src/features/model-diagnostics/` (`ModelDiagnosticsPage`/`RocComparisonChart`/`ThresholdCard` + CSS scoped dedicato), wiring menu/router/App completo. 17 nuovi test backend (suite totale invariata rispetto all'ultimo conteggio noto, verificata prima del commit), build frontend verificata, rendering verificato visivamente (screenshot chiaro/scuro con un mock del backend — nessun DB reale raggiungibile da questa sessione cloud). Dettaglio completo in `IMPLEMENTATION_LOG.md`.

**Nota per il deploy**: nessuna migration necessaria (nessuna modifica di schema). Richiede solo il rebuild dei container (`docker compose up -d --build`) dopo il pull, come ogni altra modifica di codice/frontend.

**AGGIORNAMENTO 2026-09-10 (3): colorazione badge previsioni per esito reale + fix punteggio finale mancante**. Due richieste separate dell'operatore lo stesso giorno, entrambe gia' mergiate in `main`: (1) i badge previsione si colorano ora di verde/rosso in base all'esito REALE per le partite concluse (riuso diretto della logica di settlement BET-06, nessuna duplicazione); (2) il punteggio finale (`Match.score_home`/`score_away`, nuove colonne) viene ora salvato per OGNI partita conclusa direttamente dall'endpoint leggero `fixtures`, non piu' solo quando l'endpoint dettagliato `fixtures/statistics` restituisce dati (causa delle partite "Finita" senza risultato segnalate dall'operatore) — con un flag `has_full_stats` per evitare che l'assenza di quel dettaglio venga letta come "0" invece che "dato mancante" su corners/cards. Suite completa: 870 passed. Dettaglio completo in `IMPLEMENTATION_LOG.md`.

**AGGIORNAMENTO 2026-09-10 (2): vista storica Dashboard sempre veloce + copertura completa banca dati predizioni**. Richiesto esplicitamente dall'operatore dopo aver segnalato che il cambio data restava lento ANCHE al secondo giro sulla STESSA data storica (non solo la prima volta): la banca dati `match_prediction_snapshot` (introdotta il giorno prima) copriva solo le fixture viste in Dashboard o intercettate dal job mentre erano ancora `NS`, mai lo storico gia' passato ne' le partite finite fuori da quella finestra. Piano a 4 punti (documentato e aggiornato ad ogni punto, su richiesta esplicita dell'operatore, in `docs/soccer_oracle_v2_detailed/PROMPT_fast_historical_predictions.md`), tutti e 4 completati dal lato codice:

1. **Niente ricalcolo al volo per le date storiche** (commit `ad50648`): nuovo `allow_compute: bool` su `PredictionSnapshotService.resolve_predictions` — `False` disabilita qualunque calcolo nuovo, serve solo cio' che e' gia' salvato. Attivo SOLO per `get_day_matches` su date storiche (mai oggi/future, mai il dettaglio di una singola fixture). Frontend: placeholder "In coda" per i mercati non ancora coperti.
2. **Job esteso alle partite appena concluse** (commit `caf12bb`): `run_prediction_snapshot_refresh` copre ora anche le fixture concluse negli ultimi 3 giorni prive di snapshot, con anti-join bulk per non toccare quelle gia' coperte.
3. **Script di backfill storico una tantum** (commit `d72b4f5`): `scripts/backfill_prediction_snapshots.py`, keyset pagination su TUTTO lo storico concluso. Scritto e testato; lanciato una volta contro il DB reale (sessione bridge locale), interrotto a meta' e **deciso di NON completarlo**: lo storico profondo serve solo al training (che non dipende da questa tabella), e l'esigenza reale dell'operatore ("qualche giorno indietro + da oggi in poi") e' gia' coperta per sempre dal job del punto 2.
4. **Bottone "Ricalcola previsione"** (commit `5ceba8b`): nuovo `force: bool` su `resolve_predictions` (ignora anche una riga "congelata"), endpoint `POST /dashboard/matches/{fixture_id}/recompute-predictions`, bottone nel pannello di dettaglio partita.

34 nuovi/estesi test, suite completa **829 passed** (era 813). Build frontend verificata. **Mergiato in `main` e deployato** (merge commit `06b1405`, 2026-09-10). Dettaglio completo in `IMPLEMENTATION_LOG.md`.

**AGGIORNAMENTO 2026-09-09 (4): banca dati predizioni persistita + fix "sempre lentissimo"**. Richiesto esplicitamente dall'operatore ("la mia idea e' quella di salvare queste e le prossime che ci saranno di predizioni e non ricalcolare tutto, anche perche' dobbiamo avere una banca dati da accumulare... per le partite di oggi o future, solo se cambia una delle feature"), dopo una diagnosi congiunta del perche' la Dashboard fosse "sempre lentissima": trovato che `DashboardService._model_cache` era un attributo PER-ISTANZA, e `main.py` crea una `DashboardService()` NUOVA ad ogni richiesta — quindi i modelli `.pkl` (decine/centinaia di MB, 5 mercati) venivano ricaricati da disco via `joblib.load` ad OGNI singola chiamata Dashboard, indipendentemente dalla data o dalla quota API-Sports. Risolto con un redesign piu' ampio del semplice "sposta la cache a livello di classe":

- **Nuova tabella APPEND-ONLY `match_prediction_snapshot`** (migration `7b1e0c85a98d`, revises `a7d92c4f18e6`): `fixture_id`/`market`/`prediction`/`probability`/`model_name`/`model_run_id`/`feature_fingerprint`/`computed_at`. Distinta da `prediction_ledger` (BET-06, quella logga SOLO le scommesse loggate esplicitamente) - questa logga OGNI predizione grezza calcolata per OGNI mercato, sia come cache di serving (si legge l'ultima riga) sia come storico di ricerca ("banca dati da accumulare" richiesta dall'operatore: come si e' mossa la stima del modello prima del calcio d'inizio).
- **Nuovo `src/ml/serving/prediction_snapshot_service.py`** (`PredictionSnapshotService`): risolve la predizione per fixture+mercato con due regimi, decisi dallo status della partita:
  - **Conclusa** (FT/AET/PEN/ABD/CANC/PST/WO): se esiste gia' una riga salvata, viene servita DIRETTAMENTE - zero query di frame, zero caricamento modello. Se non esiste ancora (mercato promosso dopo la fine di quella partita), viene calcolata UNA VOLTA e **congelata per sempre**: scelta esplicita dell'operatore, un retrain futuro NON tocca le predizioni storiche gia' salvate (rappresentano "cosa prediceva il modello in quel momento").
  - **Non conclusa** (NS/live): il frame di feature viene comunque costruito (economico) e se ne calcola un `feature_fingerprint` deterministico (hash SHA-256 dei valori arrotondati a 6 decimali, generico per costruzione — non lega l'invalidazione a colonne specifiche come "solo le quote": qualunque feature cambi, odds o mean_statistics o fonti future, invalida). La riga salvata viene riusata SOLO se fingerprint E `model_run_id` sono identici all'ultimo calcolo; altrimenti si ricalcola e si APPENDE una riga nuova (mai un overwrite).
  - `_model_cache` (i modelli `.pkl` caricati) e' ora a LIVELLO DI CLASSE (`PredictionSnapshotService._model_cache`, sopravvive tra le istanze) - questo e', da solo, il fix diretto del bug "sempre lentissimo": un processo API/scheduler carica ciascun modello da disco UNA VOLTA SOLA, non ad ogni richiesta.
- **`DashboardService._predict_fixture` refactored**: non carica piu' modelli/frame da solo - delega interamente a `PredictionSnapshotService.resolve_predictions` (passando anche `status`, ora threaded attraverso tutti e 3 i call site), poi applica la proiezione di coerenza monotona (SEMPRE, sul payload grezzo risolto - mai persistita, e' una combinazione tra mercati diversi che va ricalcolata ad ogni lettura). Rimossi `_load_model`/`_latest_model_for_market`/`_model_meta_cache`/`_model_cache` (nessun consumatore esterno, verificato).
- **Nuovo job schedulato `prediction_snapshot_refresh`** (8° job, stesso heartbeat/self-gating appena costruito nell'aggiornamento (3) sopra — riuso diretto): ricalcola in BACKGROUND le predizioni delle fixture NS nella finestra oggi->oggi+`daily_refresh_days_ahead` giorni (stessa finestra gia' sincronizzata da `data_daily_refresh`/`data_future_sync`), cosi' che quando un utente apre la Dashboard trovi quasi sempre una riga gia' calcolata - il percorso di serving resta una PURA lettura da DB, mai un'inferenza ML nella richiesta utente. Un fallimento su una singola fixture non blocca le altre (stesso principio "provider errors isolati" di LIVE-01). Intervallo di default 30 minuti (`AppConfig.prediction_snapshot_interval_minutes`, editabile da Impostazioni come gli altri 7 job).
- **Nuovi test**: `match_prediction_snapshot_repository_test.py` (7), `prediction_snapshot_service_test.py` (15, inclusi i 3 regimi + cache di classe condivisa), `prediction_snapshot_refresh_job_test.py` (4, finestra/isolamento errori/default), piu' l'aggiornamento di `dashboard_service_test.py`/`scheduler_test.py` per il nuovo delegate/8° job.

**Nota per il deploy**: serve `alembic upgrade head` (nuova tabella `match_prediction_snapshot`, revision `7b1e0c85a98d`) PRIMA del rebuild dei container - la migration non e' mai stata eseguibile da questa sessione cloud (nessun accesso di rete diretto al Postgres Railway, vedi blocco documentato in IMPLEMENTATION_LOG.md 2026-09-07) quindi va lanciata dall'operatore o da una sessione con accesso DB reale.

**AGGIORNAMENTO 2026-09-09 (3): orario/intervallo dei job automatici editabile da Impostazioni, senza restart**. Richiesto esplicitamente dall'operatore ("mettere l'orario editabile così che io possa avere maggiori controllo"), con scelta esplicita (tra due opzioni proposte con relativi tradeoff) per l'approccio **ad effetto immediato** (nessun restart del container `scheduler` necessario) invece della semplice alternativa "salva su file, effetto al prossimo riavvio".

Redesign strutturale dello scheduler (`src/jobs/scheduler.py`): i vecchi trigger APScheduler per-job (`CronTrigger`/`IntervalTrigger` calcolati UNA VOLTA da `cfg.*` all'avvio) sono sostituiti da un unico **heartbeat condiviso** (`IntervalTrigger(seconds=30)`, tutti e 7 i job) + **self-gating** ad ogni tick: `_is_job_due(job_id, cfg)` calcola se il job e' "dovuto" confrontando l'orario/intervallo EFFETTIVO corrente (`job_settings.resolve_job_schedule`, che riflette un eventuale override salvato) con l'ultima esecuzione TERMINATA nello storico gia' esistente (`JobHistory`/`jobs_history.jsonl`, nessun nuovo tracking parallelo) - per i job "daily" (`data_daily_refresh`/`data_future_sync`/`ml_training`): vero se l'ora locale (Europe/Rome) ha superato `hour:minute` di oggi E l'ultima esecuzione risale a un giorno locale precedente; per gli "interval" (`data_sync_today`/`data_settlement`/`data_quality_report`/`data_sync_live`): vero se e' trascorso l'intervallo configurato dall'ultima esecuzione. `_run_if_due` sostituisce il vecchio `_run_if_enabled`, combinando enabled (gia' esistente) + due (nuovo). Un cambio di orario da Impostazioni ha quindi effetto dal tick di heartbeat successivo (max 30s), MAI un restart.

`src/jobs/job_settings.py` esteso: ogni `JOB_DEFINITIONS[job_id]` dichiara ora `schedule_kind` ("daily"/"interval_minutes"/"interval_seconds") e `cfg_fields` (mappa parametro schedule -> attributo `AppConfig`, per il default quando non c'e' un override); nuovo file di stato dedicato `best_models/job_schedule.json` (stesso pattern di scrittura atomica file-temp+`os.replace` di `job_settings.json`/`quota_pause_state.json`) con `get_job_schedule_overrides`/`update_job_schedule`/`reset_job_schedule`/`resolve_job_schedule`, validati da `_validate_schedule` (chiavi ESATTE per lo `schedule_kind` del job, range sensati: ore 0-23, minuti 0-59, intervallo minuti 5-1440, intervallo secondi 30-3600 - mai un valore troppo aggressivo che rischi di bombardare API-Sports/il DB). `list_job_definitions` arricchito con `schedule_kind`/`schedule`/`schedule_is_default` per il frontend.

API (`src/api/main.py`/`schemas.py`): `JobSettingRow` esteso con gli stessi 3 campi; nuovi `PUT /settings/jobs/{job_id}/schedule` (valida e salva l'override, 400 su job/chiavi/valori non validi, 404 su job sconosciuto) e `DELETE /settings/jobs/{job_id}/schedule` (torna al default `AppConfig`) - verificati end-to-end via `TestClient` con path JSON isolati.

Frontend (`SettingsPage.jsx`): nuovo componente `JobScheduleEditor` per riga job - time picker HTML (`<input type="time">`) per i job "daily", input numerico per gli "interval_minutes"/"interval_seconds" (salvataggio al blur), bottone "ripristina default" mostrato solo quando esiste un override attivo (`schedule_is_default=false`). Nuove funzioni `api.js` (`updateJobSchedule`/`resetJobSchedule`) + stato/handler dedicati in `App.jsx` (`jobScheduleSavingId`/`saveJobSchedule`/`resetJobScheduleToDefault`, stesso pattern gia' usato per il toggle enabled/disabled). Build frontend verificata senza errori.

`tests/service/scheduler_test.py` riscritto (i vecchi test verificavano trigger per-job calcolati da cfg, non piu' veri sotto il nuovo design): nuove classi `TestAllJobsShareHeartbeatTrigger`/`TestIsJobDue` (7 combinazioni schedule_kind x stato storico, `datetime.now()` reso deterministico via una sottoclasse fissa)/`TestRunIfDue`/`TestLastCompletedRun`/`TestParseHistoryTimestamp`, oltre alle classi invariate su target/indipendenza dei job. Nuovo `tests/service/job_settings_test.py` (25 test, path isolati via monkeypatch di `_schedule_path`) per validazione/override/reset/resolve/list_job_definitions. Suite completa rieseguita: **780 passed** (era 734).

**Nota per il deploy**: da riavviare (`docker compose restart api scheduler`, o equivalente) per caricare il nuovo codice - da quel momento in poi ogni ulteriore modifica di orario/intervallo da Impostazioni ha effetto immediato senza ulteriori restart.

**AGGIORNAMENTO 2026-09-09 (2), non ancora documentato in dettaglio prima d'ora**: diagnosticato e risolto un consumo eccessivo di chiamate al provider esterno dalla Dashboard (segnalato dall'operatore: "c'è un richiamo sempre del provider esterno quando finisco nella dash iniziale... soprattutto per la giornata odierna"). Causa: il polling automatico frontend ogni 60s (`App.jsx`) combinato con una cache backend ANCH'ESSA a TTL 60s (`DashboardService._api_cache_ttl_seconds`) produceva chiamate quasi continue ad API-Sports per il giorno corrente con la Dashboard semplicemente aperta in una scheda. Due fix indipendenti, scope limitato SOLO al layer Dashboard (mai ai job schedulati, che restano sulla logica invariata): (1) **Parallelizzato** il loop sequenziale per-lega di `_fetch_api_day_fixtures` (`ThreadPoolExecutor(max_workers=min(6, len(leagues)))`, risultati raccolti in ordine tramite `executor.map`) - stessa quantita' di chiamate HTTP, ma non piu' in serie, riduce sensibilmente la latenza percepita nelle return piu' lente segnalate dall'operatore. (2) **Finestra oraria per il pool "oggi"**: nuove `_DASHBOARD_API_WINDOW_START=12:30`/`_DASHBOARD_API_WINDOW_END=00:30` (Europe/Rome, finestra che attraversa la mezzanotte - logica `now>=start OR now<end`), scelta esplicita dell'operatore dopo aver chiarito la differenza tra le opzioni possibili: fuori da questa finestra (madrugada, prima di pranzo) `_fetch_api_day_fixtures` (scoped a `target_date==oggi`, bypassabile con `force_refresh`) e `_fetch_api_live_fixtures` (nessun bypass) saltano la chiamata esterna, dato che in quella fascia non ci sono tipicamente partite in corso in Europa. Fuso orario Europe/Rome (`zoneinfo.ZoneInfo`) usato ESCLUSIVAMENTE per questa decisione oraria, mai per i timestamp interni (rimasti UTC ovunque, come richiesto esplicitamente per evitare ambiguita' DST). Nuovi test in `tests/service/dashboard_service_test.py` (`TestApplyMonotonicProjection` gia' esistente + parallelismo lega + `TestDashboardApiWindow`, 9 casi su confini/bypass/altre date).

**AGGIORNAMENTO 2026-09-09 (1)**: promosso a `production` anche `goal_no_goal` (5° mercato, stesso processo dei 4 Under/Over) su richiesta esplicita dell'operatore ("portiamo in produzione i 5 modelli"). Collegata al serving reale anche la proiezione di coerenza monotona tra le 4 soglie Under/Over (`enforce_monotonic_over_probabilities`, gia' validata offline, MAI applicata prima d'ora in `dashboard_service.py`): `_predict_fixture` la applica ora alle 4 predizioni della stessa fixture (solo se tutte e 4 disponibili), aggiornando sia `probability` sia `prediction` - elimina le incoerenze tipo "Over 3.5: si" + "Over 2.5: no" nella stessa card. Nessun impatto su `bet_over_signal`/`DecisionPolicy`, che leggono il payload gia' proiettato a valle. 5 nuovi test, suite completa 734 passed. Tutti e 5 i mercati (4 Under/Over + goal_no_goal) sono ora `production` e reciprocamente coerenti.

**COMPLETATO (2026-09-08, aggiornato con promozione a production)**: addestrati modelli ML REALI (fit completo, non solo benchmark) per Under/Over 1.5/2.5/3.5/4.5, valutato (e scartato) l'uso di goal_no_goal come feature aggiuntiva, e **promossi a `production`** (`ModelRegistry.promote_with_policy`, gate verificato per tutti e 4, nessun bypass) su richiesta esplicita dell'operatore. Dettaglio completo in `IMPLEMENTATION_LOG.md` (voci "Training reale Under/Over 1.5/2.5/3.5/4.5", "SMOTE come candidato strutturale", "goal_no_goal addestrato + feature injection" e "Promozione a production + segnale Bet Over", 2026-09-08).

**Promozione a production (2026-09-08)**: run_id promossi (i piu' recenti per mercato, post-SMOTE) - under_over_1_5 (`random_forest`), under_over_2_5 (`voting`), under_over_3_5 (`random_forest_smote`), under_over_4_5 (`voting`). Prima production mai esistita per questi mercati (`comparison.method="no_production_baseline"`, promozione consentita per policy). Audit trail in `best_models/registry/promotion_history.jsonl`.

**Segnale "Bet Over" (2026-09-08)**: implementato lo switch discusso con l'operatore - nuovo modulo `src/oracle/decision_engine/over_signal_policy.py` (versionato `over_signal_policy_v1`), soglia di probabilita' orientata alla precisione sull'Over (da `betting_thresholds.json`, recall floor 30%) specifica per ciascuno dei 4 mercati. Scelta esplicita dell'operatore: segnale **indipendente** dal pick mostrato in dashboard (che resta a soglia 0.5, invariato) - si attiva quando P(Over) supera la soglia di quel mercato anche se il pick resta "Under" (es. under_over_4_5: soglia 0.1776, ben sotto 0.5). Wired in `dashboard_service.py._build_decision_cards` come nuovo campo `bet_over_signal` nelle `decision_cards` esistenti, nessuna modifica al pick/alla DecisionPolicy generica gia' in uso. Suite completa verificata dopo la modifica: 727 passed.

**Aggiornamento SMOTE (stesso giorno)**: aggiunto `random_forest_smote` come candidato IN PIU' in `_model_space` (`src/service_ia/training/train_multi_market.py`, condiviso da TUTTI i mercati, non solo Under/Over) dopo un confronto controllato richiesto dall'operatore (`scripts/analysis/test_smote_vs_class_weight.py`): SMOTE aiuta in proporzione a quanto il mercato e' sbilanciato. Ri-addestrate le 4 soglie con la pipeline aggiornata: **over_3_5 e over_4_5 hanno un nuovo champion migliore** (over_3_5: `random_forest_smote`, selection_score 0.6906->0.7008; over_4_5: `voting` su base `random_forest_smote`+`random_forest`, selection_score 0.7820->0.7860), over_1_5/2_5 restano sui champion precedenti (SMOTE competeva ma non ha vinto, nessun peggioramento). Registry, soglie ottimali e report "model diagnostics" tutti rigenerati sui nuovi champion (vedi sotto).

**Aggiornamento goal_no_goal (2026-09-08, stesso giorno, richiesto dall'operatore)**: prima di considerare la promozione degli Under/Over, l'operatore ha chiesto di addestrare anche il mercato "goal_no_goal" (GG/NG, gia' supportato da `FilterMarketService`/`train_market()` ma MAI addestrato finora - nel registry c'erano solo i 4 Under/Over) e testare l'idea di usare la sua probabilita' OOF come feature aggiuntiva per gli Under/Over (razionale: GG=Si implica deterministicamente Over 1.5, ma NON le soglie superiori - es. 1-1 e' GG=Si ma Under 2.5). Analisi preliminare: individuato che colonne piu' ricche gia' presenti a DB (`form`/`for_`/`against`/`preview_matches`/`comparison`, storico Over/Under per soglia e forma squadra, popolate dall'endpoint "predictions" di API-Sports) NON sono utilizzabili come feature, ne' per goal_no_goal ne' per altri mercati: vengono scritte SOLO quando esiste gia' la riga `Statistics` della partita (box-score), MAI per una fixture futura non ancora giocata (`status=NS`) - usarle creerebbe uno skew training/serving (feature sempre mancante in produzione). Deciso di procedere con le sole feature gia' sicure per il serving (odds + `mean_statistics`, le stesse 69 colonne usate per gli Under/Over). Esportato `goal_no_goal.csv` (stesso pattern export-via-git, script `export_goal_no_goal_for_cloud_training.py`/`train_goal_no_goal_from_export.py`) e addestrato con la pipeline di produzione INVARIATA: 4475 righe, target ben bilanciato (53.4%/46.6%, il piu' bilanciato tra tutti i mercati testati finora), champion `logistic` selection_score=0.6678 (SMOTE ha corso come candidato ma non ha vinto, coerente con un mercato gia' bilanciato), registrato come `candidate`. Soglia ottimale Youden praticamente invariata rispetto a 0.5 (0.502) - a differenza degli Under/Over, qui il default non era degenere.

**Fase 4 - feature injection goal_no_goal -> Under/Over (2026-09-08)**: nuovo script `scripts/analysis/phase4_goal_no_goal_feature_injection.py`, che riusa l'infrastruttura anti-leakage di `phase3b_sequential_stacking_thresholds.py` (`_cascade_oof`/`_assert_cascade_no_future_leakage`) applicata a UNA sola feature sorgente (goal_no_goal) iniettata indipendentemente in ciascuna delle 4 soglie (non una cascata soglia-su-soglia). OOF di goal_no_goal calcolata walk-forward (RandomForest, stessa CV di produzione), unita per `id_fixture` al frame 'totals' gia' usato in fase 3b/3c. **Risultato: SCARTATO** - delta selection_score vs baseline sullo stesso sottoinsieme di righe (n_eval_rows=1524, ristretto dall'intersezione tra OOF goal_no_goal disponibile e OOF di ciascuna soglia): over_1_5 +0.0022, over_2_5 +0.0089, over_3_5 -0.0009, over_4_5 +0.0053 - tutti sotto la soglia di adozione (0.01), stesso esito della feature deterministica `gd_over_1_5` gia' scartata in Fase 3 (+0.0008). Interpretazione: goal_no_goal usa lo STESSO spazio di feature (odds + mean_statistics) degli Under/Over, quindi la sua probabilita' OOF e' in gran parte una trasformazione ridondante di informazione gia' disponibile al modello, non un segnale nuovo - anche per over_1_5, dove l'implicazione logica GG->Over1.5 e' deterministica sul risultato REALE (non sulla stima probabilistica del modello, che resta incerta). Risultato completo in `best_models/phase4_goal_no_goal_feature_injection_result.json`. Nessun impatto sui 4 champion Under/Over gia' registrati (nessuna modifica di produzione).

**Quote separate per lato Over/Under vs paniere unico (2026-09-09, proposta dell'operatore)**: oggi `_extract_market_odds_features` mette le quote Over e Under nello stesso paniere (odds_count conta il doppio dei bookmaker reali, mean/std mescolano i due lati). Testata la variante con statistiche separate per lato (`export_split_odds_for_cloud_training.py`, sottoclasse locale che non tocca `FilterMarketService`) - 5 feature per lato (count/mean/std/min/max, 10 totali) contro le 15 attuali (5 pooled + 10 quote ordinate). **Risultato: SCARTATO su tutti e 4 i mercati** - delta selection_score: 1.5 -0.0020, 2.5 +0.0016, 3.5 +0.0012, 4.5 +0.0088, tutti sotto la soglia di adozione 0.01 (nessuno chiaramente positivo, uno addirittura negativo). L'AUC e' quasi sempre piu' bassa con le quote separate (probabile costo delle 10 feature "quote ordinate" perse nella versione split, che catturavano comunque informazione sulla forma della distribuzione). Risultato in `best_models/split_odds_vs_pooled_result.json`. **Quinto esperimento di feature engineering di fila con esito negativo/marginale** (dopo gd_over_1_5, fase 4, fase 5, XGBoost/LightGBM): segnale sempre piu' forte che la pipeline attuale (odds pooled + mean_statistics + RandomForest/SMOTE) sia gia' vicina al limite di cosa questo spazio di feature puo' dare - i prossimi miglioramenti probabili richiedono dati genuinamente nuovi (movimento quote nel tempo, o il blocco form/storico pre-match bloccato dall'ingestion), non ulteriori trasformazioni delle feature esistenti.

**Bugfix ingestion quote goal_no_goal (2026-09-08, trovato durante la preparazione del test "quote separate per lato")**: `map_odds()` in `download_match_service.py` confrontava `alternate_value` (gia' lowercased) con `'Yes'` (maiuscola) - sempre falso, quindi sia "Yes" che "No" di Both Teams Score finivano sulla STESSA chiave `'no_goal_{bookmaker}'`, con la seconda occorrenza che sovrascriveva silenziosamente la prima: le quote `goal_no_goal` salvate a DB hanno un solo lato per bookmaker, mai entrambi (perdita di dati, non solo etichetta sbagliata). Stesso bug di case-sensitivity anche per "Draw/Away" nel Double Chance (qui senza perdita dati, solo etichetta `draw/away_` invece di `X2_`). **Fix applicato** (un carattere: confronto contro `'yes'`/`'draw/away'` minuscoli) + test di regressione, suite completa verificata (729 passed). **Nessun impatto sul target di training** (da `score_ft`, mai dalle quote). Il fix e' preventivo, non retroattivo: i dati gia' a DB restano quelli vecchi finche' non si fa un re-fetch mirato delle quote storiche (costa quota API-Sports) - non urgente dato che goal_no_goal non e' in produzione, decisione rimandata all'operatore per quando vorra' investirci.

**XGBoost/LightGBM vs baseline attuale (2026-09-08, proposta dell'operatore "cosa mi sta sfuggendo?")**: entrambi gia' dipendenze del progetto (`requirements.txt`) e usati nel codice legacy pre-v2, ma mai portati in `train_multi_market.py`/`_model_space` - stessa identica situazione di SMOTE prima di oggi. Confronto controllato (`scripts/analysis/test_gradient_boosting_vs_baseline.py`, iperparametri fissi non ottimizzati per isolare l'effetto della famiglia di modello, stesso walk-forward di produzione) su tutti e 5 i mercati (4 Under/Over + goal_no_goal). **Risultato: SCARTATO su tutti e 5** - XGBoost e LightGBM hanno selection_score e AUC SISTEMATICAMENTE piu' bassi di RandomForest (sia class_weight_balanced sia SMOTE) su ogni mercato, mai vicini alla soglia di adozione. A differenza di SMOTE (che ha vinto subito, dimostrando che l'idea era gia' valida), qui il segnale e' negativo e consistente, non marginale: RandomForest sembra genuinamente piu' adatto a questo volume di dati (migliaia, non centinaia di migliaia di righe) con iperparametri di default-ish, mentre gradient boosting tende a necessitare di piu' dati o tuning piu' fine per non essere in svantaggio. Non escluso che un tuning approfondito cambi l'esito, ma il gap e' abbastanza netto da non giustificarlo come priorita' immediata. Risultato completo in `best_models/gradient_boosting_vs_baseline_result.json`. Nessun impatto sui modelli gia' registrati/promossi.

**Fase 5 - feature injection Under/Over -> goal_no_goal, direzione OPPOSTA (2026-09-08, proposta dall'operatore dopo la promozione)**: idea distinta dalla fase 4 (non solo invertita: qui le 4 probabilita' Under/Over INSIEME, non una singola) - "se il modello predice Over su tutte le soglie fino a 4.5 c'e' probabilita' alta che esca anche Goal". Nuovo script `scripts/analysis/phase5_under_over_to_goal_no_goal.py`, stessa infrastruttura anti-leakage di fase 3b/4. Le 4 OOF Under/Over (RandomForest indipendenti, non a cascata) unite per `id_fixture` a goal_no_goal. **Risultato: SCARTATO** - baseline selection_score=0.6470, con le 4 feature aggiunte=0.6516, delta=+0.0046 (n_eval_rows=1250, campione piccolo per la tripla restrizione OOF totals+goal_no_goal+join) - sotto la soglia di adozione 0.01, terzo risultato consecutivo con delta piccolo e positivo (dopo `gd_over_1_5` +0.0008 e fase 4 max +0.0089): conferma che il vincolo reale e' lo spazio di feature CONDIVISO (odds+mean_statistics) tra tutti questi mercati, non la direzione o il numero di feature aggiunte - qualunque combinazione derivata da quello stesso spazio porta poco segnale incrementale. Risultato completo in `best_models/phase5_under_over_to_goal_no_goal_result.json`. Nessun impatto sui modelli gia' registrati/promossi.

Fix propedeutici (`src/service_ia/training/train_multi_market.py`): (1) griglia `RandomForestClassifier` alleggerita (`n_estimators` 500->200, combinazioni max_depth/min_samples_split/min_samples_leaf 36->8 per selector_k) per evitare il MemoryError che aveva bloccato il pilot; (2) **fix critico ulteriore**: `StackingClassifier(cv=cv_splits)` riusato dentro `cross_val_score`/`temporal_oof_probabilities`/`CalibrationService` su sottoinsiemi piu' piccoli di `X` causava sistematicamente `ValueError: cross_val_predict only works for partitions` — MAI stato eseguito con successo finora. Fix: `cv=5` (intero).

**Risultati reali FINALI, dopo l'aggiunta di SMOTE (dataset da DB Railway, trasferito via export CSV - vedi sotto):**

| Soglia | Champion | selection_score | rows | (era, senza SMOTE) |
|---|---|---|---|---|
| under_over_1_5 | random_forest | 0.7131 | 8349 | 0.7131 (SMOTE non ha vinto) |
| under_over_2_5 | voting | 0.6729 | 15243 | 0.6727 (SMOTE non ha vinto) |
| under_over_3_5 | **random_forest_smote** | **0.7008** | 8335 | 0.6906 |
| under_over_4_5 | **voting** (base: random_forest_smote+random_forest) | **0.7860** | 8286 | 0.7820 |

Tutti e 4 registrati come `candidate`, gate di promozione (`evaluate_promotion`) verificato su ciascuno: tutte le metriche standard lette correttamente, `allowed=true` (nessuna production precedente per questi mercati, prima promozione consentita) - all'epoca restavano `candidate` (nessuna promozione automatica). **Aggiornamento 2026-09-08**: promossi a `production` su richiesta esplicita dell'operatore, vedi sezione "Promozione a production" sopra.

`scripts/analysis/phase3b_sequential_stacking_thresholds.py` (idea "stacking sequenziale a cascata tra soglie", anti-leakage verificato esplicitamente anche in negativo) eseguito su dati reali: **SCARTATO** - delta selection_score vs baseline non-cascata sullo stesso sottoinsieme di righe: over_2_5 -0.0030, over_3_5 +0.0009, over_4_5 -0.0014, tutti sotto la soglia di adozione (0.01). Emerso anche un limite strutturale intrinseco: ogni livello di cascata consuma un fold aggiuntivo di dati per l'anti-leakage (n_eval_rows 7620 -> 6096 -> 4572 -> 3048). Risultato in `best_models/phase3b_sequential_stacking_thresholds_result.json`.

**Come e' stato risolto il blocco di rete** (la sessione cloud `anthropic_cloud` non puo' raggiungere il Postgres remoto Railway via TCP diretto, confermato non aggirabile a nessun livello di network access - vedi doc ufficiale ambienti cloud): nuovo script `scripts/analysis/export_datasets_for_cloud_training.py`, da eseguire in un ambiente CON accesso DB reale (sessione bridge sulla macchina locale dell'operatore), esporta le feature GIA' ELABORATE (non i JSON grezzi, troppo pesanti) per le 4 soglie + il frame 'totals' in CSV compatti (`scripts/analysis/_export/`, ~52MB totali, non gitignored di proposito per poter essere trasferiti via git). `scripts/analysis/train_from_export.py`/`phase3b_from_export.py` eseguono la logica di produzione INVARIATA (`train_market()`/`run_sequential_cascade()`) sostituendo solo la sorgente dati (CSV invece di query DB dirette). I modelli addestrati (file `.pkl` + registry, gitignored) sono stati consegnati all'operatore per essere posizionati nel `best_models/` della sua macchina locale (dove gira l'app reale). I CSV di export sono stati rimossi dal repo dopo l'uso (workaround temporaneo, non uno strumento di produzione permanente).

Nota: due tentativi precedenti di training diretto su una sessione bridge locale sono falliti per disconnessioni ripetute della sessione (macchina/rete dell'operatore, non un bug del codice) - da cui la scelta di spostare il training pesante in cloud e limitare la sessione bridge alla sola (breve) esportazione dati.

---

Roadmap operativa di base **COMPLETA** (53/53 task, 12/12 fasi — vedi `MANIFEST.json` e `IMPLEMENTATION_LOG.md`).

`LIVE-03` (ultimo task, Fase 11 LIVE ORACLE) è stato completato e validato.

Dopo LIVE-03 sono state completate e validate ulteriori estensioni extra-roadmap (2026-09-05/07, vedi `IMPLEMENTATION_LOG.md` sezione "Estensioni introdotte" per il dettaglio completo): bugfix critico gestione quota API-Sports, job "Aggiorna tutto"/Daily Refresh (bottone Sidebar sempre visibile), pagina Impostazioni (enable/disable job + quota API-Sports), consolidamento frontend Dashboard (rimosse le pagine separate Live/Today/Match Center, `PhaseTabs`/`MarketTabs` + select data accumulata), fix critici di performance/concorrenza DB (`scoped_session`, sessione persistente per job, bulk upsert `odds_snapshot`), auto-pausa job + disabilitazione bottoni a quota esaurita, fix critico performance cambio-giorno Dashboard (query multi-mercato consolidata, `get_overview` senza predizioni, skip API-Sports per date storiche gia' a DB, `force_refresh`, filtro Fase/Mercato client-side). Suite completa rieseguita: 712 passed. Import `src.api.main` verificato end-to-end (51 route). Build frontend verificata senza errori.

Il **07/09** e' stata inoltre completata un'estensione mirata su Under/Over 1.5/2.5/3.5/4.5 (MARKET-04), a fasi (0/1+2/3/4), dettaglio completo in `IMPLEMENTATION_LOG.md`: Fase 0 nuovo report Data Quality per soglia (`DataQualityService.build_under_over_threshold_report`); Fase 1+2 nuovo campo `best_approach_by_threshold` sul benchmark totals (rivela che `over_2_5` preferisce `binary_independent` mentre le altre 3 soglie preferiscono `goal_distribution`); **fix critico**: le metriche registrate dal benchmark totals non esponevano le chiavi standard richieste dal gate di promozione (OPS-02), che quindi falliva sempre per 'totals' — risolto; Fase 3 A/B test cascading sulla soglia 2.5 con esito negativo documentato (feature scartata, delta insufficiente). Script di analisi in `scripts/analysis/`, risultati in `best_models/*.json`. Test mirati verificati: 28/28 passed.

## Regola
Completare e validare questo task prima di aggiornare il file al task successivo.

## Task completati (vedi IMPLEMENTATION_LOG.md)
SOCCER-00, SOCCER-01, SOCCER-02, DATA-01..08, ML-01..07, FE-01..03, EXP-01..05 (fase ORACLE EXPERTS completata), MARKET-01..06 (fase MARKETS completata), ORACLE-01..04 (fase ENSEMBLE completata), BET-01..06 (fase BETTING completata), MATCH-01..02 (fase MATCH CENTER completata), SLIP-01..03 (fase SCHEDINA completata), OPS-01..03 (fase OPERATIONS completata), LIVE-01..03 (fase LIVE ORACLE completata)

## Prossimi in coda
Nessuno pianificato nella roadmap operativa attuale. Possibili prossimi passi (da concordare, non ancora un task formale):
- **COMPLETATO 2026-09-12 (lato codice)**: raffinamento feature arbitro (shrinkage bayesiano + indice di severita' normalizzato per lega) e report "tutte le metriche possibili" per Corners/Cards (MARKET-05/06) - vedi "Task corrente" sopra. **Ancora da fare**: eseguire `scripts/analysis/train_corners_cards_benchmark.py` contro il DB reale (richiede un ambiente con accesso diretto al Postgres Railway, non questa sessione cloud) e, in base ai risultati, decidere se/quali linee promuovere a `production`.
- **COMPLETATO 2026-09-08, aggiornato dopo SMOTE**: soglie di decisione ottimali (statistica J di Youden) per i 4 champion Under/Over registrati - `scripts/analysis/find_optimal_thresholds.py`, risultato in `best_models/optimal_thresholds.json`. Il cutoff 0.5 lasciava 3 modelli su 4 quasi ciechi sulla classe minoritaria (recall ~0-2%) pur con AUC 0.58-0.61: alla soglia ottimale il recall minoritario sale al 40-70%, a costo di un'accuracy aggregata piu' bassa (era gonfiata dal prevedere quasi sempre la classe maggioritaria). Soglie sui champion FINALI (con SMOTE): over_1_5=0.781, over_2_5=0.545, over_3_5=0.304, over_4_5=0.155 (soglie specifiche di QUESTI modelli, non generalizzabili ad altri; over_3_5/4_5 leggermente diverse dai valori pre-SMOTE per via del nuovo champion).
- **COMPLETATO 2026-09-08**: soglie orientate a precisione sulla classe "Over" per uso di betting (l'operatore vuole puntare quando il modello dice "Over") - `scripts/analysis/find_betting_thresholds.py`, risultato in `best_models/betting_thresholds.json`. A recall~25-30% (buon compromesso volume/affidabilita'): over_1_5 precisione 83.8% (base rate 76.5%), over_2_5 65.6% (base rate 53.4%), over_3_5 41.6% (base rate 29.7%, quasi 1.4x lift), over_4_5 22.6% (base rate 13.5%, quasi 1.7x lift) - il modello aggiunge piu' valore relativo dove l'evento e' piu' raro.
- **COMPLETATO 2026-09-08**: SMOTE testato (`scripts/analysis/test_smote_vs_class_weight.py`) e poi integrato come candidato strutturale in `_model_space` (`train_multi_market.py`, condiviso da tutti i mercati) - vedi sezione "Task corrente" sopra per i risultati finali. Registry, soglie ottimali, soglie di betting e report "model diagnostics" tutti rigenerati sui nuovi champion dopo il retrain.
- **COMPLETATO 2026-09-08 (con i champion PRE-SMOTE, non ancora rifatto sui nuovi)**: fase 3c, confronto "indipendente" vs "cascata sequenziale" (fase 3b) ENTRAMBE con coerenza monotona forzata in output (`enforce_monotonic_over_probabilities`, gia' in `totals_market.py`) - `scripts/analysis/phase3c_coherence_experiment.py`, risultato in `best_models/phase3c_coherence_experiment_result.json`. Su 3048 righe comuni (stesse fixture, tutte e 4 le soglie valutabili per entrambe le varianti): violazioni di monotonicita' PRIMA della proiezione ~13.9% dei confronti possibili per ENTRAMBE le varianti (1269/9144 indipendente, 1273/9144 cascata) - un problema reale, indipendente dall'approccio di training. La proiezione monotona migliora leggermente il selection_score aggregato in entrambi i casi (indipendente 0.6915->0.6929, cascata 0.6996->0.7005) - nessun costo, piccolo beneficio, va sempre applicata in serving qualunque sia il modello sottostante. Cascata vs indipendente dopo proiezione: selection_score aggregato cascata leggermente piu' alto (+0.008, ancora sotto la soglia di adozione 0.01 usata altrove nel progetto), ma le metriche di classificazione a soglia ottimale (confusion matrix/precision/recall) sono sostanzialmente equivalenti tra le due varianti (per over_1_5 sono identiche per costruzione, essendo la soglia radice della cascata). **Raccomandazione**: dato il guadagno marginale e sotto-soglia della cascata, a fronte della sua complessita' aggiuntiva e della perdita strutturale di dati per soglia (gia' documentata in fase 3b), restano preferibili i 4 modelli indipendenti + proiezione di coerenza monotona in serving (nessun retraining aggiuntivo necessario, la funzione di proiezione esiste gia'). **AGGIORNAMENTO 2026-09-09**: collegata al serving reale (`DashboardService._apply_monotonic_projection`, vedi "Task corrente" in cima al file).
- **COMPLETATO 2026-09-08 (con i champion PRE-SMOTE, non ancora rifatto sui nuovi)**: proiezione monotona applicata (in analisi, non ancora in serving) alle predizioni OOF dei 4 champion REALMENTE registrati (non un frame ad-hoc come la fase 3c sopra) - `scripts/analysis/apply_monotonic_to_champions.py`, risultato in `best_models/champions_monotonic_result.json`. Allineamento via `id_fixture` sull'intersezione dei 4 mercati (3789 fixture comuni su 4170/7620/4165/4140 disponibili per mercato). Violazioni di monotonicita' PRIMA della proiezione: solo 0.3% dei confronti (36/11367) - molto meno del 13.9% visto in fase 3c, segno che i champion reali (grid search completa + calibrazione) sono gia' internamente piu' coerenti del RandomForest leggero usato solo per l'esperimento di confronto. Effetto della proiezione sul selection_score: sostanzialmente nullo (variazioni ±0.0002, atteso vista la bassa incidenza) - la proiezione qui e' soprattutto una rete di sicurezza per il residuo 0.3% di casi, non una correzione sostanziale. Ancora da fare: rieseguire su champion post-SMOTE (non bloccante). **AGGIORNAMENTO 2026-09-09**: la proiezione monotona e' ora collegata al percorso di serving reale (`DashboardService._apply_monotonic_projection`, vedi "Task corrente" in cima al file) - non piu' solo un'analisi offline.
- Report "model diagnostics" (Artifact pubblicato, vedi Task corrente) aggiornato con confusion matrix/ROC/precision-recall dei champion FINALI (post-SMOTE): over_1_5 e over_2_5 pressoche' identici a prima (champion invariati), over_3_5 e over_4_5 leggermente diversi (nuovo champion).
- Promozione controllata (`ModelRegistry.promote_with_policy`) dei modelli `1x2_live` (LIVE-03) da `candidate` a `production` una volta raccolto un volume sufficiente di fixture concluse nel dataset live reale.
- Esposizione API/frontend delle probabilità live aggiornate (oggi solo `src/ml/live/`, nessun endpoint dedicato).
- Eventuale nuova roadmap di crescita (vedi `ROADMAP_CRESCITA_ML_DASHBOARD.md`, documento di contesto più datato, in gran parte già superato dalle 12 fasi completate).

La roadmap completa è in `ROADMAP_OPERATIVA.md`.

















