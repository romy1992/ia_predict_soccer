# Soccer Oracle V2 - Implementation Log

## Baseline (SOCCER-00)
- base branch verificato: `feature/ml-dashboard-platform`
- branch operativo attivo: `feature/soccer-oracle-v2`
- baseline SHA iniziale: `d12a661`
- data verifica: 2026-09-02

## Premessa comune prima di ogni task
Prima di ogni task viene applicata la premessa in `AI_MASTER_PROMPT.md`:
1. ispezione file coinvolti
2. no riscritture inutili
3. compatibilita DB storico
4. migration Alembic per cambi DB
5. no leakage temporale
6. no split random in pipeline production
7. latest model != production model
8. aggiornare test pertinenti

## Sequenza immediata (CURRENT_TASK)
1. SOCCER-00
2. SOCCER-01
3. SOCCER-02
4. DATA-01
5. DATA-02
6. DATA-03
7. DATA-04
8. DATA-06
9. ML-01
10. ML-02

## Stato implementazione
- [x] SOCCER-00
- [x] SOCCER-01
- [x] SOCCER-02
- [x] DATA-01
- [x] DATA-02
- [x] DATA-03
- [x] DATA-04
- [x] DATA-06
- [x] ML-01
- [x] ML-02
- [x] DATA-05
- [x] DATA-07
- [x] ML-03
- [x] DATA-08
- [x] FE-01
- [x] FE-02
- [x] FE-03
- [x] ML-04
- [x] ML-05
- [x] ML-06
- [x] ML-07
- [x] EXP-01
- [x] EXP-02
- [x] EXP-03
- [x] EXP-04
- [x] EXP-05
- [x] MARKET-01
- [x] MARKET-02
- [x] MARKET-03
- [x] MARKET-04
- [x] MARKET-05
- [x] MARKET-06
- [x] ORACLE-01
- [x] ORACLE-02
- [x] ORACLE-03
- [x] ORACLE-04
- [x] BET-01
- [x] BET-02
- [x] BET-03
- [x] BET-04
- [x] BET-05
- [x] BET-06
- [x] MATCH-01
- [x] MATCH-02
- [x] SLIP-01
- [x] SLIP-02
- [x] SLIP-03

## Estensioni introdotte
- settlement job idempotente con completezza finale (`/jobs/settlement`)
- overview settlement (`/settlement/overview`)
- report Data Quality machine-readable (`/data/quality`)
- pipeline training con selector dentro CV fold (anti-leakage)
- migrazione Alembic per campi settlement su `match`
- job history evoluta con lifecycle queued/running/success/failed e filtri tipo/stato
- refactor frontend a feature components + router locale (`frontend/src/features/`)
- Data Center FE con import storico/oggi/future e settlement manuale
- Data Quality FE con coverage/anomalie/distribuzioni e filtri stagione/lega
- migration compatibilità schema `match` (`current_league`, `league_match`, `status`, `mean_statistics`)
- baseline bookmaker implied/fair probabilities nei report match detail
- framework metriche probabilistiche (LogLoss, Brier, ECE, AUC) e champion selection multi-metrica
- calibration framework (Platt/sigmoid + isotonic) con confronto pre/post e calibratore versionato per model run (`src/ml/calibration/calibration_service.py`)
- Model Registry con lifecycle completo candidate/champion/production/retired, promotion history, lookup produzione per mercato (`src/service_ia/training/model_registry.py`)
- Team Strength Expert: rating offensivo/difensivo, home advantage, rolling form point-in-time, backtest base (`src/ml/experts/team_strength/team_strength_expert.py`)
- Goal Distribution Expert: Poisson lambda regressor con validazione temporale (no train_test_split), soglie U/O 1.5/2.5/3.5/4.5, score distribution completa e confronto Poisson vs Binomiale Negativa (`src/ml/experts/goal_distribution/goal_distribution_expert.py`)
- Statistics Expert: modello pre-match basato esclusivamente su `mean_statistics` (nessuna feature odds), validazione temporale con metriche probabilistiche ML-05, embedding numerico riusabile (`src/ml/experts/statistics/statistics_expert.py`)
- Market/Odds Expert: fair probabilities (riuso bookmaker_baseline), dispersione bookmaker, movement quote e opening/latest/closing point-in-time-safe (closing mai valorizzato prima del kickoff) (`src/ml/experts/market/market_odds_expert.py`)
- Direct Market Expert: interfaccia comune `predict_proba` sui champion model esistenti (h2h/dc/goal_no_goal/corners/cards/under_over_*), con metadati espliciti che impediscono di scambiare h2h binario per un 1X2 multiclasse (`src/ml/experts/direct/direct_market_expert.py`)
- **Fase ORACLE EXPERTS completata (EXP-01..05)**
- Vero mercato 1X2 multiclass (HOME/DRAW/AWAY, nessun mapping draw->away): dataset dedicato, GridSearchCV logistic/random_forest con validazione temporale, metriche multiclasse dedicate (log_loss/brier generalizzato/ECE su confidence/AUC OvR) e calibrazione multiclasse via `CalibratedClassifierCV` (`src/ml/markets/market_1x2.py`, `src/ml/evaluation/multiclass_probability_metrics.py`, `src/ml/calibration/multiclass_calibration_service.py`)
- Double Chance derivata da 1X2 coerente (nessun training proprio): P(1X)=P(HOME)+P(DRAW), P(12)=P(HOME)+P(AWAY), P(X2)=P(DRAW)+P(AWAY), fair odds e wrapper batch su `Market1x2Expert` (`src/ml/markets/market_double_chance.py`)
- BTTS consolidato (score distribution EXP-02 vs direct expert 'goal_no_goal' EXP-05): benchmark comparativo (score_distribution/direct_expert/ensemble) con selezione via `champion_probability_score`, calibrazione OOF temporale (Platt/isotonic) del solo approccio vincente, P(Yes)+P(No)=1 garantito per costruzione (No=1-Yes); dataset builder end-to-end che riusa rating point-in-time (`TeamStrengthExpert`, EXP-01) + score distribution Poisson (`GoalDistributionExpert`, EXP-02) + feature/target 'goal_no_goal' (`FilterMarketService`, EXP-05); orchestratore `run_btts_benchmark(_from_db)` che registra il calibratore vincente come 'candidate' (mai 'production' automatica) (`src/ml/markets/btts/btts_market.py`)
- U/O 1.5-4.5 multi-linea consolidato: confronto sullo STESSO walk-forward tra binary_independent (4 classificatori scorrelati), hierarchical (1 solo classificatore multiclasse sui bin di gol totali 0/1/2/3/4, confini esattamente sulle soglie, P(Over t) = cumulata dall'alto) e goal_distribution (Poisson EXP-02 da rating EXP-01); selezione per metriche probabilistiche+betting (`champion_probability_score`) aggregate sulle 4 soglie; monotonicità P(O1.5)>=P(O2.5)>=P(O3.5)>=P(O4.5) resa OBBLIGATORIA sull'output finale via proiezione isotonica (`enforce_monotonic_over_probabilities`), indipendentemente dall'approccio vincente; orchestratore `run_totals_benchmark(_from_db)` che registra il modello vincente come 'candidate' solo se introduce un nuovo estimator (hierarchical/binary_independent), nessun salvataggio per goal_distribution (deterministico) (`src/ml/markets/totals/totals_market.py`)
- Corners O/U specializzato con linea configurabile (8.5/9.5/10.5/11.5 come parametro, non più soglia fissa a 9.5): feature dedicate (media storica corner fatti/concessi per squadra, totale atteso, differenziale, da `mean_statistics['Corner Kicks']`) oltre alle generiche odds/mean_stats; calibrazione (Platt/isotonic) via `CalibrationService` (ML-06, riusata) per il modello di ciascuna linea; report con metriche per linea; `CornersExpert` parametrico sulla linea, ogni linea registrata come mercato indipendente nel registry (es. `corners_line_9_5`) (`src/ml/markets/corners/corners_market.py`)
- Cards O/U specializzato con linea configurabile (3.5/4.5/5.5/6.5 come parametro): feature "team/style" già disponibili genericamente da `mean_statistics` (Yellow/Red Cards, Fouls) più feature ARBITRO nuove e point-in-time (`build_referee_features_dataset`: media storica cartellini elargiti nelle partite precedenti arbitrate dallo stesso arbitro, nessun leakage, fallback a prior neutro senza storico); calibrazione via `CalibrationService` (ML-06) per ciascuna linea; `CardsExpert` parametrico sulla linea, stesso pattern registry di `CornersExpert` (`src/ml/markets/cards/cards_market.py`)
- **Fase MARKETS completata (MARKET-01..06)**
- Schema di output standardizzato per gli Oracle Experts (dataclass `ExpertOutput`: expert_name/expert_version, probability_vector con validazione [0,1], model_run_id, stage, feature_timestamp, confidence con default automatico, metadata, raw_output) e `combine_expert_outputs` per assemblare la riga di feature multi-esperto senza collisioni di naming (`src/ml/ensemble/expert_output.py`); adapter dedicati per ciascun esperto esistente SENZA modificarli (EXP-01 team_strength: probability_vector vuoto per costruzione, solo rating/feature; EXP-02 goal_distribution: over/under da Poisson o da score matrix; EXP-03 statistics: vettore binario complementare dalla probabilità calibrata; EXP-04 market_odds: fair probabilities da `compute_market_baseline`; adapter generico `from_predict_proba_expert` per EXP-05 `DirectMarketExpert` + MARKET-05/06 `CornersExpert`/`CardsExpert`, che condividono la stessa interfaccia `predict_proba`/`predict_proba_dict`) (`src/ml/ensemble/adapters.py`)
- **ORACLE-01 completato**: tutti gli esperti sono consumabili da un futuro meta-model tramite lo schema comune (acceptance criteria verificato dal test end-to-end `test_outputs_from_different_experts_are_consumable_by_meta_model`)
- Meta Model / Stacker per mercato (ORACLE-02): confronto, sulle STESSE meta-feature (probabilita' dei singoli Oracle Expert per lo stesso mercato, tipicamente da `combine_expert_outputs`/ORACLE-01), tra `weighted_blend` (`WeightedBlendClassifier`, pesi non negativi che sommano a 1, ottimizzati via softmax+minimizzazione log loss) e `learned_stacker` (default `LogisticRegression`); ENTRAMBI validati con lo stesso meccanismo di OOF walk-forward gia' in uso nel progetto (`temporal_oof_probabilities`, ML-05, mai fit sul fold di validazione), selezione del vincitore via `champion_probability_score` e meta-model finale rifittato sull'intero storico; `build_meta_features_from_expert_outputs` collega esplicitamente lo schema ORACLE-01 alle meta-feature; orchestratore `run_stacking_benchmark` che registra il meta-model vincente come 'candidate' (mai 'production' automatica) (`src/ml/ensemble/stacking.py`)
- Calibrazione finale Oracle Ensemble (ORACLE-03): applica `CalibrationService` (ML-06, gia' riusata da corners/cards, non modificata) al meta-model vincitore di ORACLE-02 (`benchmark_stacking_approaches`), stessi fold walk-forward gia' usati per selezionare weighted_blend/learned_stacker (nessun nuovo meccanismo di split); report pre/post metrics (log_loss/brier/ece) esposto in `OracleEnsembleCalibrationReport`; fallback esplicito e non bloccante — nessuna eccezione propagata al chiamante — quando il campione e' sotto `DEFAULT_MIN_CALIBRATION_SAMPLES` (60 righe) o la calibrazione fallisce per qualunque motivo: in quel caso il meta-model raw (non calibrato) di ORACLE-02 viene usato cosi' com'e', con il motivo riportato in `fallback_reason`; orchestratore `run_oracle_ensemble_calibration` che registra il modello finale (calibrato o raw fallback) come 'candidate' (mai 'production' automatica) (`src/ml/ensemble/oracle_calibration.py`)
- Model Consensus per spiegabilita' (ORACLE-04): per una singola fixture/mercato, assembla l'output di Direct Expert (EXP-05, modello registrato per quel mercato) e Market/Odds Expert (EXP-04, fair probability dalle quote gia' salvate in `odds_snapshot`) — GENERICAMENTE applicabili a QUALUNQUE mercato di `FilterMarketService.SUPPORTED_MARKETS`, a differenza di team_strength/goal_distribution/statistics volutamente esclusi (rispettivamente: nessun probability_vector per costruzione, non generico oltre i mercati "gol", nessuna persistenza su registry); l'Oracle finale usa il meta-model registrato (ORACLE-02/03, cercato ESPLICITAMENTE per prefisso `model_name` — mai un `get_latest(market)` generico che confonderebbe meta-model e modello diretto sullo stesso mercato) quando disponibile, altrimenti la media semplice delle probabilita' comparabili (fallback esplicito, mai un'eccezione); dispersione = deviazione standard tra le probabilita' comparabili, con etichetta di accordo (high/medium/low); nessuna spiegazione testuale generata, solo numeri/metadati calcolati (`src/ml/ensemble/model_consensus.py`); esposto tramite `GET /dashboard/match/{fixture_id}/consensus?market=...` (`src/api/main.py`, `src/api/schemas.py::ModelConsensusResponse`)
- **Fase ENSEMBLE completata (ORACLE-01..04)**
- Fair Odds Engine (BET-01): standardizza, per ciascun outcome di un mercato, `p_market_raw`/`p_market_fair` (overround rimosso, RIUSA `compute_market_baseline` di ML-04 senza duplicarlo) + `fair_odd = 1/p_market_fair` + `p_model` (Oracle, quando fornito dal chiamante — tipicamente da `model_consensus.py`, ORACLE-04) affiancati per lo stesso outcome ("Confronto Oracle vs market"); scope DELIBERATAMENTE limitato alle sole probabilita'/quote (NESSUN calcolo di `prob_edge`/`EV`/decision: quello e' BET-02, dipendenza tecnica esplicita, "REWRITE" di `DashboardService._value_decision`); outcome con `p_model` noto ma senza quota di mercato (o viceversa) restano comunque nell'output invece di sparire silenziosamente; nuovo package `src/oracle/` (primo modulo della Fase BETTING, distinto da `src/ml/`) (`src/oracle/fair_odds/fair_odds_engine.py`)
- Value Engine (BET-02, REWRITE): sostituisce il precedente calcolo che chiamava impropriamente "edge" un Expected Value; distingue esplicitamente `prob_edge = p_model - p_market_fair` (differenza di probabilita', indipendente dalla quota — invariante se la quota cambia a parita' di probabilita') da `ev = p_model*odd-1` (valore atteso in termini di quota, cio' che il codice precedente calcolava sotto il nome "edge"); soglie di decisione PLAY/BORDERLINE/NO BET VERSIONATE (`ValueDecisionPolicy`, mai hardcoded inline nel corpo della funzione, stessi valori numerici gia' in uso per non introdurre un cambio di policy non richiesto da questo task); consuma direttamente `FairOddsOutcome` (BET-01) garantendo per costruzione che market/outcome/quota si riferiscano allo stesso outcome; corretto in `DashboardService._pick_and_odd_for_prediction` il bug per cui il pick "Away" sul mercato h2h poteva usare la quota "Draw" come fallback (outcome scorretto: ora resta `None` se manca la quota "Away", gestito esplicitamente dal Value Engine) (`src/oracle/value_engine/value_engine.py`, wiring in `src/api/dashboard_service.py::_build_decision_cards`)
- Betting Backtester (BET-03): per un singolo mercato di `FilterMarketService.SUPPORTED_MARKETS`, genera `p_model` SOLO out-of-sample riusando lo STESSO walk-forward gia' validato altrove (ML-05: `expanding_window_splits` + `temporal_oof_probabilities`, nessuna nuova logica di split/leakage), lo abbina alle quote storiche REALI per lo stesso outcome (fair odds via `compute_market_baseline`/BET-01: per corners/cards viene usata ESCLUSIVAMENTE la linea che coincide con la soglia target di `FilterMarketService`, 9.5/4.5 — una linea non quotata per quella fixture resta esplicitamente NO BET invece di un fallback silenzioso su una quota di una linea diversa che distorcerebbe ROI/EV) e valuta ciascuna bet con il Value Engine (BET-02, non modificato); calcola, con stake flat, ROI/yield/profit, hit rate, avg odds, max drawdown (peak-to-trough sulla curva di equity cumulata) e la scomposizione per edge bucket/mercato/lega, con ordinamento cronologico deterministico che rende il report indipendente dall'ordine di input passato dal chiamante ("Report riproducibile"); parte "pura" testabile con numeri fissi e zero dipendenze DB/ML (`BacktestBet`/`compute_backtest_report`, `src/oracle/backtest/betting_backtester.py`) separata dall'orchestratore che collega dataset reale/OOF/fair-odds/value-engine (`run_market_backtest(_from_db)`, `src/oracle/backtest/market_backtest.py`)
- Decision Policy versionata (BET-04, REWRITE): estende `ValueDecisionPolicy` (BET-02, un'unica istanza GLOBALE identica per ogni mercato/outcome) con soglie che possono variare ESPLICITAMENTE per mercato e per singolo outcome di un mercato (`DecisionPolicy.thresholds_for`, priorita' outcome > mercato > default), piu' due nuovi filtri richiesti dall'acceptance criteria: `min_samples` (numero minimo di bookmaker che quotano l'outcome, da `FairOddsOutcome.bookmakers` gia' calcolato da BET-01/ML-04 — sotto soglia, NO BET indipendentemente da edge/EV) e `min_odd`/`max_odd` opzionali (range di quota accettabile, NO BET se fuori range); riusa DIRETTAMENTE `compute_prob_edge`/`compute_expected_value` (BET-02, mai duplicati); `DEFAULT_DECISION_POLICY` (`decision_policy_v1`) riproduce ESATTAMENTE lo stesso comportamento di `DEFAULT_POLICY` (BET-02) quando i nuovi filtri non sono vincolanti (`min_samples=0`, `min/max odd=None`) — nessun override per-mercato precaricato in produzione, la capacita' e' disponibile ma non attivata finche' una taratura specifica non sia esplicitamente richiesta (nessun cambio di policy non richiesto da questo task); `dashboard_service.py::_build_decision_cards` aggiornato per usare `evaluate_decision_from_fair_odds_outcome` (passando `bookmakers` come `samples`) al posto della chiamata diretta al Value Engine — nessuna soglia numerica hardcoded nel dashboard service (acceptance criteria) (`src/oracle/decision_engine/decision_policy.py`)
- Closing Line Value — CLV (BET-05, NEW): calcola quanto la quota presa al momento della decisione (`odd_at_bet`) e' stata migliore/peggiore della vera quota di CHIUSURA (`closing_odd`), metrica di valutazione EX-POST ("quando disponibile": SOLO per le fixture con `OddsSnapshot`/DATA-06 registrati fino al kickoff — mai per il backtest storico su `match.odds`, singolo valore senza serie temporale, per cui il CLV resta esplicitamente non disponibile); riusa `build_opening_latest_closing` (EXP-04) chiamato con `as_of=kickoff_at` per ottenere per costruzione l'ultimo snapshot PRIMA del kickoff per bookmaker/outcome (mai una quota "live" post-kickoff spacciata per chiusura) e `compute_market_baseline`/`get_market_outcome_baseline` (ML-04/BET-01) per la fair probability aggregata al closing; espone due metriche distinte — `clv_odd_pct = odd_at_bet/closing_odd - 1` (quota) e `clv_prob = p_fair_at_bet - p_fair_closing` (probabilita' fair) — mai calcolate come feature di training (acceptance criteria "Nessun uso del closing price come feature pre-match illegittima": modulo puramente descrittivo/ex-post); nessuna migration DB necessaria (schema `odds_snapshot` gia' introdotto da DATA-06); report aggregato per mercato/modello (`compute_clv_report`) con conteggio esplicito disponibili/non disponibili, mai nascosto (`src/oracle/backtest/clv.py`)
- **Fix critico CrudRepository.search_filter** (scoperto durante un retrain manuale multi-mercato che falliva SEMPRE, per qualunque mercato, indipendentemente dai dati presenti — `retrain_run.log`): (1) `col.is_not(None)`/`is_(None)` su relazioni ORM one-to-many (`Match.odds`/`Match.statistics`) solleva `NotImplementedError` in SQLAlchemy — fix con `_is_relationship_attribute` che usa `.any()`/`~.any()` per le relazioni, invariato per le colonne scalari; (2) `Match.mean_statistics` (colonna JSON) salvava un Python `None` come letterale JSON `'null'` anziche' SQL `NULL` (default SQLAlchemy `JSON(none_as_null=False)`), rendendo il filtro `mean_statistics: not None` un NO-OP silenzioso su TUTTA la pipeline multi-mercato (`filter_market_service.py`, `point_in_time_builder.py`, `corners_market.py`, `totals_market.py`, `cards_market.py`, `market_backtest.py`) — fix con `JSON(none_as_null=True)` (`src/service_ia/model/match.py`); verificato via query diretta sul DB locale che non ci sono righe storiche da bonificare con una migration di data-fix (0 righe con `'null'` letterale su 3 totali). Test con vero SQLite in-memory (non mock, per esercitare davvero la query SQLAlchemy generata) in `tests/service/crud_repository_test.py`; suite completa rieseguita dopo il fix: 395 passed, 0 failed (`src/repository/base/crud_repository.py`, `src/service_ia/model/match.py`)
- Prediction Ledger / Paper Betting (BET-06): nuova tabella `prediction_ledger` (migration `f3a9c1d8e2b7`, validata upgrade/downgrade/upgrade sul DB reale) che separa ESPLICITAMENTE i campi "originali" scritti UNA SOLA VOLTA prima del kickoff (fixture_id/market/outcome/model_run_id/model_name/policy_version/p_model/p_market_fair/odd/fair_odd/prob_edge/ev/decision/stake/kickoff_at/created_at) dai campi di SETTLEMENT scritti SOLO dopo il risultato reale (is_settled/settled_at/settlement_status/actual_outcome/won/pnl) — "immutabilita' logica della prediction originale" garantita dalla service layer (`settle_prediction_record` usa `dataclasses.replace`, mai una mutazione in-place) e verificata da un test dedicato che confronta il record pre/post settlement; modulo puro (`src/oracle/ledger/prediction_ledger.py`, nessun DB, stesso stile BET-01/02/03) con `build_prediction_record` (riusa DIRETTAMENTE `Decision`/BET-04: market/outcome/p_model/p_market_fair/odd/prob_edge/ev/decision/policy_version provengono TUTTI dalla stessa valutazione, garanzia gia' vista in BET-01/02/04 che si riferiscano allo stesso outcome), `resolve_actual_outcome` (esito REALE riusando `FilterMarketService._label_by_market` + `_canonical_outcome_for_prediction`/BET-03, mai una nuova logica di labeling) e `compute_paper_pnl` (stessa formula di `bet_profit`/BET-03, duplicata deliberatamente per mantenere il ledger indipendente dal backtest storico, coerenza numerica garantita da test); orchestrazione DB in `src/oracle/ledger/ledger_service.py` (`PredictionLedgerService`): `log_prediction` idempotente per (fixture/market/outcome/model_run_id), `settle_pending`/`settle_prediction` settlano SOLO le prediction il cui match e' effettivamente concluso (`FINAL_STATUSES`, riusato da `settlement_service.py`) — le altre restano pending, mai forzate; nessun esito inventato quando le statistiche non sono disponibili (`settlement_status='void_no_result'`) o quando manca la quota (`'void_missing_odd'`); report PnL doppio: `raw_pnl_summary` (somma diretta dei `pnl` gia' salvati, sempre affidabile) e `paper_pnl_report` (RIUSA `compute_backtest_report`/BET-03 costruendo `BacktestBet` dalle righe settled, per ROI/hit-rate/max drawdown/edge bucket); endpoint `POST/GET /predictions/ledger`, `GET /predictions/ledger/fixture/{fixture_id}`, `POST /predictions/ledger/settle`, `GET /predictions/ledger/pnl` (`src/api/main.py`, `src/api/schemas.py`); repository dedicato stesso pattern di `OddsSnapshotRepository` (`src/repository/prediction_ledger_repository.py`); 26 nuovi test (pure + service con vero SQLite in-memory, stesso principio di `crud_repository_test.py`) in `tests/service/prediction_ledger_test.py`; `src/service_ia/training/prediction_logger.py` (JSONL) NON modificato/rimosso (compatibilita', endpoint `/predict/{market}` e `/predictions/log` invariati) — il Ledger e' un componente NUOVO e distinto, non ancora agganciato all'endpoint `/predict/{market}` (nessuna riscrittura non richiesta di un endpoint funzionante: il chiamante che ha gia' calcolato p_model/odd puo' loggare esplicitamente via `POST /predictions/ledger`)
- **Fase BETTING completata (BET-01..06)**

- Match Center con output Oracle ufficiali (MATCH-01, REFACTOR): fino a questo task `decision_cards` (probabilita'/quota/fair probability/edge/EV/badge PLAY-BORDERLINE-NO BET, BET-01/02/04) erano calcolati SOLO nel dettaglio match (`get_match_detail`, una fetch odds dedicata per singola fixture); la vista LISTA (`get_day_matches`/`get_live_matches`, la tabella "Match Center" dove si scorrono tutte le partite del giorno) mostrava solo probabilita' grezza del modello, senza alcun confronto col mercato. Estesi `_serialize_match`/`_serialize_api_fixture` con un nuovo metodo `_decisions_for_row` che riusa DIRETTAMENTE `_build_decision_cards` (mai una logica duplicata) calcolato SOLO quando le quote sono gia' disponibili SENZA fetch aggiuntive: da `match.odds` gia' caricato dalla query unica di `_fetch_matches`/`selectinload` (fix performance esistente) - MAI una nuova chiamata odds API-Sports per riga (quota giornaliera limitata, gia' vista esaurirsi nei job history). Nuovo helper `_db_matches_lookup(target_date)` (dict fixture_id->Match da un'UNICA query) riusato sia da `get_day_matches` sia da `get_live_matches` per arricchire le righe API con gli odds DB quando la fixture esiste gia' localmente, altrimenti `decision_cards=[]`/`best_decision=None` (mai un dato inventato - il dettaglio match resta comunque completo, fa la sua fetch odds dedicata). Nuovo `_select_best_decision_card` (priorita' PLAY>BORDERLINE>NO BET poi probabilita' piu' alta) per il badge sintetico di riga. Aggiunto anche `fair_odd` (BET-01, gia' calcolato ma non esposto) alle decision_cards esistenti ("fair market" richiesto dall'acceptance criteria). Frontend: nuovo componente `ValueBadge` (`frontend/src/features/matches/components/ValueBadge.jsx`) che mostra `row.best_decision` in una colonna dedicata di `MatchTable` — SOLO formattazione di dati gia' calcolati dal backend, NESSUN calcolo di edge/EV/decisione lato client (acceptance criteria "FE non ricalcola logica betting"); `MatchDetailPanel` arricchito con quota fair ed EV espliciti (prima solo "Edge"); voce di menu "Partite del giorno" rinominata "Match Center" (terminologia target, `docs/DOCUMENTAZIONE_PROGETTO_SOCCER_ORACLE.md` sezione 5.1). Nessuna migration DB (nessun cambio schema). 2 nuovi test in `tests/service/dashboard_service_test.py` (arricchimento riga quando la fixture e' nel DB con odds; nessun badge quando disponibile solo da API) + 1 assert aggiuntivo su `fair_odd` nel test dettaglio esistente; suite completa rieseguita: 425/425 passed. Build frontend (`npm run build`) verificata senza errori.
- **Oracle Match Detail (MATCH-02, NEW)**: nuovo `src/api/oracle_match_detail_service.py::OracleMatchDetailService` che aggrega, per UNA fixture, le 8 sezioni richieste dall'acceptance criteria ("Dettaglio navigabile per fixture") RIUSANDO direttamente i moduli gia' esistenti (nessuna logica duplicata): (1-3) Overview/Probabilities/Value Bets da `DashboardService.get_match_detail` (MATCH-01, invariato) - `value_bets` e' il sottoinsieme di `decision_cards` con `value_label` in {PLAY, BORDERLINE}, stessa etichetta gia' prodotta da BET-04; (4) Team Strength (EXP-01): rating PRE-match delle due squadre calcolato da `TeamStrengthExpert.current_ratings` su una query MIRATA (`MatchRepository.search_filter` con `OR` su `id_team_home`/`id_team_away`, mai l'intero storico DB) filtrata point-in-time in Python (`date_match < kickoff`, stesso pattern lessicografico gia' in uso in `DashboardService._fetch_matches` - nessun leakage se la fixture e' gia' conclusa); (5-6) Expected Goals + Score Matrix (EXP-02): lambda Poisson stimata DETERMINISTICAMENTE dai rating appena calcolati (`GoalDistributionExpert.estimate_lambdas_from_ratings` + `build_expert_output`, nessun training/modello necessario), chiavi della score matrix stringificate esplicitamente per un payload JSON non ambiguo; (7) Odds Movement: `OddsSnapshotRepository.opening_latest_closing` (sola visualizzazione, esplicitamente NON `MarketOddsExpert`/EXP-04 che e' invece la variante point-in-time-safe per la pipeline ML - vedi docstring del modulo); (8) Model Consensus (ORACLE-04): `build_model_consensus_for_fixture` richiamato per ciascun mercato in `model_markets`, un fallimento su un singolo mercato non blocca gli altri. Acceptance criteria "Dati mancanti gestiti": ogni sezione e' calcolata in modo indipendente e avvolta in try/except, con `warnings` esplicito (es. `team_strength_not_available`, `odds_movement_not_available`) MAI un valore inventato ne' un'eccezione che blocca l'intero dettaglio. Nuovo endpoint `GET /dashboard/match/{fixture_id}/oracle-detail` (`src/api/main.py`, schema `OracleMatchDetailResponse` in `src/api/schemas.py`). Frontend: nuova pagina `frontend/src/features/oracle/OracleMatchDetailPage.jsx` (navigabile tramite nuovo pulsante "Oracle" in `MatchTable` e link "Oracle Match Detail →" nel pannello rapido `MatchDetailPanel`, nuovo stato `activePage="oracle-detail"` con `AppRouter`/`App.jsx` che ricorda la pagina di provenienza per il pulsante "torna indietro") - mostra tutte le 8 sezioni con Score Matrix come tabella (troncata a 0-5 gol per leggibilita', il dato completo resta disponibile via API), SOLO formattazione di dati gia' calcolati dal backend (nessun calcolo lato client). Nessuna migration DB (nessuna nuova tabella). 5 nuovi test in `tests/service/oracle_match_detail_service_test.py` (happy path end-to-end con tutti i mock, fixture mancante con warnings completi, esclusione point-in-time delle partite post-cutoff, fallimento parziale del model consensus); suite completa rieseguita: 430/430 passed. Build frontend verificata senza errori; import `src.api.main` verificato end-to-end (32 route registrate).
- **Pick Pool per Schedina Oracle (SLIP-01, NEW)**: nuovo package `src/oracle/betslip/` (prima cartella della Fase SCHEDINA), stesso pattern a due livelli gia' consolidato nella Fase BETTING (modulo puro + orchestratore DB-aware). `pick_pool.py` (puro, nessun DB/IO): `PickPoolPolicy` versionata (soglie MAI hardcoded, stesso principio di `DecisionThresholds`/BET-04) con `include_borderline` (default `False`, solo PLAY), `min_odd`/`max_odd`/`min_ev`/`min_prob_edge`/`min_samples` opzionali; `PickPoolPolicy.with_overrides(...)` calcola una `version` derivata DETERMINISTICAMENTE (hash breve dei parametri, stesso principio di `TeamStrengthExpert.VERSION`) quando i parametri differiscono dal default - mai la stessa `policy_version` per soglie diverse. `build_pick_pool(candidates, policy)` applica la policy (motivo di esclusione esplicito per ogni scarto: `decision_not_allowed`/`odd_missing`/`odd_below_min`/`odd_above_max`/`ev_below_min`/`prob_edge_below_min`/`samples_below_min`), poi impone "una selezione per outcome/mercato" (raggruppamento per `(fixture_id, market)`, vincitore per priorita' PLAY>BORDERLINE poi EV poi prob_edge, il perdente finisce comunque in `excluded` con motivo `superseded_same_fixture_market`), infine ordina in modo deterministico (EV decrescente, poi fixture_id/market) e calcola `pool_id` (hash SHA1 dai pick inclusi, stabile indipendentemente dall'ordine di input - acceptance criteria "Pool deterministico e tracciabile", verificato esplicitamente da test dedicati). `pick_pool_service.py` (DB-aware): `candidate_from_decision_card` mappa UNA riga di `decision_cards` (gia' calcolata da `DashboardService`, MATCH-01/BET-01/02/04) a `CandidatePick` senza ricalcolare nulla; `PickPoolService.candidates_for_day`/`build_pool_for_day` riusano `DashboardService.get_day_matches` COSI' COM'E' (stessa query unica DB gia' ottimizzata, nessuna nuova fetch odds/predizioni). Nuovo endpoint `GET /betslip/pool` (parametri opzionali `include_borderline`/`min_odd`/`max_odd`/`min_ev`/`markets`/`target_date`, schema `BetslipPoolResponse` in `src/api/schemas.py`). Nessuna migration DB (nessuna nuova tabella: il pool e' calcolato on-demand, non persistito - la persistenza nel Prediction Ledger, se richiesta, resta un'azione esplicita separata via BET-06). Nessun frontend toccato (non richiesto dal task, che indica solo `src/oracle/betslip/`; la UI arrivera' con SLIP-03). 23 nuovi test (`tests/service/pick_pool_test.py`: policy/validazione/filtri/dedup/determinismo/pool_id; `tests/service/pick_pool_service_test.py`: adapter + orchestrazione con `DashboardService` mockato); suite completa rieseguita: 453/453 passed. Import `src.api.main` verificato end-to-end (33 route registrate); tutti i file `.py` toccati validati con `py_compile` dopo aver rilevato (e corretto) due file creati con un carattere spurio in testa dovuto a un artefatto del tool di editing.
- **Correlation Engine per Schedina Oracle (SLIP-02, NEW)**: nuovo modulo `src/oracle/betslip/correlation_engine.py` (puro, nessun DB/IO, stesso pattern di `pick_pool.py`). Regole "same-match" (acceptance criteria): fixture diverse sono SEMPRE indipendenti (`different_fixture`), nessuna correlazione incrociata tra partite diverse. `classify_outcome(market, outcome)` normalizza (market, outcome) grezzi in una `SemanticOutcome` canonica per 4 famiglie — `MATCH_RESULT` (`h2h`/`1x2`: Home/Draw/Away), `DOUBLE_CHANCE` (`dc`/`double_chance`: Home/Draw, Home/Away, Draw/Away, sottoinsiemi di 2 esiti 1X2 su 3), `BTTS` (`goal_no_goal`/`btts`: Yes/No) e `TOTALS` (mercati `under_over_*`: direzione Over/Under + soglia gol) — ritornando `None` (fail-open, mai un falso positivo) per outcome non riconosciuti (es. nome squadra sul mercato h2h, limite noto e documentato nel modulo). `classify_pick_pair` classifica ogni coppia con una `severity` esplicita — `EXCLUDE` (logicamente incompatibili nella stessa partita, probabilita' congiunta zero per costruzione: stesso mercato con esiti diversi, "Over t1"+"Under t2" con t2<=t1, BTTS Yes con Under troppo basso, esito 1X2 che esclude la Double Chance scelta) o `PENALTY` (possibili insieme ma NON indipendenti: un esito implica l'altro o si sovrappone fortemente — nested totals stessa direzione soglie diverse, banda gol stretta tra soglie opposte entro `CorrelationRuleSet.totals_narrow_band_max_gap`, BTTS Yes che implica Over 1.5, esito 1X2 incluso nella Double Chance scelta) — o `INDEPENDENT` quando nessuna regola nota si applica (mercati/famiglie non correlati, es. 1X2 vs Totals) senza inventare correlazioni non dimostrabili (es. BTTS No non vincola le soglie gol totali in modo monotono, resta sempre indipendente). Matrice versionata `CorrelationRuleSet`/`DEFAULT_CORRELATION_RULESET` (`correlation_ruleset_v1`, mai soglie hardcoded inline, stesso principio di `PickPoolPolicy`/`DecisionPolicy`) + catalogo descrittivo `CORRELATION_RULE_CATALOG` per tracciabilita'. `evaluate_same_match_correlations` raggruppa per fixture e valuta tutte le coppie (solo quelle non indipendenti finiscono nel report, `CorrelationReport.is_valid=False` se esiste almeno un `EXCLUDE`). `evaluate_combination` (acceptance criteria "Combinazioni fortemente correlate non passano come indipendenti"): confronta esplicitamente `naive_probability` (prodotto semplice dei `p_model`, l'assunzione "ingenua" di indipendenza) con `adjusted_probability`, calcolata raggruppando le pick in componenti connesse tramite le coppie `PENALTY` e usando il MINIMO dei `p_model` per componente (`P(A∩B)<=min(P(A),P(B))` e' una disuguaglianza valida per QUALSIASI coppia di eventi, non un numero inventato, ed e' esatta nei casi di implicazione logica stretta che sono la maggioranza delle regole di questo motore) — forzata a `0.0` (`is_valid=False`) quando esiste anche una sola coppia `EXCLUDE`, indipendentemente da quanto ottimistico sarebbe il calcolo ingenuo. `candidates_from_pool_picks` converte comodamente l'output di SLIP-01 (`PickPoolResult.picks`) per questo motore. Nessuna migration DB, nessun nuovo endpoint (non richiesto dall'acceptance criteria; l'esposizione via API arrivera' con SLIP-03 Schedine, che consumera' direttamente questo motore). Nessun frontend toccato. 45 nuovi test in `tests/service/correlation_engine_test.py` (classificazione per famiglia, regole same-market, nested/contradiction/narrow-band Totals, BTTS-Totals incluso il caso BTTS No mai flaggato, 1X2-Double Chance implicazione/esclusione, famiglie non correlate, report per fixture, `evaluate_combination` con 2 e 3 pick inclusi i casi contraddizione/probabilita' mancante, versioning della ruleset); suite completa rieseguita: 498/498 passed. Import `src.api.main` verificato end-to-end (33 route invariate, nessuna aggiunta); tutti i file `.py` toccati validati con `py_compile`.
- **Generatore Schedine per Schedina Oracle (SLIP-03, NEW)**: nuovo modulo `src/oracle/betslip/betslip_builder.py` (puro, nessun DB/IO, stesso pattern di `pick_pool.py`/`correlation_engine.py`) + orchestratore DB-aware `src/oracle/betslip/betslip_service.py`. Tre profili di rischio VERSIONATI (acceptance criteria "Tre profili distinti"): `SlipProfile` (`SAFE`/`slip_profile_safe_v1`, `BALANCED`/`slip_profile_balanced_v1`, `AGGRESSIVE`/`slip_profile_aggressive_v1`, mai soglie hardcoded inline, stesso principio di `PickPoolPolicy`/`CorrelationRuleSet`) — ciascuno puo' generare schedine da 2 a 4 eventi (`min_legs`/`max_legs`), differenziati da soglie via via piu' permissive di probabilita' modello minima per singolo evento (0.55/0.40/0.25), quota massima per singolo evento (2.50/4.50/8.00) e numero massimo di coppie `PENALTY` ammesse nella stessa schedina (0/1/3) — una coppia `EXCLUDE` (contraddizione logica same-match, SLIP-02) resta invece un vincolo ASSOLUTO non parametrizzabile, applicato incondizionatamente per QUALSIASI profilo (mai una schedina logicamente impossibile, a prescindere da quanto aggressivo sia il profilo). `generate_betslips(candidates, profiles, ruleset, max_pool_size, max_slips_per_profile)`: per ciascun profilo, filtra il pool eligibile (quota+probabilita' modello presenti) secondo le soglie leg-level del profilo, tronca deterministicamente a `max_pool_size` (default 14, ordinato per EV decrescente, ogni troncamento tracciato in `warnings` — mai silenzioso, limite pratico DOCUMENTATO contro l'esplosione combinatoria di `itertools.combinations` per schedine a 4 eventi), genera tutte le combinazioni da `min_legs` a `max_legs`, scarta quelle con almeno una coppia `EXCLUDE` (riusa DIRETTAMENTE `evaluate_combination`/SLIP-02, nessuna logica di correlazione duplicata) e quelle che superano `max_penalty_pairs`. Metodo esplicito per quota/probabilita' combinate (acceptance criteria "Quote combinate e probabilita' dichiarate con metodo esplicito"): `combined_odd` = prodotto semplice delle quote singole (stesso metodo di qualunque bookmaker per una multipla); `naive_probability` (prodotto semplice dei `p_model`) e `adjusted_probability` (corretta per la correlazione, SLIP-02) dichiarate ENTRAMBE e mai confuse; `combined_ev` calcolato SEMPRE con `adjusted_probability` (mai con quella naive, che sovrastimerebbe l'EV reale in presenza di correlazioni positive) — `adjusted_probability * combined_odd - 1`, stesso principio di `ev = p_model*odd-1` del Value Engine (BET-02) applicato all'intera combinazione; `risk_score = 1 - adjusted_probability`. Ranking (acceptance criteria "Cosa deve fare: Ranking probability/EV/risk"): schedine ordinate per `combined_ev` decrescente, poi `adjusted_probability` decrescente, poi `risk_score` crescente, poi `slip_id` (hash deterministico dalle leg incluse, stesso principio di `pick_pool._pool_id`) per rompere i pareggi — mai un ordine dipendente dall'iterazione in input, poi troncate a `max_slips_per_profile` (default 5). Output spiegabile (acceptance criteria): ogni `GeneratedSlip` include `explanation` testuale (numero eventi, profilo/rischio, quota combinata, metodo di calcolo probabilita' con eventuale correzione per correlazione, EV) oltre a `findings` (le coppie `PENALTY` eventuali, mai una correlazione applicata silenziosamente) e `risk_label` (LOW/MEDIUM/HIGH, preso dal profilo). `betslip_service.py`: `BetslipService.generate_for_day` riusa `PickPoolService.build_pool_for_day` (SLIP-01) COSI' COM'E' (nessuna nuova query odds/predizioni) + `candidates_from_pool_picks` (SLIP-02) per il mapping, ritornando sia il `PickPoolResult` (tracciabilita' pool_id/policy_version) sia il `BetslipGenerationResult`. Nuovo endpoint `GET /betslip/generate` (stessi parametri opzionali di `GET /betslip/pool` — `include_borderline`/`min_odd`/`max_odd`/`min_ev`/`markets`/`target_date`, schema `BetslipGenerateResponse` in `src/api/schemas.py`). Nessuna migration DB (nessuna nuova tabella: le schedine sono calcolate on-demand, non persistite). Frontend: nuova pagina `frontend/src/features/betslip/BetslipPage.jsx` (voce di menu "Schedina Oracle", `AppRouter`/`Sidebar`/`menu.js`) con selezione data indipendente, tab per i tre profili e card per ciascuna schedina (legs arricchite lato client con nome squadre da `dayData` gia' disponibile in `App.jsx` — nessuna nuova fetch, nessun calcolo scientifico lato client, SOLO lookup/formattazione: stesso principio di `ValueBadge`/MATCH-01) che mostra quota combinata, probabilita' naive/corretta, EV, coppie penalizzate e la spiegazione testuale; nuove classi CSS `.risk-low`/`.risk-medium`/`.risk-high`/`.slip-card`/`.slip-legs` (stessa palette di `.value-play`/`.value-borderline`/`.value-no-bet`). 24 nuovi test (`tests/service/betslip_builder_test.py`: validazione profili, filtri leg-level/pool, limiti di correlazione per profilo con verifica esplicita che una coppia EXCLUDE non sia MAI generata in nessun profilo mentre una coppia PENALTY sia esclusa da SAFE ma ammessa da BALANCED/AGGRESSIVE, ranking/determinismo, explanation, versioning ruleset personalizzato; `tests/service/betslip_service_test.py`: orchestrazione con `PickPoolService` mockato); suite completa rieseguita: 522/522 passed. Import `src.api.main` verificato end-to-end (34 route registrate, +1 rispetto a SLIP-01/02); build frontend (`npm run build`) verificata senza errori; tutti i file `.py` toccati validati con `py_compile`.



## Migrazioni applicate (locale + docker)
- locale: `alembic stamp 55bbb5f0a367` + `alembic upgrade head` (ora include anche `f3a9c1d8e2b7`, vedi sotto)
- docker: nessuna piu' necessaria, l'API Docker riusa lo schema del DB locale (nessun Postgres containerizzato)

## Connessione DB locale
- runtime locale fissato su DB reale: `DATABASE_URL=postgresql://postgres:postgres@localhost:5432/match_db`
- verifica rapida: tabella `match` letta con volume storico (>47k righe)

**RISOLTO (2026-09-03):** rimosso il servizio Postgres containerizzato (`soccer_db`, immagine `postgres:16-alpine`) da `docker-compose.yml`. Causa del problema precedentemente annotato in questa sezione: sulla macchina di sviluppo le porte `5432`/`5433` dell'host sono contese tra istanze Postgres NATIVE Windows (`postgresql-x64-17` su 5432, `postgresql-x64-18` su 5433, entrambe servizi Windows sempre attivi) e container Docker di ALTRI progetti (`fantappero-postgres` prova a pubblicare la 5432) — il DB Docker di QUESTO progetto (porta pubblicata 5433) non era percio' mai raggiungibile dall'host (la 5433 risultava "vinta" dal Postgres nativo 18), mentre i container `soccer_api`/`soccer_scheduler` (rete Docker interna, hostname `db`) si connettevano correttamente al Postgres Docker interno — ma quel volume (`ia_predict_soccer_postgres_data`) conteneva un dataset separato e quasi vuoto (poche righe in `match`, creato 2026-09-01), disallineato dal DB storico reale (`localhost:5432`, Postgres 17 nativo, >47k `match`). Risultato pratico: l'API/scheduler in Docker (quindi anche il frontend che ci parla) lavoravano su un DB vuoto, mentre solo l'esecuzione locale (test/script/training) vedeva i dati veri — causa root del gap "nessuna previsione visibile sul FE" nonostante il lavoro ML/backend completato. Fix: `docker-compose.yml` non definisce piu' il servizio `db`; `api`/`scheduler` puntano ora a `postgresql://postgres:postgres@host.docker.internal:5432/match_db` (hostname speciale di Docker Desktop che risolve all'host Windows, reso portabile anche su Linux con `extra_hosts: host.docker.internal:host-gateway` gia' incluso nel compose); applicata su `localhost:5432` la migration mancante `f3a9c1d8e2b7` (BET-06 `prediction_ledger`, additiva, nessun impatto su tabelle esistenti) cosi' da allinearlo alla head del codice; container Docker `soccer_db` fermato e rimosso, volume `ia_predict_soccer_postgres_data` lasciato su disco ma non piu' referenziato (nessuna cancellazione distruttiva, pulizia manuale opzionale con `docker volume rm` se non serve piu'). Verificato con `GET /health/database` dal container `soccer_api` rebuilded: `host=host.docker.internal`, `database=match_db`, `counts={match: 47228, statistics: 81244, odds: 20402}` — stessa sorgente dati per locale, Docker e training.

Note: gli stati sopra sono riferiti all'implementazione tecnica nel branch corrente; la validazione finale dipende dall'esecuzione acceptance/test su ambiente dati reale.

**RISOLTO (2026-09-03, seguito):** dopo il fix del DB, il frontend mostrava ancora "Failed to fetch" e contatori a 0. Individuate e risolte TRE cause concatenate:
1. **Container `soccer_api` morto senza restart policy** (`exit code 137`, arrestato durante il ciclo down/up precedente e mai piu' rialzato — nessuno dei 3 servizi aveva `restart:` nel compose). Fix: aggiunto `restart: unless-stopped` a `api`/`web`/`scheduler` + `healthcheck` su `api`.
2. **Bug performance critico in `DashboardService._fetch_matches`** (`src/api/dashboard_service.py`): la query non aveva NESSUN filtro SQL sulla data — caricava l'intero storico (`match` + `selectinload(statistics, odds)`, decine di migliaia di righe) ad OGNI richiesta `/dashboard/day`/`/dashboard/live`, misurato **22.6s** in locale (timeout >40s dal container via `host.docker.internal`). Fix: filtro SQL su range `date_match >= start AND < end` (colonna stringa ISO 8601, confronto lessicografico valido, margine di 1 giorno per fusi orari) — tempo sceso a **0.24s** (~95x). Aggiunta migration additiva `9c4e7a21f6b3` (indice btree su `match.date_match`, `index=True` anche nel modello ORM) per scalabilita' futura. Nuovi test con SQLite in-memory reale in `tests/service/dashboard_service_test.py` (`TestFetchMatchesDateFilter`), 2 mock esistenti aggiornati alla nuova firma.
3. **Model Registry vuoto** (`best_models/registry/` senza alcun `index.jsonl`): il retrain precedente (`jobs_history.jsonl`, 2026-09-02) risultava "success" ma con **0 righe per ogni mercato** — stesso bug SQLAlchemy `is_not(None)` gia' corretto nel codice in una fase precedente di questa sessione, ma MAI seguito da un retrain effettivo con il fix applicato. Causa ultima del "nessuna previsione sul FE": anche con DB e query corretti, senza modelli registrati `ModelRegistry.list_markets()` ritorna `[]` e la dashboard non calcola nessuna prediction. Lanciato un primo retrain con l'intero storico (2014-2026, 47k+ righe, tutti i mercati) che ha esposto un QUARTO problema (vedi nota seguente).

**RISOLTO (2026-09-03, parallelismo annidato):** il primo tentativo di retrain (13 stagioni storiche, tutti i mercati) ha fatto crashare il container **due volte** (`exit code 137`, `OOMKilled: false` — quindi non il cgroup del container specifico, ma un livello piu' esterno: verosimilmente Docker Desktop/WSL2 che protegge l'host quando la VM si avvicina ai suoi limiti), consumando picchi di **1853% CPU e 79% della memoria disponibile alla VM Docker (6+GB su 7.6GB totali)**. Causa: **parallelismo annidato a PROCESSI separati** in `train_multi_market.py` — `GridSearchCV(n_jobs=-1)` (e allo stesso modo `VotingClassifier`/`StackingClassifier`/`cross_val_score`, tutti `n_jobs=-1`) lanciava N processi paralleli, ciascuno dei quali eseguiva un `RandomForestClassifier(n_estimators=500, n_jobs=-1)` che tentava A SUA VOLTA di usare tutti i core — il parallelismo si moltiplica (N_core × N_core) invece di sommarsi, ogni processo worker duplica in memoria dati/modello. Lo stesso identico problema era gia' stato diagnosticato e risolto in una sessione precedente in `src/ml/markets/market_1x2.py` (commento esplicativo gia' presente nel codice), ma il fix non era mai stato portato in `train_multi_market.py` — il modulo REALMENTE usato da `train_all_markets`/`/jobs/retrain` per tutti i mercati (incluso "h2h"). Fix: wrappate le 3 chiamate `.fit()`/`cross_val_score()` (GridSearchCV, Voting, Stacking) in `with parallel_backend("threading", n_jobs=-1):` (backend a thread condivisi, non processi — stesso pattern di `market_1x2.py`). Aggiunto anche `mem_limit: 4g` esplicito sul servizio `api` in `docker-compose.yml` come rete di sicurezza (se la memoria satura, va in OOM SOLO il container, in modo controllato e riconosciuto da Docker, permettendo a `restart: unless-stopped` di farlo ripartire — non l'intera VM). Risultato verificato dopo il fix: **CPU 1853%→788-1180%, memoria 79% di 7.6GB→73-80% di un limite di 4GB**, container stabile (`healthy`) e **dashboard `/dashboard/day` risponde 200 OK anche con training in corso** (impossibile prima del fix). Test dedicato (`tests/service/train_multi_market_pipeline_test.py`, 2/2) e suite completa (423/423) invariati.










































