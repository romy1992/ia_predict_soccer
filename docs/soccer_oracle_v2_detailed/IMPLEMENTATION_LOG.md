## 2026-09-10 — Correzione quota void IA e Match Center per mercato

- Introdotta `decision_policy_v2_model_break_even`, senza modificare la v1
  storica: `model_void_odd = 1 / p_model`, soglia PLAY configurabile tramite
  `min_edge_percent` (default 2%), classificazioni `PLAY`, `BORDERLINE`,
  `NO BET`, `SENZA QUOTA` e `N/D` con motivazione esplicita.
- Distinte le grandezze `model_void_odd` e `market_fair_odd`; aggiunti edge
  quota assoluto/percentuale, EV, ROI atteso e soglia PLAY. L'edge
  probabilistico preesistente resta invariato.
- Esteso il Prediction Ledger in modo additivo per congelare le nuove
  metriche e la motivazione al momento della cattura. Nessun record
  preesistente viene aggiornato retroattivamente.
- Dashboard e cattura ufficiale continuano a usare lo stesso entry point
  della Decision Policy. Il dettaglio supporta inoltre output 1X2
  multiclass Home/Draw/Away e Double Chance derivata, prezzando ogni outcome
  soltanto con la propria quota.
- Match Center esteso con selettore mercato, filtro Situazione, contatori,
  colonne separate per quota mercato/quota void/edge/ROI atteso/stato
  ufficiale e card mobile. Le NO BET restano visibili.
- Per fixture con Ledger ufficiale, Match Center usa i valori congelati e
  mostra PENDING/WON/LOST/VOID e PnL; in assenza di record mostra
  “Non ufficiale”.
- Rifinitura vista “Tutti i mercati”: mantenuti i chip storici di ogni
  previsione (con colore di correttezza/confidenza), aggiunto su ciascun chip
  lo stato economico prodotto dal backend e affiancato il “Pronostico
  vincitore” con probabilità, quota mercato, quota void IA, edge, ROI atteso
  ed esito ufficiale. Se viene selezionato un singolo mercato resta la vista
  analitica dedicata a quel mercato.

# Soccer Oracle V2 - Implementation Log

## Baseline (SOCCER-00)
- base branch verificato: `feature/ml-dashboard-platform`
- branch operativo attivo: `feature/soccer-oracle-v2`
- baseline SHA iniziale: `d12a661`
- data verifica: 2026-09-02

## Premessa comune prima di ogni task
Prima di ogni task viene applicata la premessa in `AI_MASTER_PROMPT.md`:
1. ispezione file coinvolti
2. no riscritture inutili
3. compatibilita DB storico
4. migration Alembic per cambi DB
5. no leakage temporale
6. no split random in pipeline production
7. latest model != production model
8. aggiornare test pertinenti

## Sequenza immediata (CURRENT_TASK)
1. SOCCER-00
2. SOCCER-01
3. SOCCER-02
4. DATA-01
5. DATA-02
6. DATA-03
7. DATA-04
8. DATA-06
9. ML-01
10. ML-02

## Stato implementazione
- [x] SOCCER-00
- [x] SOCCER-01
- [x] SOCCER-02
- [x] DATA-01
- [x] DATA-02
- [x] DATA-03
- [x] DATA-04
- [x] DATA-06
- [x] ML-01
- [x] ML-02
- [x] DATA-05
- [x] DATA-07
- [x] ML-03
- [x] DATA-08
- [x] FE-01
- [x] FE-02
- [x] FE-03
- [x] ML-04
- [x] ML-05
- [x] ML-06
- [x] ML-07
- [x] EXP-01
- [x] EXP-02
- [x] EXP-03
- [x] EXP-04
- [x] EXP-05
- [x] MARKET-01
- [x] MARKET-02
- [x] MARKET-03
- [x] MARKET-04
- [x] MARKET-05
- [x] MARKET-06
- [x] ORACLE-01
- [x] ORACLE-02
- [x] ORACLE-03
- [x] ORACLE-04
- [x] BET-01
- [x] BET-02
- [x] BET-03
- [x] BET-04
- [x] BET-05
- [x] BET-06
- [x] MATCH-01
- [x] MATCH-02
- [x] SLIP-01
- [x] SLIP-02
- [x] SLIP-03
- [x] OPS-01
- [x] OPS-02
- [x] OPS-03
- [x] LIVE-01
- [x] LIVE-02
- [x] LIVE-03

**ROADMAP OPERATIVA COMPLETA: 53/53 task, 12/12 fasi (vedi `MANIFEST.json`).**

## Estensioni introdotte
- settlement job idempotente con completezza finale (`/jobs/settlement`)
- overview settlement (`/settlement/overview`)
- report Data Quality machine-readable (`/data/quality`)
- pipeline training con selector dentro CV fold (anti-leakage)
- migrazione Alembic per campi settlement su `match`
- job history evoluta con lifecycle queued/running/success/failed e filtri tipo/stato
- refactor frontend a feature components + router locale (`frontend/src/features/`)
- Data Center FE con import storico/oggi/future e settlement manuale
- Data Quality FE con coverage/anomalie/distribuzioni e filtri stagione/lega
- migration compatibilità schema `match` (`current_league`, `league_match`, `status`, `mean_statistics`)
- baseline bookmaker implied/fair probabilities nei report match detail
- framework metriche probabilistiche (LogLoss, Brier, ECE, AUC) e champion selection multi-metrica
- calibration framework (Platt/sigmoid + isotonic) con confronto pre/post e calibratore versionato per model run (`src/ml/calibration/calibration_service.py`)
- Model Registry con lifecycle completo candidate/champion/production/retired, promotion history, lookup produzione per mercato (`src/service_ia/training/model_registry.py`)
- Team Strength Expert: rating offensivo/difensivo, home advantage, rolling form point-in-time, backtest base (`src/ml/experts/team_strength/team_strength_expert.py`)
- Goal Distribution Expert: Poisson lambda regressor con validazione temporale (no train_test_split), soglie U/O 1.5/2.5/3.5/4.5, score distribution completa e confronto Poisson vs Binomiale Negativa (`src/ml/experts/goal_distribution/goal_distribution_expert.py`)
- Statistics Expert: modello pre-match basato esclusivamente su `mean_statistics` (nessuna feature odds), validazione temporale con metriche probabilistiche ML-05, embedding numerico riusabile (`src/ml/experts/statistics/statistics_expert.py`)
- Market/Odds Expert: fair probabilities (riuso bookmaker_baseline), dispersione bookmaker, movement quote e opening/latest/closing point-in-time-safe (closing mai valorizzato prima del kickoff) (`src/ml/experts/market/market_odds_expert.py`)
- Direct Market Expert: interfaccia comune `predict_proba` sui champion model esistenti (h2h/dc/goal_no_goal/corners/cards/under_over_*), con metadati espliciti che impediscono di scambiare h2h binario per un 1X2 multiclasse (`src/ml/experts/direct/direct_market_expert.py`)
- **Fase ORACLE EXPERTS completata (EXP-01..05)**
- Vero mercato 1X2 multiclass (HOME/DRAW/AWAY, nessun mapping draw->away): dataset dedicato, GridSearchCV logistic/random_forest con validazione temporale, metriche multiclasse dedicate (log_loss/brier generalizzato/ECE su confidence/AUC OvR) e calibrazione multiclasse via `CalibratedClassifierCV` (`src/ml/markets/market_1x2.py`, `src/ml/evaluation/multiclass_probability_metrics.py`, `src/ml/calibration/multiclass_calibration_service.py`)
- Double Chance derivata da 1X2 coerente (nessun training proprio): P(1X)=P(HOME)+P(DRAW), P(12)=P(HOME)+P(AWAY), P(X2)=P(DRAW)+P(AWAY), fair odds e wrapper batch su `Market1x2Expert` (`src/ml/markets/market_double_chance.py`)
- BTTS consolidato (score distribution EXP-02 vs direct expert 'goal_no_goal' EXP-05): benchmark comparativo (score_distribution/direct_expert/ensemble) con selezione via `champion_probability_score`, calibrazione OOF temporale (Platt/isotonic) del solo approccio vincente, P(Yes)+P(No)=1 garantito per costruzione (No=1-Yes); dataset builder end-to-end che riusa rating point-in-time (`TeamStrengthExpert`, EXP-01) + score distribution Poisson (`GoalDistributionExpert`, EXP-02) + feature/target 'goal_no_goal' (`FilterMarketService`, EXP-05); orchestratore `run_btts_benchmark(_from_db)` che registra il calibratore vincente come 'candidate' (mai 'production' automatica) (`src/ml/markets/btts/btts_market.py`)
- U/O 1.5-4.5 multi-linea consolidato: confronto sullo STESSO walk-forward tra binary_independent (4 classificatori scorrelati), hierarchical (1 solo classificatore multiclasse sui bin di gol totali 0/1/2/3/4, confini esattamente sulle soglie, P(Over t) = cumulata dall'alto) e goal_distribution (Poisson EXP-02 da rating EXP-01); selezione per metriche probabilistiche+betting (`champion_probability_score`) aggregate sulle 4 soglie; monotonicità P(O1.5)>=P(O2.5)>=P(O3.5)>=P(O4.5) resa OBBLIGATORIA sull'output finale via proiezione isotonica (`enforce_monotonic_over_probabilities`), indipendentemente dall'approccio vincente; orchestratore `run_totals_benchmark(_from_db)` che registra il modello vincente come 'candidate' solo se introduce un nuovo estimator (hierarchical/binary_independent), nessun salvataggio per goal_distribution (deterministico) (`src/ml/markets/totals/totals_market.py`)
- Corners O/U specializzato con linea configurabile (8.5/9.5/10.5/11.5 come parametro, non più soglia fissa a 9.5): feature dedicate (media storica corner fatti/concessi per squadra, totale atteso, differenziale, da `mean_statistics['Corner Kicks']`) oltre alle generiche odds/mean_stats; calibrazione (Platt/isotonic) via `CalibrationService` (ML-06, riusata) per il modello di ciascuna linea; report con metriche per linea; `CornersExpert` parametrico sulla linea, ogni linea registrata come mercato indipendente nel registry (es. `corners_line_9_5`) (`src/ml/markets/corners/corners_market.py`)
- Cards O/U specializzato con linea configurabile (3.5/4.5/5.5/6.5 come parametro): feature "team/style" già disponibili genericamente da `mean_statistics` (Yellow/Red Cards, Fouls) più feature ARBITRO nuove e point-in-time (`build_referee_features_dataset`: media storica cartellini elargiti nelle partite precedenti arbitrate dallo stesso arbitro, nessun leakage, fallback a prior neutro senza storico); calibrazione via `CalibrationService` (ML-06) per ciascuna linea; `CardsExpert` parametrico sulla linea, stesso pattern registry di `CornersExpert` (`src/ml/markets/cards/cards_market.py`)
- **Fase MARKETS completata (MARKET-01..06)**
- Schema di output standardizzato per gli Oracle Experts (dataclass `ExpertOutput`: expert_name/expert_version, probability_vector con validazione [0,1], model_run_id, stage, feature_timestamp, confidence con default automatico, metadata, raw_output) e `combine_expert_outputs` per assemblare la riga di feature multi-esperto senza collisioni di naming (`src/ml/ensemble/expert_output.py`); adapter dedicati per ciascun esperto esistente SENZA modificarli (EXP-01 team_strength: probability_vector vuoto per costruzione, solo rating/feature; EXP-02 goal_distribution: over/under da Poisson o da score matrix; EXP-03 statistics: vettore binario complementare dalla probabilità calibrata; EXP-04 market_odds: fair probabilities da `compute_market_baseline`; adapter generico `from_predict_proba_expert` per EXP-05 `DirectMarketExpert` + MARKET-05/06 `CornersExpert`/`CardsExpert`, che condividono la stessa interfaccia `predict_proba`/`predict_proba_dict`) (`src/ml/ensemble/adapters.py`)
- **ORACLE-01 completato**: tutti gli esperti sono consumabili da un futuro meta-model tramite lo schema comune (acceptance criteria verificato dal test end-to-end `test_outputs_from_different_experts_are_consumable_by_meta_model`)
- Meta Model / Stacker per mercato (ORACLE-02): confronto, sulle STESSE meta-feature (probabilita' dei singoli Oracle Expert per lo stesso mercato, tipicamente da `combine_expert_outputs`/ORACLE-01), tra `weighted_blend` (`WeightedBlendClassifier`, pesi non negativi che sommano a 1, ottimizzati via softmax+minimizzazione log loss) e `learned_stacker` (default `LogisticRegression`); ENTRAMBI validati con lo stesso meccanismo di OOF walk-forward gia' in uso nel progetto (`temporal_oof_probabilities`, ML-05, mai fit sul fold di validazione), selezione del vincitore via `champion_probability_score` e meta-model finale rifittato sull'intero storico; `build_meta_features_from_expert_outputs` collega esplicitamente lo schema ORACLE-01 alle meta-feature; orchestratore `run_stacking_benchmark` che registra il meta-model vincente come 'candidate' (mai 'production' automatica) (`src/ml/ensemble/stacking.py`)
- Calibrazione finale Oracle Ensemble (ORACLE-03): applica `CalibrationService` (ML-06, gia' riusata da corners/cards, non modificata) al meta-model vincitore di ORACLE-02 (`benchmark_stacking_approaches`), stessi fold walk-forward gia' usati per selezionare weighted_blend/learned_stacker (nessun nuovo meccanismo di split); report pre/post metrics (log_loss/brier/ece) esposto in `OracleEnsembleCalibrationReport`; fallback esplicito e non bloccante — nessuna eccezione propagata al chiamante — quando il campione e' sotto `DEFAULT_MIN_CALIBRATION_SAMPLES` (60 righe) o la calibrazione fallisce per qualunque motivo: in quel caso il meta-model raw (non calibrato) di ORACLE-02 viene usato cosi' com'e', con il motivo riportato in `fallback_reason`; orchestratore `run_oracle_ensemble_calibration` che registra il modello finale (calibrato o raw fallback) come 'candidate' (mai 'production' automatica) (`src/ml/ensemble/oracle_calibration.py`)
- Model Consensus per spiegabilita' (ORACLE-04): per una singola fixture/mercato, assembla l'output di Direct Expert (EXP-05, modello registrato per quel mercato) e Market/Odds Expert (EXP-04, fair probability dalle quote gia' salvate in `odds_snapshot`) — GENERICAMENTE applicabili a QUALUNQUE mercato di `FilterMarketService.SUPPORTED_MARKETS`, a differenza di team_strength/goal_distribution/statistics volutamente esclusi (rispettivamente: nessun probability_vector per costruzione, non generico oltre i mercati "gol", nessuna persistenza su registry); l'Oracle finale usa il meta-model registrato (ORACLE-02/03, cercato ESPLICITAMENTE per prefisso `model_name` — mai un `get_latest(market)` generico che confonderebbe meta-model e modello diretto sullo stesso mercato) quando disponibile, altrimenti la media semplice delle probabilita' comparabili (fallback esplicito, mai un'eccezione); dispersione = deviazione standard tra le probabilita' comparabili, con etichetta di accordo (high/medium/low); nessuna spiegazione testuale generata, solo numeri/metadati calcolati (`src/ml/ensemble/model_consensus.py`); esposto tramite `GET /dashboard/match/{fixture_id}/consensus?market=...` (`src/api/main.py`, `src/api/schemas.py::ModelConsensusResponse`)
- **Fase ENSEMBLE completata (ORACLE-01..04)**
- Fair Odds Engine (BET-01): standardizza, per ciascun outcome di un mercato, `p_market_raw`/`p_market_fair` (overround rimosso, RIUSA `compute_market_baseline` di ML-04 senza duplicarlo) + `fair_odd = 1/p_market_fair` + `p_model` (Oracle, quando fornito dal chiamante — tipicamente da `model_consensus.py`, ORACLE-04) affiancati per lo stesso outcome ("Confronto Oracle vs market"); scope DELIBERATAMENTE limitato alle sole probabilita'/quote (NESSUN calcolo di `prob_edge`/`EV`/decision: quello e' BET-02, dipendenza tecnica esplicita, "REWRITE" di `DashboardService._value_decision`); outcome con `p_model` noto ma senza quota di mercato (o viceversa) restano comunque nell'output invece di sparire silenziosamente; nuovo package `src/oracle/` (primo modulo della Fase BETTING, distinto da `src/ml/`) (`src/oracle/fair_odds/fair_odds_engine.py`)
- Value Engine (BET-02, REWRITE): sostituisce il precedente calcolo che chiamava impropriamente "edge" un Expected Value; distingue esplicitamente `prob_edge = p_model - p_market_fair` (differenza di probabilita', indipendente dalla quota — invariante se la quota cambia a parita' di probabilita') da `ev = p_model*odd-1` (valore atteso in termini di quota, cio' che il codice precedente calcolava sotto il nome "edge"); soglie di decisione PLAY/BORDERLINE/NO BET VERSIONATE (`ValueDecisionPolicy`, mai hardcoded inline nel corpo della funzione, stessi valori numerici gia' in uso per non introdurre un cambio di policy non richiesto da questo task); consuma direttamente `FairOddsOutcome` (BET-01) garantendo per costruzione che market/outcome/quota si riferiscano allo stesso outcome; corretto in `DashboardService._pick_and_odd_for_prediction` il bug per cui il pick "Away" sul mercato h2h poteva usare la quota "Draw" come fallback (outcome scorretto: ora resta `None` se manca la quota "Away", gestito esplicitamente dal Value Engine) (`src/oracle/value_engine/value_engine.py`, wiring in `src/api/dashboard_service.py::_build_decision_cards`)
- Betting Backtester (BET-03): per un singolo mercato di `FilterMarketService.SUPPORTED_MARKETS`, genera `p_model` SOLO out-of-sample riusando lo STESSO walk-forward gia' validato altrove (ML-05: `expanding_window_splits` + `temporal_oof_probabilities`, nessuna nuova logica di split/leakage), lo abbina alle quote storiche REALI per lo stesso outcome (fair odds via `compute_market_baseline`/BET-01: per corners/cards viene usata ESCLUSIVAMENTE la linea che coincide con la soglia target di `FilterMarketService`, 9.5/4.5 — una linea non quotata per quella fixture resta esplicitamente NO BET invece di un fallback silenzioso su una quota di una linea diversa che distorcerebbe ROI/EV) e valuta ciascuna bet con il Value Engine (BET-02, non modificato); calcola, con stake flat, ROI/yield/profit, hit rate, avg odds, max drawdown (peak-to-trough sulla curva di equity cumulata) e la scomposizione per edge bucket/mercato/lega, con ordinamento cronologico deterministico che rende il report indipendente dall'ordine di input passato dal chiamante ("Report riproducibile"); parte "pura" testabile con numeri fissi e zero dipendenze DB/ML (`BacktestBet`/`compute_backtest_report`, `src/oracle/backtest/betting_backtester.py`) separata dall'orchestratore che collega dataset reale/OOF/fair-odds/value-engine (`run_market_backtest(_from_db)`, `src/oracle/backtest/market_backtest.py`)
- Decision Policy versionata (BET-04, REWRITE): estende `ValueDecisionPolicy` (BET-02, un'unica istanza GLOBALE identica per ogni mercato/outcome) con soglie che possono variare ESPLICITAMENTE per mercato e per singolo outcome di un mercato (`DecisionPolicy.thresholds_for`, priorita' outcome > mercato > default), piu' due nuovi filtri richiesti dall'acceptance criteria: `min_samples` (numero minimo di bookmaker che quotano l'outcome, da `FairOddsOutcome.bookmakers` gia' calcolato da BET-01/ML-04 — sotto soglia, NO BET indipendentemente da edge/EV) e `min_odd`/`max_odd` opzionali (range di quota accettabile, NO BET se fuori range); riusa DIRETTAMENTE `compute_prob_edge`/`compute_expected_value` (BET-02, mai duplicati); `DEFAULT_DECISION_POLICY` (`decision_policy_v1`) riproduce ESATTAMENTE lo stesso comportamento di `DEFAULT_POLICY` (BET-02) quando i nuovi filtri non sono vincolanti (`min_samples=0`, `min/max odd=None`) — nessun override per-mercato precaricato in produzione, la capacita' e' disponibile ma non attivata finche' una taratura specifica non sia esplicitamente richiesta (nessun cambio di policy non richiesto da questo task); `dashboard_service.py::_build_decision_cards` aggiornato per usare `evaluate_decision_from_fair_odds_outcome` (passando `bookmakers` come `samples`) al posto della chiamata diretta al Value Engine — nessuna soglia numerica hardcoded nel dashboard service (acceptance criteria) (`src/oracle/decision_engine/decision_policy.py`)
- Closing Line Value — CLV (BET-05, NEW): calcola quanto la quota presa al momento della decisione (`odd_at_bet`) e' stata migliore/peggiore della vera quota di CHIUSURA (`closing_odd`), metrica di valutazione EX-POST ("quando disponibile": SOLO per le fixture con `OddsSnapshot`/DATA-06 registrati fino al kickoff — mai per il backtest storico su `match.odds`, singolo valore senza serie temporale, per cui il CLV resta esplicitamente non disponibile); riusa `build_opening_latest_closing` (EXP-04) chiamato con `as_of=kickoff_at` per ottenere per costruzione l'ultimo snapshot PRIMA del kickoff per bookmaker/outcome (mai una quota "live" post-kickoff spacciata per chiusura) e `compute_market_baseline`/`get_market_outcome_baseline` (ML-04/BET-01) per la fair probability aggregata al closing; espone due metriche distinte — `clv_odd_pct = odd_at_bet/closing_odd - 1` (quota) e `clv_prob = p_fair_at_bet - p_fair_closing` (probabilita' fair) — mai calcolate come feature di training (acceptance criteria "Nessun uso del closing price come feature pre-match illegittima": modulo puramente descrittivo/ex-post); nessuna migration DB necessaria (schema `odds_snapshot` gia' introdotto da DATA-06); report aggregato per mercato/modello (`compute_clv_report`) con conteggio esplicito disponibili/non disponibili, mai nascosto (`src/oracle/backtest/clv.py`)
- **Fix critico CrudRepository.search_filter** (scoperto durante un retrain manuale multi-mercato che falliva SEMPRE, per qualunque mercato, indipendentemente dai dati presenti — `retrain_run.log`): (1) `col.is_not(None)`/`is_(None)` su relazioni ORM one-to-many (`Match.odds`/`Match.statistics`) solleva `NotImplementedError` in SQLAlchemy — fix con `_is_relationship_attribute` che usa `.any()`/`~.any()` per le relazioni, invariato per le colonne scalari; (2) `Match.mean_statistics` (colonna JSON) salvava un Python `None` come letterale JSON `'null'` anziche' SQL `NULL` (default SQLAlchemy `JSON(none_as_null=False)`), rendendo il filtro `mean_statistics: not None` un NO-OP silenzioso su TUTTA la pipeline multi-mercato (`filter_market_service.py`, `point_in_time_builder.py`, `corners_market.py`, `totals_market.py`, `cards_market.py`, `market_backtest.py`) — fix con `JSON(none_as_null=True)` (`src/service_ia/model/match.py`); verificato via query diretta sul DB locale che non ci sono righe storiche da bonificare con una migration di data-fix (0 righe con `'null'` letterale su 3 totali). Test con vero SQLite in-memory (non mock, per esercitare davvero la query SQLAlchemy generata) in `tests/service/crud_repository_test.py`; suite completa rieseguita dopo il fix: 395 passed, 0 failed (`src/repository/base/crud_repository.py`, `src/service_ia/model/match.py`)
- Prediction Ledger / Paper Betting (BET-06): nuova tabella `prediction_ledger` (migration `f3a9c1d8e2b7`, validata upgrade/downgrade/upgrade sul DB reale) che separa ESPLICITAMENTE i campi "originali" scritti UNA SOLA VOLTA prima del kickoff (fixture_id/market/outcome/model_run_id/model_name/policy_version/p_model/p_market_fair/odd/fair_odd/prob_edge/ev/decision/stake/kickoff_at/created_at) dai campi di SETTLEMENT scritti SOLO dopo il risultato reale (is_settled/settled_at/settlement_status/actual_outcome/won/pnl) — "immutabilita' logica della prediction originale" garantita dalla service layer (`settle_prediction_record` usa `dataclasses.replace`, mai una mutazione in-place) e verificata da un test dedicato che confronta il record pre/post settlement; modulo puro (`src/oracle/ledger/prediction_ledger.py`, nessun DB, stesso stile BET-01/02/03) con `build_prediction_record` (riusa DIRETTAMENTE `Decision`/BET-04: market/outcome/p_model/p_market_fair/odd/prob_edge/ev/decision/policy_version provengono TUTTI dalla stessa valutazione, garanzia gia' vista in BET-01/02/04 che si riferiscano allo stesso outcome), `resolve_actual_outcome` (esito REALE riusando `FilterMarketService._label_by_market` + `_canonical_outcome_for_prediction`/BET-03, mai una nuova logica di labeling) e `compute_paper_pnl` (stessa formula di `bet_profit`/BET-03, duplicata deliberatamente per mantenere il ledger indipendente dal backtest storico, coerenza numerica garantita da test); orchestrazione DB in `src/oracle/ledger/ledger_service.py` (`PredictionLedgerService`): `log_prediction` idempotente per (fixture/market/outcome/model_run_id), `settle_pending`/`settle_prediction` settlano SOLO le prediction il cui match e' effettivamente concluso (`FINAL_STATUSES`, riusato da `settlement_service.py`) — le altre restano pending, mai forzate; nessun esito inventato quando le statistiche non sono disponibili (`settlement_status='void_no_result'`) o quando manca la quota (`'void_missing_odd'`); report PnL doppio: `raw_pnl_summary` (somma diretta dei `pnl` gia' salvati, sempre affidabile) e `paper_pnl_report` (RIUSA `compute_backtest_report`/BET-03 costruendo `BacktestBet` dalle righe settled, per ROI/hit-rate/max drawdown/edge bucket); endpoint `POST/GET /predictions/ledger`, `GET /predictions/ledger/fixture/{fixture_id}`, `POST /predictions/ledger/settle`, `GET /predictions/ledger/pnl` (`src/api/main.py`, `src/api/schemas.py`); repository dedicato stesso pattern di `OddsSnapshotRepository` (`src/repository/prediction_ledger_repository.py`); 26 nuovi test (pure + service con vero SQLite in-memory, stesso principio di `crud_repository_test.py`) in `tests/service/prediction_ledger_test.py`; `src/service_ia/training/prediction_logger.py` (JSONL) NON modificato/rimosso (compatibilita', endpoint `/predict/{market}` e `/predictions/log` invariati) — il Ledger e' un componente NUOVO e distinto, non ancora agganciato all'endpoint `/predict/{market}` (nessuna riscrittura non richiesta di un endpoint funzionante: il chiamante che ha gia' calcolato p_model/odd puo' loggare esplicitamente via `POST /predictions/ledger`)
- **Fase BETTING completata (BET-01..06)**

- Match Center con output Oracle ufficiali (MATCH-01, REFACTOR): fino a questo task `decision_cards` (probabilita'/quota/fair probability/edge/EV/badge PLAY-BORDERLINE-NO BET, BET-01/02/04) erano calcolati SOLO nel dettaglio match (`get_match_detail`, una fetch odds dedicata per singola fixture); la vista LISTA (`get_day_matches`/`get_live_matches`, la tabella "Match Center" dove si scorrono tutte le partite del giorno) mostrava solo probabilita' grezza del modello, senza alcun confronto col mercato. Estesi `_serialize_match`/`_serialize_api_fixture` con un nuovo metodo `_decisions_for_row` che riusa DIRETTAMENTE `_build_decision_cards` (mai una logica duplicata) calcolato SOLO quando le quote sono gia' disponibili SENZA fetch aggiuntive: da `match.odds` gia' caricato dalla query unica di `_fetch_matches`/`selectinload` (fix performance esistente) - MAI una nuova chiamata odds API-Sports per riga (quota giornaliera limitata, gia' vista esaurirsi nei job history). Nuovo helper `_db_matches_lookup(target_date)` (dict fixture_id->Match da un'UNICA query) riusato sia da `get_day_matches` sia da `get_live_matches` per arricchire le righe API con gli odds DB quando la fixture esiste gia' localmente, altrimenti `decision_cards=[]`/`best_decision=None` (mai un dato inventato - il dettaglio match resta comunque completo, fa la sua fetch odds dedicata). Nuovo `_select_best_decision_card` (priorita' PLAY>BORDERLINE>NO BET poi probabilita' piu' alta) per il badge sintetico di riga. Aggiunto anche `fair_odd` (BET-01, gia' calcolato ma non esposto) alle decision_cards esistenti ("fair market" richiesto dall'acceptance criteria). Frontend: nuovo componente `ValueBadge` (`frontend/src/features/matches/components/ValueBadge.jsx`) che mostra `row.best_decision` in una colonna dedicata di `MatchTable` — SOLO formattazione di dati gia' calcolati dal backend, NESSUN calcolo di edge/EV/decisione lato client (acceptance criteria "FE non ricalcola logica betting"); `MatchDetailPanel` arricchito con quota fair ed EV espliciti (prima solo "Edge"); voce di menu "Partite del giorno" rinominata "Match Center" (terminologia target, `docs/DOCUMENTAZIONE_PROGETTO_SOCCER_ORACLE.md` sezione 5.1). Nessuna migration DB (nessun cambio schema). 2 nuovi test in `tests/service/dashboard_service_test.py` (arricchimento riga quando la fixture e' nel DB con odds; nessun badge quando disponibile solo da API) + 1 assert aggiuntivo su `fair_odd` nel test dettaglio esistente; suite completa rieseguita: 425/425 passed. Build frontend (`npm run build`) verificata senza errori.
- **Oracle Match Detail (MATCH-02, NEW)**: nuovo `src/api/oracle_match_detail_service.py::OracleMatchDetailService` che aggrega, per UNA fixture, le 8 sezioni richieste dall'acceptance criteria ("Dettaglio navigabile per fixture") RIUSANDO direttamente i moduli gia' esistenti (nessuna logica duplicata): (1-3) Overview/Probabilities/Value Bets da `DashboardService.get_match_detail` (MATCH-01, invariato) - `value_bets` e' il sottoinsieme di `decision_cards` con `value_label` in {PLAY, BORDERLINE}, stessa etichetta gia' prodotta da BET-04; (4) Team Strength (EXP-01): rating PRE-match delle due squadre calcolato da `TeamStrengthExpert.current_ratings` su una query MIRATA (`MatchRepository.search_filter` con `OR` su `id_team_home`/`id_team_away`, mai l'intero storico DB) filtrata point-in-time in Python (`date_match < kickoff`, stesso pattern lessicografico gia' in uso in `DashboardService._fetch_matches` - nessun leakage se la fixture e' gia' conclusa); (5-6) Expected Goals + Score Matrix (EXP-02): lambda Poisson stimata DETERMINISTICAMENTE dai rating appena calcolati (`GoalDistributionExpert.estimate_lambdas_from_ratings` + `build_expert_output`, nessun training/modello necessario), chiavi della score matrix stringificate esplicitamente per un payload JSON non ambiguo; (7) Odds Movement: `OddsSnapshotRepository.opening_latest_closing` (sola visualizzazione, esplicitamente NON `MarketOddsExpert`/EXP-04 che e' invece la variante point-in-time-safe per la pipeline ML - vedi docstring del modulo); (8) Model Consensus (ORACLE-04): `build_model_consensus_for_fixture` richiamato per ciascun mercato in `model_markets`, un fallimento su un singolo mercato non blocca gli altri. Acceptance criteria "Dati mancanti gestiti": ogni sezione e' calcolata in modo indipendente e avvolta in try/except, con `warnings` esplicito (es. `team_strength_not_available`, `odds_movement_not_available`) MAI un valore inventato ne' un'eccezione che blocca l'intero dettaglio. Nuovo endpoint `GET /dashboard/match/{fixture_id}/oracle-detail` (`src/api/main.py`, schema `OracleMatchDetailResponse` in `src/api/schemas.py`). Frontend: nuova pagina `frontend/src/features/oracle/OracleMatchDetailPage.jsx` (navigabile tramite nuovo pulsante "Oracle" in `MatchTable` e link "Oracle Match Detail →" nel pannello rapido `MatchDetailPanel`, nuovo stato `activePage="oracle-detail"` con `AppRouter`/`App.jsx` che ricorda la pagina di provenienza per il pulsante "torna indietro") - mostra tutte le 8 sezioni con Score Matrix come tabella (troncata a 0-5 gol per leggibilita', il dato completo resta disponibile via API), SOLO formattazione di dati gia' calcolati dal backend (nessun calcolo lato client). Nessuna migration DB (nessuna nuova tabella). 5 nuovi test in `tests/service/oracle_match_detail_service_test.py` (happy path end-to-end con tutti i mock, fixture mancante con warnings completi, esclusione point-in-time delle partite post-cutoff, fallimento parziale del model consensus); suite completa rieseguita: 430/430 passed. Build frontend verificata senza errori; import `src.api.main` verificato end-to-end (32 route registrate).
- **Pick Pool per Schedina Oracle (SLIP-01, NEW)**: nuovo package `src/oracle/betslip/` (prima cartella della Fase SCHEDINA), stesso pattern a due livelli gia' consolidato nella Fase BETTING (modulo puro + orchestratore DB-aware). `pick_pool.py` (puro, nessun DB/IO): `PickPoolPolicy` versionata (soglie MAI hardcoded, stesso principio di `DecisionThresholds`/BET-04) con `include_borderline` (default `False`, solo PLAY), `min_odd`/`max_odd`/`min_ev`/`min_prob_edge`/`min_samples` opzionali; `PickPoolPolicy.with_overrides(...)` calcola una `version` derivata DETERMINISTICAMENTE (hash breve dei parametri, stesso principio di `TeamStrengthExpert.VERSION`) quando i parametri differiscono dal default - mai la stessa `policy_version` per soglie diverse. `build_pick_pool(candidates, policy)` applica la policy (motivo di esclusione esplicito per ogni scarto: `decision_not_allowed`/`odd_missing`/`odd_below_min`/`odd_above_max`/`ev_below_min`/`prob_edge_below_min`/`samples_below_min`), poi impone "una selezione per outcome/mercato" (raggruppamento per `(fixture_id, market)`, vincitore per priorita' PLAY>BORDERLINE poi EV poi prob_edge, il perdente finisce comunque in `excluded` con motivo `superseded_same_fixture_market`), infine ordina in modo deterministico (EV decrescente, poi fixture_id/market) e calcola `pool_id` (hash SHA1 dai pick inclusi, stabile indipendentemente dall'ordine di input - acceptance criteria "Pool deterministico e tracciabile", verificato esplicitamente da test dedicati). `pick_pool_service.py` (DB-aware): `candidate_from_decision_card` mappa UNA riga di `decision_cards` (gia' calcolata da `DashboardService`, MATCH-01/BET-01/02/04) a `CandidatePick` senza ricalcolare nulla; `PickPoolService.candidates_for_day`/`build_pool_for_day` riusano `DashboardService.get_day_matches` COSI' COM'E' (stessa query unica DB gia' ottimizzata, nessuna nuova fetch odds/predizioni). Nuovo endpoint `GET /betslip/pool` (parametri opzionali `include_borderline`/`min_odd`/`max_odd`/`min_ev`/`markets`/`target_date`, schema `BetslipPoolResponse` in `src/api/schemas.py`). Nessuna migration DB (nessuna nuova tabella: il pool e' calcolato on-demand, non persistito - la persistenza nel Prediction Ledger, se richiesta, resta un'azione esplicita separata via BET-06). Nessun frontend toccato (non richiesto dal task, che indica solo `src/oracle/betslip/`; la UI arrivera' con SLIP-03). 23 nuovi test (`tests/service/pick_pool_test.py`: policy/validazione/filtri/dedup/determinismo/pool_id; `tests/service/pick_pool_service_test.py`: adapter + orchestrazione con `DashboardService` mockato); suite completa rieseguita: 453/453 passed. Import `src.api.main` verificato end-to-end (33 route registrate); tutti i file `.py` toccati validati con `py_compile` dopo aver rilevato (e corretto) due file creati con un carattere spurio in testa dovuto a un artefatto del tool di editing.
- **Correlation Engine per Schedina Oracle (SLIP-02, NEW)**: nuovo modulo `src/oracle/betslip/correlation_engine.py` (puro, nessun DB/IO, stesso pattern di `pick_pool.py`). Regole "same-match" (acceptance criteria): fixture diverse sono SEMPRE indipendenti (`different_fixture`), nessuna correlazione incrociata tra partite diverse. `classify_outcome(market, outcome)` normalizza (market, outcome) grezzi in una `SemanticOutcome` canonica per 4 famiglie — `MATCH_RESULT` (`h2h`/`1x2`: Home/Draw/Away), `DOUBLE_CHANCE` (`dc`/`double_chance`: Home/Draw, Home/Away, Draw/Away, sottoinsiemi di 2 esiti 1X2 su 3), `BTTS` (`goal_no_goal`/`btts`: Yes/No) e `TOTALS` (mercati `under_over_*`: direzione Over/Under + soglia gol) — ritornando `None` (fail-open, mai un falso positivo) per outcome non riconosciuti (es. nome squadra sul mercato h2h, limite noto e documentato nel modulo). `classify_pick_pair` classifica ogni coppia con una `severity` esplicita — `EXCLUDE` (logicamente incompatibili nella stessa partita, probabilita' congiunta zero per costruzione: stesso mercato con esiti diversi, "Over t1"+"Under t2" con t2<=t1, BTTS Yes con Under troppo basso, esito 1X2 che esclude la Double Chance scelta) o `PENALTY` (possibili insieme ma NON indipendenti: un esito implica l'altro o si sovrappone fortemente — nested totals stessa direzione soglie diverse, banda gol stretta tra soglie opposte entro `CorrelationRuleSet.totals_narrow_band_max_gap`, BTTS Yes che implica Over 1.5, esito 1X2 incluso nella Double Chance scelta) — o `INDEPENDENT` quando nessuna regola nota si applica (mercati/famiglie non correlati, es. 1X2 vs Totals) senza inventare correlazioni non dimostrabili (es. BTTS No non vincola le soglie gol totali in modo monotono, resta sempre indipendente). Matrice versionata `CorrelationRuleSet`/`DEFAULT_CORRELATION_RULESET` (`correlation_ruleset_v1`, mai soglie hardcoded inline, stesso principio di `PickPoolPolicy`/`DecisionPolicy`) + catalogo descrittivo `CORRELATION_RULE_CATALOG` per tracciabilita'. `evaluate_same_match_correlations` raggruppa per fixture e valuta tutte le coppie (solo quelle non indipendenti finiscono nel report, `CorrelationReport.is_valid=False` se esiste almeno un `EXCLUDE`). `evaluate_combination` (acceptance criteria "Combinazioni fortemente correlate non passano come indipendenti"): confronta esplicitamente `naive_probability` (prodotto semplice dei `p_model`, l'assunzione "ingenua" di indipendenza) con `adjusted_probability`, calcolata raggruppando le pick in componenti connesse tramite le coppie `PENALTY` e usando il MINIMO dei `p_model` per componente (`P(A∩B)<=min(P(A),P(B))` e' una disuguaglianza valida per QUALSIASI coppia di eventi, non un numero inventato, ed e' esatta nei casi di implicazione logica stretta che sono la maggioranza delle regole di questo motore) — forzata a `0.0` (`is_valid=False`) quando esiste anche una sola coppia `EXCLUDE`, indipendentemente da quanto ottimistico sarebbe il calcolo ingenuo. `candidates_from_pool_picks` converte comodamente l'output di SLIP-01 (`PickPoolResult.picks`) per questo motore. Nessuna migration DB, nessun nuovo endpoint (non richiesto dall'acceptance criteria; l'esposizione via API arrivera' con SLIP-03 Schedine, che consumera' direttamente questo motore). Nessun frontend toccato. 45 nuovi test in `tests/service/correlation_engine_test.py` (classificazione per famiglia, regole same-market, nested/contradiction/narrow-band Totals, BTTS-Totals incluso il caso BTTS No mai flaggato, 1X2-Double Chance implicazione/esclusione, famiglie non correlate, report per fixture, `evaluate_combination` con 2 e 3 pick inclusi i casi contraddizione/probabilita' mancante, versioning della ruleset); suite completa rieseguita: 498/498 passed. Import `src.api.main` verificato end-to-end (33 route invariate, nessuna aggiunta); tutti i file `.py` toccati validati con `py_compile`.
- **Generatore Schedine per Schedina Oracle (SLIP-03, NEW)**: nuovo modulo `src/oracle/betslip/betslip_builder.py` (puro, nessun DB/IO, stesso pattern di `pick_pool.py`/`correlation_engine.py`) + orchestratore DB-aware `src/oracle/betslip/betslip_service.py`. Tre profili di rischio VERSIONATI (acceptance criteria "Tre profili distinti"): `SlipProfile` (`SAFE`/`slip_profile_safe_v1`, `BALANCED`/`slip_profile_balanced_v1`, `AGGRESSIVE`/`slip_profile_aggressive_v1`, mai soglie hardcoded inline, stesso principio di `PickPoolPolicy`/`CorrelationRuleSet`) — ciascuno puo' generare schedine da 2 a 4 eventi (`min_legs`/`max_legs`), differenziati da soglie via via piu' permissive di probabilita' modello minima per singolo evento (0.55/0.40/0.25), quota massima per singolo evento (2.50/4.50/8.00) e numero massimo di coppie `PENALTY` ammesse nella stessa schedina (0/1/3) — una coppia `EXCLUDE` (contraddizione logica same-match, SLIP-02) resta invece un vincolo ASSOLUTO non parametrizzabile, applicato incondizionatamente per QUALSIASI profilo (mai una schedina logicamente impossibile, a prescindere da quanto aggressivo sia il profilo). `generate_betslips(candidates, profiles, ruleset, max_pool_size, max_slips_per_profile)`: per ciascun profilo, filtra il pool eligibile (quota+probabilita' modello presenti) secondo le soglie leg-level del profilo, tronca deterministicamente a `max_pool_size` (default 14, ordinato per EV decrescente, ogni troncamento tracciato in `warnings` — mai silenzioso, limite pratico DOCUMENTATO contro l'esplosione combinatoria di `itertools.combinations` per schedine a 4 eventi), genera tutte le combinazioni da `min_legs` a `max_legs`, scarta quelle con almeno una coppia `EXCLUDE` (riusa DIRETTAMENTE `evaluate_combination`/SLIP-02, nessuna logica di correlazione duplicata) e quelle che superano `max_penalty_pairs`. Metodo esplicito per quota/probabilita' combinate (acceptance criteria "Quote combinate e probabilita' dichiarate con metodo esplicito"): `combined_odd` = prodotto semplice delle quote singole (stesso metodo di qualunque bookmaker per una multipla); `naive_probability` (prodotto semplice dei `p_model`) e `adjusted_probability` (corretta per la correlazione, SLIP-02) dichiarate ENTRAMBE e mai confuse; `combined_ev` calcolato SEMPRE con `adjusted_probability` (mai con quella naive, che sovrastimerebbe l'EV reale in presenza di correlazioni positive) — `adjusted_probability * combined_odd - 1`, stesso principio di `ev = p_model*odd-1` del Value Engine (BET-02) applicato all'intera combinazione; `risk_score = 1 - adjusted_probability`. Ranking (acceptance criteria "Cosa deve fare: Ranking probability/EV/risk"): schedine ordinate per `combined_ev` decrescente, poi `adjusted_probability` decrescente, poi `risk_score` crescente, poi `slip_id` (hash deterministico dalle leg incluse, stesso principio di `pick_pool._pool_id`) per rompere i pareggi — mai un ordine dipendente dall'iterazione in input, poi troncate a `max_slips_per_profile` (default 5). Output spiegabile (acceptance criteria): ogni `GeneratedSlip` include `explanation` testuale (numero eventi, profilo/rischio, quota combinata, metodo di calcolo probabilita' con eventuale correzione per correlazione, EV) oltre a `findings` (le coppie `PENALTY` eventuali, mai una correlazione applicata silenziosamente) e `risk_label` (LOW/MEDIUM/HIGH, preso dal profilo). `betslip_service.py`: `BetslipService.generate_for_day` riusa `PickPoolService.build_pool_for_day` (SLIP-01) COSI' COM'E' (nessuna nuova query odds/predizioni) + `candidates_from_pool_picks` (SLIP-02) per il mapping, ritornando sia il `PickPoolResult` (tracciabilita' pool_id/policy_version) sia il `BetslipGenerationResult`. Nuovo endpoint `GET /betslip/generate` (stessi parametri opzionali di `GET /betslip/pool` — `include_borderline`/`min_odd`/`max_odd`/`min_ev`/`markets`/`target_date`, schema `BetslipGenerateResponse` in `src/api/schemas.py`). Nessuna migration DB (nessuna nuova tabella: le schedine sono calcolate on-demand, non persistite). Frontend: nuova pagina `frontend/src/features/betslip/BetslipPage.jsx` (voce di menu "Schedina Oracle", `AppRouter`/`Sidebar`/`menu.js`) con selezione data indipendente, tab per i tre profili e card per ciascuna schedina (legs arricchite lato client con nome squadre da `dayData` gia' disponibile in `App.jsx` — nessuna nuova fetch, nessun calcolo scientifico lato client, SOLO lookup/formattazione: stesso principio di `ValueBadge`/MATCH-01) che mostra quota combinata, probabilita' naive/corretta, EV, coppie penalizzate e la spiegazione testuale; nuove classi CSS `.risk-low`/`.risk-medium`/`.risk-high`/`.slip-card`/`.slip-legs` (stessa palette di `.value-play`/`.value-borderline`/`.value-no-bet`). 24 nuovi test (`tests/service/betslip_builder_test.py`: validazione profili, filtri leg-level/pool, limiti di correlazione per profilo con verifica esplicita che una coppia EXCLUDE non sia MAI generata in nessun profilo mentre una coppia PENALTY sia esclusa da SAFE ma ammessa da BALANCED/AGGRESSIVE, ranking/determinismo, explanation, versioning ruleset personalizzato; `tests/service/betslip_service_test.py`: orchestrazione con `PickPoolService` mockato); suite completa rieseguita: 522/522 passed. Import `src.api.main` verificato end-to-end (34 route registrate, +1 rispetto a SLIP-01/02); build frontend (`npm run build`) verificata senza errori; tutti i file `.py` toccati validati con `py_compile`.
- **Separazione scheduler dati/ML (OPS-01, REFACTOR)**: refactor di `src/jobs/scheduler.py` — `build_scheduler(cfg)` registra 4 job APScheduler completamente separati e indipendenti (`data_sync_today`, `data_settlement`, `data_future_sync`, `ml_training`), ciascuno con `max_instances=1`/`coalesce=True`; il retrain (`ml_training`) NON e' piu' innescato dal completamento di alcun job di import (CronTrigger indipendente, default 23:00). `AppConfig` esteso con i campi versionati per lo scheduling. 21 nuovi test; suite completa rieseguita: 543/543 passed. Import `src.api.main` verificato end-to-end (34 route invariate).
- **Candidate -> Champion -> Production promotion (OPS-02, NEW)**: nuovo modulo PURO `src/ml/registry/promotion_policy.py` con gate metriche VERSIONATO (`PromotionGateThresholds`) + confronto candidate/production (`compare_candidate_to_production`, sempre `delta >= min_improvement_over_production`); estensione di `ModelRegistry` con `evaluate_promotion` (dry-run), `promote_with_policy` (promozione controllata, `force=True` sempre tracciato nell'audit) e `rollback`/`list_promotion_events` (audit trail completo, incluse promozioni bloccate). Endpoint nuovi: `GET /models/{market}/registry`, `GET /models/{market}/promotion/evaluate`, `POST /models/{market}/promote`, `POST /models/{market}/rollback`, `GET /models/{market}/promotion-history`. 27+17 nuovi test; suite completa rieseguita: 587/587 passed. Import `src.api.main` verificato end-to-end (39 route registrate, +5 rispetto a OPS-01).
- **Monitoring performance modello e data drift (OPS-03, NEW)**: nuovo package `src/ml/monitoring/` (policy pura + service DB-aware). Quattro segnali diagnostici, mai un'azione automatica: (1) prediction volume (bucket giornalieri dal Prediction Ledger, BET-06); (2) calibration drift (ECE/Brier/LogLoss recent-vs-baseline, riusa `compute_probability_metrics`/ML-05); (3) ROI rolling SOLO diagnostico (riusa `compute_backtest_report`/BET-03, soglia alert disattivata di default, mai un impatto su Decision Policy/BET-04 o gate promotion/OPS-02); (4) feature coverage strutturale delle `feature_names` del modello in produzione (campiona fixture recenti da `PredictionLogger`, ricostruisce il frame con `FilterMarketService.build_prediction_frame` — nota onesta sul `fillna(0)` gia' applicato a monte, la coverage misura presenza strutturale della colonna non "valore non-null"). `evaluate_alerts` (modulo puro) produce alert `info`/`warning`/`critical` da `MonitoringThresholds` VERSIONATA (`monitoring_policy_v1`), incluso il conteggio job falliti recenti (DATA-08). Estensione additiva retro-compatibile `PredictionLedgerRepository.list_all(..., since=None)`. Endpoint nuovi: `GET /monitoring/overview?market=...`, `GET /monitoring/alerts?market=...`. Frontend: nuova pagina `frontend/src/features/monitoring/MonitoringPage.jsx` (voce menu "Monitoring", badge severity riusa palette `.value-play`/`.value-borderline`/`.value-no-bet` tramite nuovo helper `severityClass` in `formatters.js`). Nessuna migration DB. 31 nuovi test (`monitoring_policy_test.py`, `monitoring_service_test.py`) + 1 test aggiuntivo in `prediction_ledger_test.py` per la retro-compatibilita' di `since`; suite completa rieseguita: 618/618 passed. Import `src.api.main` verificato end-to-end (41 route registrate, +2 rispetto a OPS-02). Build frontend (`npm run build`) verificata senza errori.
- **Pipeline dati live separata (LIVE-01, NEW, Fase LIVE ORACLE)**: nuovo package `src/data/live/` - dataset LIVE COMPLETAMENTE distinto dal dataset pre-match (`match`/`statistics`/`odds`, DATA-03), tre nuove tabelle additive (migration `a7d92c4f18e6`): `live_fixture_snapshot` (stato/punteggio/minuto per fixture, uno storico completo per poll, non un solo "ultimo stato" sovrascritto), `live_match_event` (eventi con timestamp, id DETERMINISTICO da `live_event_id` — fixture+tipo+dettaglio+minuto+team+player — cosi' che poll ripetuti sullo stesso evento, che l'API ritorna sempre per intero e non solo i nuovi, restino idempotenti via `session.merge` invece di duplicarsi) e `live_fixture_stat_snapshot` (statistiche correnti per squadra, payload grezzo non normalizzato: la normalizzazione/feature engineering resta scope di LIVE-02). `live_models.py` importa la STESSA `Base`/metadata di `src.service_ia.model.match` (nessuna relationship verso `Match`, nessun accoppiamento col dataset pre-match). `live_data_service.py`: funzioni pure di mapping (`extract_fixture_snapshot`/`extract_event`/`extract_stat_snapshot`, testabili senza rete/DB) + `LiveDataService` che riusa `ApiSportsProvider` COSI' COM'E' (`get_fixtures(live="all", ...)`/`get_fixture_events`/`get_fixture_statistics`, nessuna duplicazione di retry/rate-limit) aggiungendo (1) isolamento dell'errore PER LEGA (fetch fixtures) e PER FIXTURE (fetch eventi/statistiche) — un fallimento finisce in `errors` senza bloccare il resto del batch, acceptance criteria "Provider errors isolati" — e (2) una cache TTL DEDICATA (`live_cache_ttl_seconds`, nuovo campo `AppConfig`, SEPARATA da `DashboardService._api_cache`) per "Cache/polling controllato". `sync_live_data` orchestra fetch+mapping+persistenza (via nuovo `LiveDataRepository`, stesso pattern di `OddsSnapshotRepository`) e ritorna un report `{fixtures_live, events_saved, stat_snapshots_saved, errors}` (stesso stile "report" di `download_import_matches`/DATA-03). Job manuale `live_sync_job.py::run_manual_live_sync` (stesso pattern try/except + `JobHistory` di `run_manual_today_update`/`run_manual_settlement`), registrato in `build_scheduler` come QUINTO job schedulato `data_sync_live` (`IntervalTrigger` in SECONDI — non minuti, nuovo campo `AppConfig.live_sync_interval_seconds`, default 90 — completamente indipendente dagli altri 4 job di OPS-01). Endpoint nuovi: `POST /jobs/live-sync` (stesso pattern queued/sync degli altri job), `GET /live/fixtures` (ultimo snapshot per fixture non in stato finale), `GET /live/fixtures/{fixture_id}/events`, `GET /live/fixtures/{fixture_id}/statistics` — dato GREZZO della pipeline, distinti da `/dashboard/live` (MATCH-01, che resta invariato e continua a fare fetch diretto+cache propria per l'uso dashboard). Nessun frontend toccato (non richiesto dal task, che indica solo `src/data/live/`/`src/api/`). 22 nuovi test (`tests/service/live_data_service_test.py`: mapping puro, isolamento errori/cache con provider mockato, orchestrazione `sync_live_data` con errori parziali, repository con vero SQLite in-memory incluso l'upsert idempotente degli eventi, job con `JobHistory`/`LiveDataService` mockati) + 2 nuovi test in `scheduler_test.py` (quinto job con trigger a secondi, target function/indipendenza dal training); suite completa rieseguita: 643/643 passed (era 619/619 dopo OPS-03). Import `src.api.main` verificato end-to-end (45 route registrate, +4 rispetto a OPS-03).
- **Feature store live (LIVE-02, NEW, Fase LIVE ORACLE)**: nuovo package `src/ml/live/` (`live_feature_store.py`) - trasforma le osservazioni GREZZE del dataset live (LIVE-01: `live_fixture_snapshot`/`live_match_event`/`live_fixture_stat_snapshot`, via `LiveDataRepository`, non modificato) in feature NUMERICHE point-in-time per una fixture ad un istante `as_of` esplicito, senza alcun accoppiamento col dataset/pipeline pre-match (`src/ml/datasets/point_in_time_builder.py`/ML-01) - stesso principio di separazione di LIVE-01, "Non mischiare training pre-match e live". Cinque famiglie di feature (acceptance criteria): (1) `minute`/`minute_extra`/`minute_total`/`status` dallo snapshot fixture piu' recente con `captured_at <= as_of`; (2) `home_goals`/`away_goals`/`goal_diff`/`total_goals` (scoreline) dallo stesso snapshot; (3) `cards` (`yellow_cards_home/away/diff`, `red_cards_home/away/diff` — "second yellow" conta come rosso) contate dagli EVENTI (`event_type='Card'`) con `captured_at <= as_of`, piu' precise di un conteggio aggregato perche' ogni cartellino ha gia' il proprio timestamp; (4) `shots/xG se disponibili` (`shots_on_goal_*`, `total_shots_*`, `expected_goals_*`, + `corner_kicks`/`fouls`/`ball_possession_pct`/`yellow_cards_stat`/`red_cards_stat` come bonus dalla stessa fonte grezza `fixtures/statistics`, whitelist `_STAT_TYPE_FEATURE_MAP`) — una colonna e' presente SOLO se il provider ha effettivamente valorizzato quel tipo per quella squadra (mai uno zero finto quando `expected_goals` e' `null`, comune sui piani base API-Sports); (5) `pre-match prior` (`prior__{market}__{outcome}__p_model`/`__p_market_fair`/`__prob_edge`) dalle prediction gia' salvate PRIMA del kickoff nel Prediction Ledger (BET-06, `PredictionLedgerRepository.list_for_fixture`, non modificato) — l'unica fonte di "cosa pensava l'Oracle prima che la partita iniziasse". Ogni timestamp letto dal DB e' normalizzato ad UTC (`_ensure_utc`: SQLite, usato nei test, perde il tzinfo dei `DateTime(timezone=True)` alla lettura, Postgres in produzione lo preserva) prima di essere confrontato con `as_of`, per evitare `TypeError` naive/aware e garantire il filtro point-in-time in ogni ambiente. `LiveFeatureRow.feature_available_at_max` (massimo timestamp EFFETTIVAMENTE usato tra tutte le fonti) rende verificabile "Ogni feature live timestamped" via `LiveFeatureStore.assert_no_leakage` (stesso principio di `PointInTimeDatasetBuilder.assert_no_leakage`, ML-01). `LiveFeatureStore.build_feature_row(fixture_id, as_of=None)` ritorna `None` se non esiste ancora nessuno snapshot con `captured_at <= as_of` (riga non costruibile, stesso stile di `_build_row`/ML-01); `build_feature_history(fixture_id)` produce una riga per OGNI snapshot noto (uno storico completo, base per un futuro dataset di training LIVE-03). "Schema documentato" (acceptance criteria): docstring del modulo con l'elenco completo delle colonne per famiglia, piu' riga dedicata in `src/ml/README.md`. Nessuna migration DB (nessuna nuova tabella, solo lettura dei dataset LIVE-01/BET-06 gia' esistenti). Nessun endpoint API ne' modifica al frontend (non richiesti dal task, che indica solo `src/ml/live/`; l'esposizione/consumo di queste feature e' scope di LIVE-03). 15 nuovi test in `tests/service/live_feature_store_test.py` (funzioni pure di estrazione feature senza DB; vero SQLite in-memory per `LiveFeatureStore` end-to-end, incluso il caso "nessuno snapshot ancora disponibile" e il caso cruciale anti-leakage "eventi/statistiche registrati DOPO `as_of` esclusi dalla riga"); suite completa rieseguita: 658/658 passed (era 643/643 dopo LIVE-01).
- **Modelli live separati (LIVE-03, NEW, Fase LIVE ORACLE — ULTIMO TASK DELLA ROADMAP)**: due nuovi moduli in `src/ml/live/`. `live_match_outcome_dataset.py::build_match_outcome_dataset` costruisce, SOLO per le fixture GIA' CONCLUSE (`completed_fixture_snapshots`: ultimo snapshot con stato finale e goals noti, stessa semantica di `_FINAL_STATUSES`/LIVE-01), un frame con UNA RIGA PER OGNI snapshot noto (`LiveFeatureStore.build_feature_history`, LIVE-02, non modificato) e target 1X2 (`label_1x2`, riusato da `src.ml.markets.market_1x2` senza duplicarne la definizione) COSTANTE per tutte le righe della stessa fixture, essendo il risultato REALE finale — mai dalle quote. `live_match_outcome_model.py` addestra e valida il modello: `match_level_temporal_splits` (acceptance criteria "Split per match/time" + "No leakage eventi futuri") riduce il frame a 1 riga per fixture (`match_time` minimo = proxy kickoff), applica `expanding_window_splits` (ML-02, invariato) su quel frame ridotto per ottenere split A LIVELLO DI MATCH, poi espande ogni indice di match a TUTTE le righe della stessa fixture nel frame originale — garantendo per costruzione che nessuna fixture compaia sia in train che in validation nello stesso fold (uno split riga-per-riga mescolerebbe minuti diversi della STESSA partita tra le due parti, un leakage evidente). `train_live_match_outcome_model` confronta due pipeline semplici (logistic/random_forest, senza `GridSearchCV` per restare veloce) sullo stesso OOF walk-forward multiclasse (`temporal_oof_multiclass_probabilities`/`compute_multiclass_probability_metrics`/`multiclass_champion_score`, tutti riusati da MARKET-01 senza modifiche), seleziona il campione e lo registra nel `ModelRegistry` con `market='1x2_live'` — chiave ESPLICITAMENTE diversa da `'1x2'` (il modello pre-match di `market_1x2.py`), cosi' che stage/lifecycle/promotion restino completamente indipendenti tra le due pipeline (acceptance criteria "Pipeline separata da pre-match"), sempre `stage='candidate'` (mai auto-production). `minute_window_report` (acceptance criteria "Metriche live per minuto/finestra"): equivalente multiclasse di `grouped_probability_report` per finestre `00_15/15_30/.../75_90/90_plus/FT` (`minute_window_label`, uno stato finale vale sempre `'FT'` indipendentemente dal minuto), verificato che la finestra `FT` abbia log_loss nettamente migliore di `00_15` (sanity check "probabilita' aggiornate": vicino alla fine lo scoreline e' quasi quello reale, a inizio partita e' quasi sempre 0-0). `LiveMatchOutcomeExpert` (stesso contratto di `Market1x2Expert`: `predict_proba`/`predict_proba_dict`/`load_production`/`load_latest`/`from_estimator`). Nessuna migration DB (nessuna nuova tabella, solo lettura di LIVE-01/LIVE-02/BET-06 gia' esistenti). Nessun endpoint API ne' modifica al frontend (non richiesti dal task, che indica solo `src/ml/live/`). 20 nuovi test (`tests/service/live_match_outcome_dataset_test.py`: vero SQLite in-memory, fixture concluse vs non concluse, target costante per fixture, piu' fixture con target indipendenti; `tests/service/live_match_outcome_model_test.py`: frame sintetico deterministico con gol assegnati a minuti espliciti (scoreline parziale sempre coerente col finale), `minute_window_label` per ogni bucket, `match_level_temporal_splits` con verifica esplicita "nessuna fixture a cavallo tra train e valid" + ordine cronologico, training end-to-end con report per finestra e sanity check FT<00_15, dataset vuoto/insufficiente/etichette inattese, `LiveMatchOutcomeExpert`); suite completa rieseguita: 678/678 passed (era 658/658 dopo LIVE-02). **Con LIVE-03 la roadmap operativa di Soccer Oracle V2 e' COMPLETA: 53/53 task, 12/12 fasi (`MANIFEST.json`).**
- **Cambio sorgente DATABASE_URL: DB dev remoto su Railway (INFRA, 2026-09-04)**: richiesto esplicitamente ("puntare sempre al remoto dev d'ora in poi") - `DATABASE_URL` di riferimento (`properties/config.env`, `docker-compose.yml` per `api`/`scheduler`, `alembic.ini`) sostituito dal precedente Postgres nativo Windows (`localhost:5432/match_db`) con il Postgres remoto "dev" gia' in uso nella sessione precedente per verifiche/restore (`sakura.proxy.rlwy.net:18862/railway`, credenziali in chiaro nel file come da convenzione gia' in uso in questo progetto per `properties/config.env`) - stessa unica sorgente per esecuzione locale, Docker e Alembic (nessun DB duplicato). Connessione riverificata (`SELECT current_database(), now()`) prima del cambio; nessuna modifica di codice applicativo (nessun modulo Python toccato, `AppConfig.database_url`/`repository_db.py` gia' generici tramite env var) - i test NON sono impattati perche' non leggono `DATABASE_URL` (usano SQLite in-memory dedicato o mock, verificato via ricerca nel codice). Vecchi valori locali mantenuti come riferimento in commento in ciascun file modificato. `README_PLATFORM.md` (sezione "Policy DATABASE_URL") aggiornato di conseguenza.
- **Fix incoerenza residua DATABASE_URL (INFRA, 2026-09-04, stessa giornata)**: verifica di follow-up del task precedente ("prossimo task": bonifica di tutti i punti che referenziano ancora il DB locale) ha trovato 2 punti NON allineati al cambio sopra: (1) `docker-compose.yml`, servizio `api` (`environment:`) era rimasto agganciato al vecchio `postgresql://postgres:postgres@host.docker.internal:5432/match_db` mentre `scheduler` gia' puntava a Railway - stesso file, due servizi disallineati; (2) `DEFAULT_DATABASE_URL` in `src/service_ia/config/app_config.py` (fallback usato SOLO quando `DATABASE_URL` non e' impostata affatto) era rimasto sul vecchio valore locale. Fix: rimosso l'override esplicito `DATABASE_URL` da `environment:` per ENTRAMBI i servizi Docker (`api` e `scheduler`) - ora ereditano il valore UNICO da `properties/config.env` via `env_file`, eliminando la duplicazione della stessa credenziale in due punti dello stesso file (causa root della divergenza) invece di limitarsi a correggere il valore copiato; `DEFAULT_DATABASE_URL` allineato al DB dev Railway. Aggiornati anche i commenti/README (`README_PLATFORM.md` sezione "Configurazione" e "Policy DATABASE_URL") che mostravano ancora l'URL locale come esempio. Nessuna modifica a `alembic.ini`/`properties/config.env` (gia' corretti). Nessun modulo di logica applicativa toccato oltre alla costante di default; nessuna migration necessaria.
- **Bugfix critico riduzione build context Docker (INFRA, 2026-09-05)**: il build context Docker era gonfiato a ~840MB (56s solo per il transfer ad ogni rebuild), causato da cartelle NON referenziate dal codice attivo sotto `src/` ma non escluse da `.dockerignore`: `/service_ia` (copia legacy duplicata in radice, il codice attivo importa sempre da `src.service_ia...`), `src/service_ia/training/under_over/inconsistent` (esperimento di training abbandonato) e `src/service_ia/dataset` (CSV/Excel di un vecchio flusso pre-DB). Aggiunte al `.dockerignore` (con `**/__pycache__`/`**/best_models`/`**/backup_db` per coprire anche le occorrenze annidate, non solo quelle in radice). Nessuna modifica di codice applicativo.
- **Bugfix critico gestione quota API-Sports (2026-09-05/06, NEW)**: diagnosticato da ore di debug su `jobs_history.jsonl` (import che restava a `fixtures_seen=0` per QUALSIASI lega/stagione/data) che API-Sports risponde HTTP 200 anche a quota esaurita, con un campo `errors` nel body invece di uno status code 4xx — senza un controllo esplicito il chiamante scambiava "quota finita" per "nessuna partita trovata". Nuova `ApiSportsQuotaExceededError` (`src/service_ia/pre_processing/api_sports_provider.py`) sollevata da `ApiSportsProvider.request()` quando il body contiene `errors`. Bugfix successivo (stesso giorno, `_is_daily_quota_error`): API-Sports usa la stessa struttura `errors` sia per la quota GIORNALIERA (chiave `"requests"`, permanente fino al reset del giorno) sia per il rate-limit AL MINUTO (chiave `"rateLimit"`, transitorio, si risolve da solo) — prima venivano trattati allo stesso modo, abbandonando per sempre singole fixture colpite da un blip momentaneo invece di ritentarle (5 nuovi test in `tests/service/api_sports_provider_test.py`). Bugfix "stop immediato" in `download_import_matches` (`src/service_ia/pre_processing/download_match_service.py`): `ApiSportsQuotaExceededError` e' una `RuntimeError` e prima veniva inghiottita dal blocco `except Exception` generico nel loop per-fixture — il loop CONTINUAVA su tutte le fixture/leghe rimanenti (centinaia, nella finestra "Aggiorna tutto") senza mai impostare il nuovo flag `report["quota_exceeded"]`, causa principale per cui il job restava "in esecuzione" per decine di minuti/ore invece di fermarsi subito come gia' avveniva per l'eccezione sollevata da `get_fixtures` (2 nuovi test in `tests/service/download_match_test.py::TestDownloadImportMatchesQuotaExceededMidway`, incluso il caso multi-lega). Nuovo modulo `src/jobs/api_quota_state.py`: stato quota persistito su file JSON condiviso (`best_models/api_quota_state.json`, scrittura atomica file-temp+`os.replace`, merge mai overwrite) tra i container `api`/`scheduler` (processi separati) — `record_quota_snapshot` (stima PASSIVA dagli header `x-ratelimit-*`, dimostrata inaffidabile da sola: un `/status` HTTP 200 con `errors` di quota esaurita puo' arrivare insieme a un header "sano" tipo 7499/7500 rimanenti) e `record_status_snapshot` (check AUTORITATIVO, popolato da `ApiSportsProvider.get_status()` — nuovo metodo che interroga l'endpoint ufficiale `GET /status` — o automaticamente non appena una qualsiasi chiamata dati incontra `errors`). Nuovi endpoint `GET /settings/quota` (rilettura veloce della cache, nessuna chiamata reale) e `POST /settings/quota/refresh` (unico modo per un controllo live reale, consuma 1 chiamata) con logica di merge in `_build_quota_response`/`src/api/main.py` che preferisce SEMPRE lo snapshot live quando riferito alla giornata UTC corrente (`_is_today_utc`, la quota si resetta a mezzanotte UTC — uno snapshot di ieri non e' piu' affidabile oggi).
- **Job "Aggiorna tutto" / Daily Refresh (2026-09-05/06, NEW)**: nuova funzione `run_daily_refresh` (`src/jobs/scheduler.py`) che combina, in un'unica azione, l'import delle partite disputate IERI (tutti i campionati censiti se `leagues` non filtrato) con la sync del calendario prossimo (`days_ahead` giorni, default 7, riusa DIRETTAMENTE `run_manual_import`/`run_manual_future_sync` gia' esistenti — nessuna logica di fetch duplicata), loggando sia le due sotto-fasi singolarmente (`job_type` `daily_refresh_played`/`future_sync`) sia una riga "ombrello" `daily_refresh` col riepilogo. Nuovo sesto job schedulato `data_daily_refresh` (`CronTrigger`, orario configurabile `AppConfig.daily_refresh_hour`/`minute`/`daily_refresh_days_ahead`, env `DAILY_REFRESH_HOUR`/`MINUTE`/`DAYS_AHEAD`, default 05:00/7gg — volutamente DOPO mezzanotte e PRIMA di `future_sync_hour`/`training_hour`, cosi' "ieri" e' un giorno ormai completo). Nuovo endpoint `POST /jobs/daily-refresh` (schema `JobDailyRefreshRequest`). Frontend: il bottone "Aggiorna tutto" — PRIMA confinato al solo Data Center — e' stato spostato nella Sidebar (`frontend/src/features/layout/Sidebar.jsx`, sempre montata/visibile in ogni pagina, non solo restando su Data Center) con banner di stato (`RefreshBanner`: spinner mentre in corso, riepilogo ✅/❌ a fine job, avviso dedicato se `quota_exceeded`) e barra di progresso; `App.jsx` traccia il job in corso (`fullRefreshJobId`/polling di `/jobs/history` ogni 4s finche' non risulta `success`/`failed`) e ricarica automaticamente i dati dashboard al termine. Data Center (`DataCenterPage.jsx`) riorganizzato in due pannelli distinti "Partite disputate"/"Partite prossime" con testo esplicativo che rimanda al nuovo bottone sidebar per l'azione combinata, mantenendo le azioni granulari (import storico/settlement/aggiorna oggi/sync future) per backfill/debug mirato. `AppConfig` esteso con `daily_refresh_hour`/`daily_refresh_minute`/`daily_refresh_days_ahead`.
- **Pagina Impostazioni: job scheduler on/off + quota API-Sports (2026-09-05/06, NEW)**: nuovo modulo `src/jobs/job_settings.py` — `JOB_DEFINITIONS` (label/descrizione/default per ciascuno dei 6 job schedulati, chiavi allineate ESATTAMENTE ai `job_id` di `build_scheduler`) persistito su file JSON dedicato (`best_models/job_settings.json`, stessa scrittura atomica di `api_quota_state.py`); `data_sync_live` disabilitato di DEFAULT (job piu' costoso in chiamate API-Sports, polling ogni pochi secondi — va attivato esplicitamente da chi lo vuole). `is_job_enabled(job_id)` e' interrogato dal nuovo wrapper `_run_if_enabled` (`src/jobs/scheduler.py`, ogni job ora registrato come `functools.partial(_run_if_enabled, job_id, func, ...)`) PRIMA di ogni singola esecuzione (non solo alla registrazione) — un toggle da Impostazioni ha quindi effetto immediato, senza restart del container `scheduler` (test aggiornati con helper `_target_func` in `scheduler_test.py` per estrarre la funzione reale da dentro il `partial`). Nuovi endpoint `GET/POST /settings/jobs` (schema `JobSettingsResponse`/`JobSettingsUpdateRequest`, chiavi non riconosciute rifiutate con 400). Nuova pagina frontend `frontend/src/features/settings/SettingsPage.jsx` (voce menu "Impostazioni"): lista job con switch enabled/disabled (optimistic update, poi riallineato con la risposta server) + pannello quota API-Sports con barra percentuale (verde/giallo/rosso sotto/sopra 70%/90%) che distingue esplicitamente il valore "stimato" (polling automatico leggero, SOLO cache locale, nessuna chiamata reale consumata) dal valore "reale" (bottone "Aggiorna" esplicito, unico trigger di un vero check via `POST /settings/quota/refresh`) — bug diagnosticato 2026-09-05 in cui la barra poteva mostrare 0% con la quota reale gia' al 100%, risolto esattamente da questa distinzione.
- **Consolidamento Dashboard frontend (2026-09-06, REFACTOR)**: le tre pagine separate "Dashboard live"/"Partite in diretta"/"Match Center" (`LivePage.jsx`, `TodayPage.jsx`, `MatchesDayPage.jsx`, `MatchesLivePage.jsx`, tutte rimosse) sono state consolidate in un'unica `DashboardPage.jsx` con due nuovi componenti riusabili di filtro: `PhaseTabs` (Tutte/Da giocare/In diretta/Finite) e `MarketTabs` (un tab per mercato con modello registrato + "Tutti i mercati", sincronizzato con lo stesso stato `selectedMarket` gia' condiviso dal dropdown "Mercato" in `TopFilters`). Menu (`menu.js`) semplificato — rimosse le voci "Dashboard live"/"Partite in diretta"/"Match Center" duplicate, aggiunta "Impostazioni". Nuovo endpoint `GET /dashboard/available-dates` (`DashboardService.get_available_dates`, schema `DashboardAvailableDatesResponse`): elenco date selezionabili in `TopFilters` accumulato progressivamente dal "giorno 1" di utilizzo (persistito su file `best_models/dashboard_dates_state.json` alla prima chiamata, esteso all'indietro se il Prediction Ledger contiene una prediction ancora piu' vecchia via nuovo `PredictionLedgerRepository.get_earliest_created_date`), al posto di un calendario libero — richiesta esplicita utente. Nuovo formatter `formatDateIt` (`gg/mm/aaaa`) per la nuova select.
- **Fix critici performance/concorrenza DB (2026-09-06)**: diagnosticati da `jobs_history.jsonl` (job "Aggiorna tutto" rimasto "in esecuzione" per decine di minuti/ore, errori SQLAlchemy "concurrent operations are not permitted"/"Method close() can't be called here", violazioni FK su `odds_snapshot`). (1) **Concorrenza tra thread**: `SessionLocal` (`src/repository/base/repository_db.py`) era un `sessionmaker` "nudo" condiviso — `CrudRepository` e' usato da repository singleton di MODULO (`repo_match`/`repo_snapshot` in `download_match_service.py`) condivisi tra job schedulati DIVERSI (es. `data_sync_today` + `data_settlement`) eseguiti in thread APScheduler separati ma nello stesso processo: fix con `scoped_session` (Session thread-local, mai piu' condivisa tra thread diversi). (2) **Performance round-trip**: `CrudRepository.session` era creata UNA VOLTA in `__init__` e i metodi (`save`/`filter_by`/...) la chiudevano ad ogni chiamata (`with self.session as session:`), costringendo SQLAlchemy a riaprire connessione/transazione verso il DB remoto (Railway) ad ogni singola query — misurato fino a ~25s/fixture in produzione, causa principale della lentezza. Fix: `session` diventa una property che risolve sempre la Session scoped del thread corrente (mai chiusa dai metodi), con un setter dedicato SOLO ai test per iniettare SQLite in-memory; cleanup esplicito (`SessionLocal.remove()`) a fine job in `download_import_matches`. (3) **Bulk upsert quote**: `OddsSnapshotRepository.save_many` faceva `session.merge()` in loop Python (una SELECT implicita per riga — migliaia di round-trip sequenziali per singolo job, fino a 7196 snapshot osservati in un solo "Aggiorna tutto"): sostituito con un vero `INSERT ... ON CONFLICT (id_snapshot) DO UPDATE` (`sqlalchemy.dialects.postgresql.insert`) in batch da 500 righe, stesso comportamento idempotente sulla PK ma in poche query invece di migliaia. (4) **Bugfix minore dati**: `calculate_mean`/`get_value` ora coerce robustamente valori stringa numerici restituiti da API-Sports (es. `goals_prevented` come stringa senza suffisso `%`, che prima faceva crashare `statistics.mean()` con "can't convert type 'str'"). Suite completa rieseguita: 688 passed. Import `src.api.main` verificato end-to-end (51 route registrate, +6 rispetto a LIVE-03). Build frontend (`npm run build`) verificata senza errori.
- **Auto-pausa job + disabilitazione bottoni a quota esaurita (2026-09-06, NEW)**: richiesto esplicitamente ("quando arrivo al 100% smettere di chiamare il servizio", poi "disabilitando anche i bottoni che richiamano il servizio quando sono al 100%"). Nuovo `api_quota_state.is_quota_exhausted_today()`: True SOLO se l'ultimo check AUTORITATIVO (`status_error_message`, popolato ESCLUSIVAMENTE da `ApiSportsProvider._mark_quota_exhausted` — mai dalla stima passiva `daily_remaining`, dimostrata inaffidabile da sola, vedi bugfix 2026-09-05/06) e' riferito alla giornata UTC corrente. `job_settings.py`: ogni `JOB_DEFINITIONS` ora dichiara `calls_api_sports` (derivato in `QUOTA_SENSITIVE_JOB_IDS`, single source of truth) — SOLO `data_daily_refresh`/`data_sync_today`/`data_future_sync`/`data_sync_live` chiamano davvero il provider esterno; `data_settlement` (riconciliazione locale) e `ml_training` (training sui dati gia' a DB) NON consumano quota e non vengono mai toccati dall'auto-pausa. Nuova `sync_job_settings_with_quota()`: quando la quota risulta esaurita OGGI, disattiva i soli job quota-sensitive salvandone la baseline pre-pausa (`best_models/quota_pause_state.json`, stessa scrittura atomica degli altri stati) e la RI-APPLICA ad ogni chiamata successiva (non solo alla prima rilevazione) — un eventuale toggle manuale durante la pausa viene quindi corretto di nuovo al controllo seguente invece di restare "acceso" fino al giorno dopo; al reset (quota non piu' segnalata esaurita per oggi) ripristina ESATTAMENTE lo stato pre-pausa. Chiamata sia da `scheduler.py::_run_if_enabled` (PRIMA di ogni esecuzione di OGNI job, non solo alla registrazione) sia da `GET/POST /settings/jobs` (`src/api/main.py`), cosi' lo stato e' coerente sia che lo scheduler giri da solo sia che l'utente apra Impostazioni. `JobSettingsResponse`/`JobSettingRow` estesi con `quota_paused`/`quota_paused_since`/`calls_api_sports` per il frontend. Verificato manualmente end-to-end (path temporanei isolati, nessun file reale toccato): pausa/idempotenza/toggle-corretto-di-nuovo/non-toccare-job-non-quota-sensitive/ripristino-alla-baseline tutti confermati. Frontend: `App.jsx` ora carica `apiQuota` GLOBALMENTE (non solo mentre Impostazioni e' aperta — al mount in `loadMetaData` + refresh ogni 60s, sempre solo cache locale, nessuna chiamata reale) e calcola `isQuotaExhausted` (`daily_used_percentage >= 100`); propagato a `Sidebar` (bottone "Aggiorna tutto" disabilitato + hint sostituito da avviso), `DataCenterPage` ("Import storico"/"Aggiorna oggi"/"Sync future" disabilitati + banner — "Settlement finali" resta SEMPRE attivo, non chiama API-Sports) e `OpsPage`/ML Lab ("Import giornaliero" disabilitato — "Retrain mercati" resta SEMPRE attivo). `SettingsPage.jsx`: banner dedicato quando `quota_paused` (elenca esattamente quali job sono in pausa) + badge "API-Sports" sui job quota-sensitive nella lista. Nuovo stile globale `button:disabled` (opacity/cursor) + `.quota-exhausted-hint`/`.quota-pill`. Nessuna migration DB. Suite completa rieseguita: 688/688 passed. Import `src.api.main` verificato end-to-end.
- **Fix critico performance cambio-giorno Dashboard + riduzione ulteriore consumo quota API-Sports (2026-09-07, NEW)**: diagnosticato manualmente (cambio data in Dashboard percepito come lento, quota consumata anche per date storiche gia' importate). Quattro fix indipendenti, tutti retro-compatibili (nessuna migration DB, nessun nuovo endpoint): (1) **Query multi-mercato consolidata** (`FilterMarketService`, `src/service_ia/training/market_service/filter_market_service.py`): il vecchio `build_prediction_frame(market, fixture_id)` rifaceva la query `Match` (+ relazioni `statistics`/`odds`) da ZERO per ciascuno dei mercati richiesti (fino a 9 `SUPPORTED_MARKETS`), chiamato in loop da `DashboardService._predict_fixture` - 9x query per singola fixture, causa principale della lentezza con decine di fixture per giorno. Nuovi `build_prediction_frames(fixture_id, markets)` (UNA query condivisa tra tutti i mercati) e `build_prediction_frames_from_match(match, markets)` (ZERO query: `match` gia' un ORM `Match` o dict, puro calcolo in-memory via `_build_row`); `build_prediction_frame` diventa un wrapper di `build_prediction_frames` (retro-compatibilita' per `main.py`/`monitoring_service.py`/`model_consensus.py`, invariati). `DashboardService._predict_fixture` accetta ora un `db_match` opzionale: quando il chiamante ha GIA' un `Match` caricato in BATCH (`_serialize_api_fixture`/`_serialize_match`, entrambe basate su `_fetch_matches` con `selectinload` sull'intera finestra di date), usa `build_prediction_frames_from_match` (zero query aggiuntive) invece di rifare una query dedicata per fixture. (2) **`get_overview` non calcola piu' le predizioni**: il frontend (`DashboardPage`) legge da questa risposta SOLO i counts/badge di riepilogo (`counts.total/live/to_play`, `model_markets.length`) e MAI i valori di predizione (verificato su tutto il codice frontend) - prima, con `with_predictions=True` e `limit=0`, l'endpoint ricalcolava le predizioni ML per OGNI fixture del giorno IN AGGIUNTA a `/dashboard/day` (stessa data, chiamato in parallelo dallo stesso `loadDashboardData`): un secondo giro identico e completamente inutilizzato di query DB + inferenza modello. Ora `get_overview` chiama `get_day_matches(..., with_predictions=False)`. (3) **Skip chiamata API-Sports per date STORICHE gia' sincronizzate a DB** (`DashboardService.get_day_matches`): il DB locale viene sincronizzato quotidianamente dal job "Aggiorna tutto"/`data_daily_refresh` + `data_sync_today`/`future_sync` (finestra ieri -> oggi+N giorni, tutti i campionati censiti) - una partita gia' CONCLUSA (`target_date` < oggi UTC) ha risultato/quote ormai DEFINITIVI, quindi se il DB ha GIA' almeno una riga per quella data esatta (`db_has_target_date`, valutata sulla data ESATTA e non sulla finestra allargata di `_fetch_matches`) il provider esterno non viene piu' interrogato (prima: sempre chiamata, fino a N richieste HTTP per cambio data, una per campionato configurato). Le date di OGGI e FUTURE restano invece SEMPRE interrogate (quote in movimento, rinvii, live) anche se il DB ha gia' un dato sincronizzato nelle ultime 24h - l'API resta la fonte primaria, protetta comunque da cache TTL 60s e dal guard quota-esaurita. Nuovo `force_refresh` (bottone "Forza aggiornamento" in `TopFilters`, propagato `App.jsx` -> `GET /dashboard/day?force_refresh=true` -> `DashboardService.get_day_matches`/`_fetch_api_day_fixtures`) per i rari casi in cui serve ri-sincronizzare a mano anche una data storica gia' a DB (dato importato errato, correzione tardiva del provider): bypassa SOLO lo skip DB-first e la cache TTL, MAI il guard quota-esaurita (nessun bottone puo' forzare una chiamata quando la quota e' al 100%, stessa policy di tutti gli altri bottoni che chiamano API-Sports). (4) **Guard quota-esaurita su TUTTE le fetch dirette verso il provider esterno** in `dashboard_service.py` (`_fetch_api_day_fixtures`, `_fetch_api_live_fixtures`, `_fetch_api_fixture_detail`, `_fetch_api_events`, `_fetch_api_odds`): con `is_quota_exhausted_today()` (BET-06/2026-09-06, non modificato) gia' vero, la chiamata fallirebbe comunque - evitato lo spreco di tempo/rete e il rischio di ulteriori penalita' dal provider, protegge in un colpo solo ogni punto di ingresso (polling frontend ogni 60s, job schedulati, richieste manuali, incluso `force_refresh`). Frontend (riduzione dei giri di rete per cambio tab, non solo backend): `App.jsx::loadDashboardData` ora scarica SEMPRE il giorno intero con tutte le fasi/mercati in un colpo solo (niente piu' `phase`/`markets` in query string) - i tab Fase/Mercato (`PhaseTabs`/`MarketTabs`) filtrano poi ISTANTANEAMENTE in memoria i dati gia' scaricati (`dashboardDayData` = `useMemo` su `dayData` + nuovo `filterRowByMarket`, `frontend/src/features/shared/formatters.js`: filtra `predictions`/`decision_cards`/`best_decision` di una riga gia' calcolata, nessun ricalcolo lato client), senza piu' rifare la fetch/ricalcolare le predizioni ML ad ogni click su un tab (prima: un giro di rete completo per ogni cambio Fase/Mercato). Il polling automatico ogni 60s salta `loadDashboardData("silent")` quando `isQuotaExhausted` (il backend rifiuterebbe comunque le fetch esterne) ma continua a rileggere `apiQuota` (sola cache locale) per accorgersi del reset appena avviene. `getDashboardLive` (preview "Partite in diretta") passa ora `withPredictions: false` (quella preview non mostra badge/predizioni, nessun bisogno di calcolarle). 15 nuovi test in `tests/service/filter_market_service_test.py` (nuovo file: query condivisa tra piu' mercati con vero SQLite in-memory e conteggio effettivo delle chiamate `filter_by`, retro-compatibilita' wrapper singolo mercato, fixture sconosciuta, mercato invalido, `build_prediction_frames_from_match` con ORM/dict/markets vuoti) + 14 nuovi test in `tests/service/dashboard_service_test.py` (riuso di `db_match` senza query extra vs fallback a query quando assente, `get_overview` che non calcola mai predizioni, skip API per date storiche gia' sincronizzate vs fallback quando il DB e' vuoto per quella data esatta vs sempre chiamata per oggi/futuro, `force_refresh` che bypassa lo skip storico ma resta bloccato dal guard quota-esaurita, guard quota su tutte e 5 le funzioni di fetch diretto con regressione esplicita "continua a funzionare quando la quota non e' esaurita"). Suite completa rieseguita: 712/712 passed. Import `src.api.main` verificato end-to-end (51 route, invariate - nessun nuovo endpoint). Build frontend (`npm run build`) verificata senza errori.
- **Data Quality + fix promotion-gate per Under/Over multi-linea (estensione MARKET-04, 2026-09-07, NEW)**: task esplorativo a fasi su Under/Over 1.5/2.5/3.5/4.5, propedeutico a un retrain informato. **Fase 0** (copertura dati REALE prima di addestrare): nuovo `DataQualityService.build_under_over_threshold_report` (`src/data/quality_report_service.py`) — per ciascuna soglia calcola fixture con odds/statistics/mean_statistics disponibili, righe REALMENTE utilizzabili per il training (riusa `FilterMarketService._build_row`, NESSUNA logica di filtro duplicata), class balance, distribuzione stagione/lega e disponibilita' della CV temporale (`expanding_window_splits`, stessa soglia minima di `train_multi_market._filter_valid_splits`); accetta un parametro opzionale `matches` gia' caricato per condividere una singola query pesante con altri report dello stesso script di analisi. Eseguito su dati reali (`scripts/analysis/phase0_under_over_data_quality.py`, risultato in `best_models/under_over_phase0_report.json`): 47529 fixture totali (43325 con statistics, coverage odds generale 43.4%), copertura odds per soglia 1.5/2.5/3.5/4.5 rispettivamente 23.4%/41.7%/23.4%/23.3% (solo 2.5 e' il mercato piu' liquido), 8793 fixture con odds valide su TUTTE e 4 le soglie contemporaneamente. **Fase 1+2** (benchmark su dati reali, `scripts/analysis/phase1_2_totals_benchmark.py` via `run_totals_benchmark_from_db`, risultato in `best_models/phase1_2_totals_benchmark_result.json`): su 15235 righe utilizzabili, vincitore aggregato `goal_distribution` (score 0.7300) davanti a `hierarchical` (0.7114) e `binary_independent` (0.6916); nuovo campo ADDITIVO `TotalsBenchmarkReport.best_approach_by_threshold` (`src/ml/markets/totals/totals_market.py`, non altera `best_approach` aggregato ne' la proiezione monotona finale) rivela che il vincitore NON e' uniforme tra le soglie: over_1_5/3_5/4_5 preferiscono `goal_distribution`, ma over_2_5 preferisce `binary_independent` (0.6683 vs 0.6074 di hierarchical su quella soglia specifica) — visibile solo grazie al nuovo campo, prima nascosto dentro l'aggregato. **Fix critico Fase 4 (promotion gate)**: `run_totals_benchmark`/`_save_winning_model` salvavano nel registry SOLO `approach_aggregate_score`, chiave MAI cercata da `promotion_policy._resolve_metric` (che richiede `selection_score`/`log_loss`/`brier`/`ece`/`auc`/`sample_size`) — il gate di promozione (OPS-02) avrebbe quindi fallito SEMPRE per 'totals' con 'sample_size non disponibile', indipendentemente dalla qualita' del modello. Fix con nuovo `_aggregate_metrics_for_winning_approach` che espone le chiavi standard mediando sulle 4 soglie SOLO le metriche del `best_approach` gia' calcolato (nessuna nuova selezione/logica di monotonicita'). **Fase 3** (A/B test cascading, puramente sperimentale): mirato SOLO sulla soglia 2.5 dove hierarchical perde sistematicamente — `scripts/analysis/phase3_cascading_ab_test.py` (NON modifica `totals_market.py`) aggiunge `gd_over_1_5` (probabilita' Poisson EXP-02 della soglia precedente, gia' out-of-fold per costruzione, nessun training che potrebbe overfittare in-sample) come feature extra a un classificatore binario altrimenti identico a `binary_independent`: selection_score 0.6715 (baseline) vs 0.6723 (+feature), delta +0.0008, sotto la soglia di adozione (>0.01) — **VERDETTO: feature scartata**, nessuna modifica alla pipeline di produzione (risultato documentato in `best_models/phase3_cascading_ab_result.json`). 7 nuovi test (`tests/service/data_quality_service_test.py::TestBuildUnderOverThresholdReport`, `tests/service/totals_market_test.py`), verificati mirati: 28/28 passed su questi due file; baseline suite completa precedente 716 passed (nessun altro file toccato oltre ai 2 sorgenti + 2 test).
- **Fix critici pipeline `train_market` (2026-09-07, NEW) — obiettivo: sbloccare il salvataggio di modelli ML REALI come 'candidate' per Under/Over 1.5/2.5/3.5/4.5**: il pilot `train_market('under_over_1_5')` (Fase 1 parte B) non aveva mai completato con successo, quindi `best_models/registry/index.jsonl` restava vuoto per ogni mercato. Due bug distinti, entrambi in `src/service_ia/training/train_multi_market.py`: **(1)** griglia `RandomForestClassifier` troppo pesante (`n_estimators=500` fisso x `max_depth` 4 valori x `min_samples_split` 3 x `min_samples_leaf` 3 = 36 combinazioni x `selector__k` 3 valori x 5 fold CV = 540 fit da 500 alberi ciascuno) — su dataset con 13+ stagioni storiche mandava il processo in MemoryError prima di completare (osservato localmente, `retrain_local_20260903.log`). Alleggerita a `n_estimators=200` fisso x 2x2x2=8 combinazioni (stesse 3 dimensioni, 2 valori ciascuna) x `selector__k`, circa 11x meno lavoro totale, stessa logica di selezione. **(2)** bug piu' subdolo, MAI emerso nei test unitari esistenti (che testano solo `_model_space`/`_extract_selected_features` in isolamento, non l'intera `train_market` end-to-end): lo `StackingClassifier` veniva costruito con `cv=cv_splits` (il walk-forward ESTERNO, riferito a posizioni del dataset COMPLETO) e poi passato sia a `cross_val_score(stacking, X, y, cv=cv_splits, ...)` sia, via `temporal_oof_probabilities`/`CalibrationService`, rifittato fold-per-fold su sottoinsiemi PIU' PICCOLI di `X` — in entrambi i casi lo stesso `cv_splits` fissato in costruzione viene riusato internamente come cv per generare le meta-feature OOF su un `X` piu' piccolo di quello su cui gli indici erano stati calcolati, producendo sistematicamente `ValueError: cross_val_predict only works for partitions` (riprodotto isolatamente con un caso minimo prima del fix). Fix: `cv=5` (intero, standard) nello `StackingClassifier` — lo stesso pattern gia' corretto usato da `CalibratedClassifierCV(..., cv=3)` in `CalibrationService`, che genera lo split OOF localmente su qualunque `X` gli venga passato in `fit()`, senza riferimenti a indici esterni. Validato con dataset sintetico end-to-end (400 righe, 15 feature): `train_market` ora completa senza errori e produrrebbe un candidate (logistic/random_forest/voting/stacking, championship su `selection_score`). Suite completa rieseguita su Python 3.13 (necessario: `src/service_ia/pre_processing/df_odds_service.py` usa una f-string con quote nidificate identiche, sintassi valida solo da Python 3.12): 720/720 passed.
- **Nuovo script di analisi `scripts/analysis/phase3b_sequential_stacking_thresholds.py` (2026-09-07, NEW)**: implementa l'idea aggiuntiva "stacking sequenziale a cascata tra soglie" (DISTINTA sia da 'hierarchical' — un solo modello multiclasse — sia da `gd_over_1_5`/Fase 3 — probabilita' Poisson deterministica): allena un RandomForest (stessi `_RF_KWARGS` di `totals_market.py`) per over_1.5, ne usa la probabilita' OOF come feature aggiuntiva per over_2.5, la cui OOF diventa feature per over_3.5 e cosi' fino a over_4.5. Anti-leakage verificato ESPLICITAMENTE (non solo per costruzione, principio ML-01/`assert_no_leakage`) da `_assert_cascade_no_future_leakage` (testata anche in negativo: rileva correttamente una riga di training iniettata nel "futuro" rispetto al proprio fold di validation) piu' un dominio di training dinamicamente ristretto alle sole righe con una feature OOF di soglia precedente non-NaN (non un'euristica: emerge che ogni livello di cascata "consuma" un fold iniziale in piu' — su un dataset sintetico a 5 fold, il campione valutabile scende da 250 righe per 1.5 a 100 per 4.5 — un limite strutturale intrinseco all'idea, documentato esplicitamente in `n_eval_rows` per ciascuna soglia nell'output). Ogni soglia a valle di 1.5 viene confrontata contro un baseline `binary_independent` allenato sullo STESSO training set ristretto ma senza la feature a cascata, per isolare l'effetto della sola feature (non del dataset ridotto). Script pronto ma **non ancora eseguito su dati reali** (vedi blocco rete sotto).
- **BLOCCO AMBIENTE (2026-09-07): impossibile eseguire il training reale (dati DB) da questa sessione cloud**. `DATABASE_URL` punta al Postgres remoto Railway (`sakura.proxy.rlwy.net:18862`, connessione TCP diretta via `psycopg2`, non HTTP/HTTPS). La policy di rete di questo ambiente (`anthropic_cloud`/"Predefinito", vedi proxy README) instrada SOLO traffico HTTPS attraverso il proxy applicativo e blocca esplicitamente "raw-TCP databases"/porte non-443: confermato con un connect TCP diretto (`socket.connect`) verso quell'host:porta, timeout pulito senza risposta (comportamento tipico di un drop a livello firewall, non un errore applicativo). Confermato ANCHE a livello di documentazione ufficiale (Anthropic, "Configure cloud environments"): qualunque livello di `network access` (Trusted/Full/Custom/None) instrada comunque tutto il traffico attraverso un proxy di sicurezza SOLO HTTP/HTTPS — non e' una restrizione di allowlist configurabile, e' un vincolo architetturale della sandbox. Di conseguenza `MatchRepository`/`FilterMarketService.build_dataset` non possono recuperare righe reali da questa sessione. I fix di training sono stati prima validati con dataset sintetici in-memory (monkeypatch di `FilterMarketService.build_dataset`/`build_totals_evaluation_frame`), poi il blocco e' stato risolto con l'approccio "export dataset" descritto nella voce successiva (2026-09-08).

- **Training reale Under/Over 1.5/2.5/3.5/4.5 (2026-09-08, NEW) — risolto il blocco ambiente sopra**: due tentativi di eseguire il training pesante direttamente su una sessione bridge Claude Code Remote collegata alla macchina locale dell'operatore (l'unico ambiente con accesso di rete reale al DB) sono falliti per disconnessioni ripetute della sessione (macchina/rete dell'operatore, non un bug applicativo - una volta con perdita dell'intero ambiente bridge e del lavoro diagnostico in corso, non recuperabile). Soluzione adottata: **spostare solo l'estrazione dati (breve, ~1 minuto) sulla macchina locale, e il training pesante (lungo, decine di minuti) nella sessione cloud**, trasferendo un'istantanea del dataset via git invece di tenere aperta una connessione DB per tutta la durata del training. Nuovo script `scripts/analysis/export_datasets_for_cloud_training.py` (eseguito con accesso DB reale): esporta le feature GIA' ELABORATE (non i JSON grezzi di odds/statistics/mean_statistics, troppo pesanti - centinaia di MB stimati) per le 4 soglie (`FilterMarketService.build_dataset`) + il frame 'totals' (`build_totals_evaluation_frame`) in CSV compatti (~52MB totali, `scripts/analysis/_export/`, deliberatamente NON gitignored per poter essere trasferiti via commit/push). Nuovi script `scripts/analysis/train_from_export.py`/`phase3b_from_export.py`: eseguono la logica di PRODUZIONE INVARIATA (`train_market()`/`run_sequential_cascade()`) sostituendo SOLO la sorgente dati (i CSV invece delle query DB dirette, via monkeypatch di `FilterMarketService.build_dataset` per `train_market` - `run_sequential_cascade` accetta gia' frame/feature_columns come parametri diretti, nessun monkeypatch necessario li').

  Risultati (dataset reale, stesso DB Railway usato per il benchmark Fase 1+2): **train_market per soglia** (griglia RandomForest leggera + fix `StackingClassifier`, entrambi sopra) - under_over_1_5: champion `random_forest`, selection_score 0.7131 (8349 righe, ~7.7 min); under_over_2_5: champion `voting`, selection_score 0.6727 (15243 righe, ~14 min - supera il riferimento noto `binary_independent` puro 0.6683 della Fase 1+2); under_over_3_5: champion `random_forest`, selection_score 0.6906 (8335 righe, ~7.5 min); under_over_4_5: champion `random_forest`, selection_score 0.7820 (8286 righe, ~7.5 min). Tutti e 4 registrati come `candidate` in `best_models/registry/index.jsonl` (**per la prima volta un modello ML vero, non un calcolo Poisson deterministico, risulta effettivamente salvato/registrato per questi mercati** - obiettivo principale del task raggiunto). Gate di promozione (`ModelRegistry.evaluate_promotion`, dry-run) verificato su tutti e 4 i run: tutte le metriche standard (`log_loss`/`brier`/`ece`/`auc`/`sample_size`) lette correttamente, `allowed=true` per tutti (nessuna production precedente per questi mercati -> prima promozione consentita), nessuna promozione effettivamente eseguita (restano `candidate`, come da criterio di accettazione "l'ultimo training non diventa automaticamente production").

  **Fase 3b (stacking sequenziale a cascata tra soglie) su dati reali**: eseguita con `_assert_cascade_no_future_leakage` (gia' validata anche in negativo su caso sintetico) senza violazioni. Verdetto: **SCARTATO** - delta selection_score cascata vs baseline non-cascata (stesso sottoinsieme di righe, per isolare l'effetto della sola feature) sotto la soglia di adozione (0.01) su tutte le soglie a valle di 1.5: over_2_5 -0.0030, over_3_5 +0.0009, over_4_5 -0.0014. Confermato anche un limite strutturale intrinseco all'idea (non un bug): ogni livello di cascata consuma un fold aggiuntivo di dati per garantire l'anti-leakage, quindi il campione valutabile si riduce ad ogni soglia (n_eval_rows: 7620 -> 6096 -> 4572 -> 3048 su n_oof totale 7620). Risultato completo in `best_models/phase3b_sequential_stacking_thresholds_result.json` (gitignored).

  I modelli addestrati (file `.pkl` + registry `best_models/`, gitignored per policy del repo) sono stati consegnati all'operatore (2 archivi zip, ~36MB totali) per essere posizionati nel `best_models/` della sua macchina locale, dove gira l'app reale che li serve. I CSV di export (`scripts/analysis/_export/`) sono stati rimossi dal repo dopo l'uso: erano un workaround temporaneo per il blocco di rete, non uno strumento di produzione permanente da mantenere versionato.

- **Diagnostica dettagliata + soglie di decisione + SMOTE (2026-09-08, NEW)** — seguito del training reale sopra, tutto richiesto esplicitamente dall'operatore in conversazione:
  - **Metriche di classificazione dettagliate** (`scripts/analysis/evaluate_champions_detailed.py`): le metriche aggregate in registry (log_loss/brier/ece/auc/selection_score) non includono confusion matrix/precision/recall/F1 per classe/curva ROC - ricalcolate via walk-forward OOF (`clone()` del champion.pkl gia' addestrato, stessa garanzia anti-leakage di `temporal_oof_probabilities` gia' in uso). **Scoperta**: a soglia di decisione p>=0.5, 3 modelli su 4 non riconoscono quasi mai la classe minoritaria (recall 0.2-2%) pur con AUC 0.58-0.61 - il modello discrimina ma il cutoff fisso non lo sfrutta. Pubblicato come Artifact ("Under/Over Champions") con confusion matrix/ROC/precision-recall per le 4 soglie (design system dedicato: palette teal/amber Under/Over, tipografia Barlow Semi Condensed/IBM Plex Sans/Mono).
  - **Soglie di decisione ottimali** (`scripts/analysis/find_optimal_thresholds.py`, statistica J di Youden) e **soglie orientate a precisione per betting** (`scripts/analysis/find_betting_thresholds.py`, l'operatore vuole puntare quando il modello dice "Over": conta la precisione, non l'equilibrio Youden) - risultati numerici in `best_models/optimal_thresholds.json`/`betting_thresholds.json`.
  - **Coerenza monotona tra soglie** (Over 3.5 implica Over 2.5 implica Over 1.5): l'operatore ha proposto di usare l'output di un modello come feature per il successivo (idea gia' testata in Fase 3b, scartata) confondendola inizialmente con la garanzia di coerenza logica, che invece e' una proiezione matematica POST-HOC (`enforce_monotonic_over_probabilities`, gia' in `totals_market.py`, MAI applicata ai 4 modelli indipendenti di `train_multi_market.py`). Testate ENTRAMBE le idee assieme su richiesta esplicita ("prova entrambe"): fase 3c (`phase3c_coherence_experiment.py`) confronta cascata vs indipendente, entrambe con proiezione monotona, su un frame condiviso ad-hoc (15243 righe, RandomForest leggero uguale per entrambe) - violazioni pre-proiezione ~13.9% per entrambe le varianti (indipendente dall'approccio), la proiezione migliora leggermente il selection_score in entrambi i casi (nessun costo), cascata vs indipendente resta un pareggio (differenza sotto la soglia di adozione 0.01). Poi la proiezione e' stata applicata anche ai 4 champion REALMENTE registrati (`apply_monotonic_to_champions.py`, allineamento via `id_fixture`): violazioni molto piu' rare sui modelli reali (0.3%, 36/11367 su 3789 fixture comuni) - i champion con grid search completa + calibrazione sono gia' internamente piu' coerenti del RandomForest leggero usato solo per l'esperimento fase 3c.
  - **SMOTE** (`scripts/analysis/test_smote_vs_class_weight.py`): l'operatore ha chiesto se fosse gia' stato provato - risposta: SMOTE esiste come dipendenza (`imbalanced-learn`) ma solo nel codice legacy pre-v2 (`src/service_ia/training/under_over/{inconsistent,consistent}/`, non importato da nulla di attivo), mai testato in questa pipeline (che usa `class_weight="balanced"`). Confronto controllato (stesso RandomForest, stesso walk-forward, SMOTE via `imblearn.pipeline.Pipeline` SOLO sul training fold di ciascun fold): aiuta in proporzione allo sbilanciamento del mercato - miglioramento netto su under_over_4_5 (minoranza 13.5%: selection_score 0.7500->0.7661), marginale su under_over_3_5, nullo/leggermente negativo su under_over_2_5 (quasi bilanciato al 47%/53%). Su richiesta esplicita, integrato come candidato STRUTTURALE `random_forest_smote` in `_model_space` (`train_multi_market.py`, condiviso da TUTTI i mercati, non solo Under/Over - compete sullo stesso `selection_score` gia' in uso, non sostituisce `class_weight="balanced"`). Ri-addestrate le 4 soglie con la pipeline aggiornata: **under_over_3_5 e under_over_4_5 hanno un nuovo champion migliore** (3_5: `random_forest_smote`, selection_score 0.6906->0.7008; 4_5: `voting` su base `random_forest_smote`+`random_forest`, selection_score 0.7820->0.7860), 1_5/2_5 restano sui champion precedenti (SMOTE competeva ma non ha vinto, nessun peggioramento - esattamente il comportamento atteso da un candidato che compete invece di sostituire). Registry, soglie ottimali/betting e Artifact "model diagnostics" tutti rigenerati sui nuovi champion.
  - **Richiesta per il futuro (memorizzata in CURRENT_TASK.md)**: portare la vista "model diagnostics" nel frontend web quando i modelli saranno pronti/promossi, non solo come report una tantum.

- **goal_no_goal addestrato + feature injection su Under/Over (2026-09-08, NEW)** — prima di considerare la promozione degli Under/Over, l'operatore ha chiesto di addestrare anche "goal_no_goal" (GG/NG - gia' supportato da `FilterMarketService.SUPPORTED_MARKETS`/`train_market()`, mai addestrato finora) e testare l'idea di usare la sua probabilita' come feature per gli Under/Over (razionale dell'operatore: GG=Si implica Over 1.5, quindi potrebbe aiutare le altre soglie).
  - **Analisi feature disponibili**: l'operatore ha chiesto esplicitamente cosa venisse usato come feature oltre a odds/mean_statistics ("ci sono anche molte altre features da tenere in considerazione"). Trovato un blocco ricco non sfruttato nello schema (`Statistics.form`/`for_`/`against`/`preview_matches`/`comparison`, popolato da `form_last_5_tot()` in `statistic_mapper.py` via l'endpoint "predictions" di API-Sports - storico Over/Under per soglia HOME/AWAY separati, forma ultime 5, distribuzione gol per fascia di minuto, comparison tra le due squadre). **Trovato pero' un blocco strutturale**: tracciata la pipeline di ingestion (`download_match_service.py`, righe 487-496) - queste colonne vivono DENTRO `Statistics`, che viene creata SOLO quando esistono gia' le statistiche box-score della partita (`if len(statistics) == 0 and status_short != 'NS': ...`); per una fixture futura (`NS`, non ancora giocata) non viene mai creata alcuna riga `Statistics`, quindi questi campi sono strutturalmente NON disponibili al momento della prediction reale. Usarli come feature di training avrebbe creato uno skew training/serving (feature presente in training, sempre mancante/zero in produzione) - peggio che non averla. Deciso con l'operatore di NON integrarle e procedere sulle sole feature gia' sicure per il serving (le stesse 69 usate per gli Under/Over: odds + `mean_statistics`, quest'ultima calcolata come media delle partite PRECEDENTI quindi disponibile anche pre-kickoff).
  - **Training goal_no_goal**: stesso pattern export-via-git gia' in uso (nuovi script `export_goal_no_goal_for_cloud_training.py`/`train_goal_no_goal_from_export.py`, non serviva riesportare gli Under/Over: CSV recuperati dalla history git, commit "export dataset per training cloud"). 4475 righe, target il piu' bilanciato tra tutti i mercati testati (53.4% GG / 46.6% NG). Champion `logistic`, selection_score=0.6678 (SMOTE ha corso come candidato nello stesso `_model_space` condiviso ma non ha vinto, coerente con un mercato gia' bilanciato). Registrato come `candidate`. Diagnostica dettagliata e soglie (Youden/betting) rigenerate includendo anche questo mercato (`evaluate_champions_detailed.py` esteso con un flag `--markets`/`--merge` per poter aggiungere un mercato senza ricalcolare gli altri 4). Soglia ottimale Youden praticamente = 0.5 (0.502) - a differenza degli Under/Over, qui il cutoff di default non era degenere.
  - **Fase 4 - feature injection** (`scripts/analysis/phase4_goal_no_goal_feature_injection.py`, riusa `_cascade_oof`/`_assert_cascade_no_future_leakage` di Fase 3b applicati a una singola feature sorgente iniettata indipendentemente in ciascuna delle 4 soglie, non una cascata soglia-su-soglia): OOF di goal_no_goal (walk-forward, RandomForest, stessa CV di produzione) unita per `id_fixture` al frame 'totals' condiviso. **Risultato: SCARTATO** - delta selection_score vs baseline sullo stesso sottoinsieme di righe (n_eval_rows=1524): over_1_5 +0.0022, over_2_5 +0.0089, over_3_5 -0.0009, over_4_5 +0.0053, tutti sotto la soglia di adozione (0.01) - stesso esito della feature deterministica `gd_over_1_5` gia' scartata in Fase 3 (+0.0008). Interpretazione: goal_no_goal usa lo stesso spazio di feature (odds + mean_statistics) degli Under/Over, quindi la sua OOF e' in gran parte una trasformazione ridondante di informazione gia' disponibile al modello, non un segnale nuovo - vale anche per over_1_5, dove l'implicazione GG->Over1.5 e' deterministica sul risultato REALE ma non sulla stima probabilistica (incerta) del modello. Risultato completo in `best_models/phase4_goal_no_goal_feature_injection_result.json` (gitignored). Nessun impatto sui 4 champion Under/Over gia' registrati.

- **Promozione a production + segnale "Bet Over" (2026-09-08, NEW)** — chiusura del thread di lavoro sopra, richiesta esplicitamente dall'operatore dopo aver rivisto diagnostica/soglie ("buona idea implementare uno switch...allora si, portiamo in produzione questi modelli over under"):
  - **Promozione**: `ModelRegistry.promote_with_policy(to_stage="production")` eseguito sui 4 run_id piu' recenti (post-SMOTE) per under_over_1_5/2_5/3_5/4_5 - gate metriche verificato per tutti (log_loss/brier/ece/auc/sample_size entro soglia), nessuna production precedente per questi mercati quindi comparison bootstrap consentito (`allow_promotion_without_production_baseline=True`), nessun bypass/force usato. **Prima production mai esistita per questi mercati** - stesso significato di "primo modello ML vero salvato" del training iniziale, ma un gradino oltre. Audit trail completo in `best_models/registry/promotion_history.jsonl` (consegnato all'operatore per la sua macchina locale, insieme a `index.jsonl` per sicurezza).
  - **Chiarimento richiesto dall'operatore prima della promozione**: confusione tra "soglia Youden" e "soglia betting" interpretate come DUE MODELLI diversi da scegliere per la promozione - chiarito che sono la stessa identica probabilita' del modello, tagliata in due punti diversi della curva; la promozione riguarda il modello (univoco per mercato), la soglia e' una scelta successiva a livello di serving.
  - **Switch "Bet Over"**: prima di implementarlo, scoperto che l'app ha GIA' due livelli di soglia distinti e indipendenti dal nuovo switch - (1) `_extract_probability` in `dashboard_service.py` (soglia fissa 0.5, decide il pick "Over X"/"Under X" mostrato) e (2) `DecisionPolicy`/BET-04 (`play_min_probability=0.62` uguale per ogni mercato/outcome, decide PLAY/BORDERLINE/NO_BET per il betslip, gia' con supporto nativo per override `per_market_outcome` mai sfruttato finora). Chiesto esplicitamente all'operatore come deve comportarsi il nuovo segnale quando P(Over) e' sotto 0.5 ma sopra la soglia di precisione (es. under_over_4_5: soglia 0.1776) - scelta: **segnale indipendente**, non altera ne' il pick ne' la DecisionPolicy esistenti, si affianca come nuovo campo. Nuovo modulo `src/oracle/decision_engine/over_signal_policy.py` (versionato `over_signal_policy_v1`, stesso principio di `DecisionPolicy`): soglia di probabilita' per mercato derivata da `betting_thresholds.json` al recall floor 30% (precisione attesa: 1.5=83.8%, 2.5=65.3%, 3.5=40.0%, 4.5=21.6%). Wired in `dashboard_service.py._build_decision_cards`: nuovo campo `bet_over_signal` (dict con signal/threshold/precision/recall attesi, `None` per mercati non under/over) aggiunto alle `decision_cards`, nessuna riga di codice esistente modificata oltre l'aggiunta. Verificato che i consumer esistenti (`pick_pool_service.py`) leggono campi per nome (`.get()`), nessun breaking change. Nuovo test `tests/service/over_signal_policy_test.py`. Suite completa: 727 passed (era 712, +7 SMOTE/goal_no_goal/over_signal + fix minori).
  - **Non ancora fatto** (nessuna richiesta esplicita finora): collegare la proiezione monotona (`enforce_monotonic_over_probabilities`) al path di serving reale - i 4 modelli sono `production` ma le predizioni servite non passano ancora da quella proiezione, restando solo un'analisi offline (fase 3c/apply_monotonic_to_champions.py); frontend non ancora aggiornato per mostrare il nuovo `bet_over_signal` (solo campo API per ora).

- **Promozione a production di `goal_no_goal` (5° mercato) + proiezione monotona collegata al serving reale (2026-09-09, NEW)** — richiesto esplicitamente dall'operatore ("portiamo in produzione i 5 modelli"): `goal_no_goal` promosso a `production` con lo stesso processo gia' usato per i 4 Under/Over (`ModelRegistry.promote_with_policy`, gate verificato, nessun bypass). Collegata al serving reale anche la proiezione di coerenza monotona tra le 4 soglie Under/Over (`enforce_monotonic_over_probabilities`, gia' validata offline in fase 3c/`apply_monotonic_to_champions.py`, mai applicata prima d'ora in `dashboard_service.py`): nuovo `DashboardService._apply_monotonic_projection(payload)`, chiamato in coda a `_predict_fixture` prima della cache, applicata SOLO quando tutte e 4 le predizioni Under/Over della stessa fixture sono disponibili — aggiorna sia `probability` sia `prediction` ricalcolata, eliminando incoerenze tipo "Over 3.5: si" + "Over 2.5: no" nella stessa card. Nessun impatto su `bet_over_signal`/`DecisionPolicy`, che leggono il payload gia' proiettato a valle. 5 nuovi test (`TestApplyMonotonicProjection`), suite completa 734 passed (era 727).

- **Parallelismo fetch multi-lega + finestra oraria "oggi" per ridurre il consumo di quota API-Sports dalla Dashboard (2026-09-09, NEW)** — diagnosticato su segnalazione esplicita dell'operatore ("c'è un richiamo sempre del provider esterno quando finisco nella dash iniziale... soprattutto per la giornata odierna... quando non capita è lentissimo delle return"). Causa radice: il polling automatico frontend ogni 60s (`App.jsx`, gia' esistente) combinato con la cache backend ANCH'ESSA a TTL 60s (`DashboardService._api_cache_ttl_seconds`) produceva chiamate quasi ininterrotte ad API-Sports per il giorno corrente semplicemente lasciando la Dashboard aperta in una scheda — non solo "al reload manuale" come inizialmente ipotizzato da entrambi. Due fix, scope esplicitamente limitato al SOLO layer Dashboard (mai ai job schedulati in `src/jobs/`, che restano invariati) dopo conferma esplicita dell'operatore su entrambe le scelte (parallelismo + finestra oraria, con relativa spiegazione delle differenze tra le opzioni prima di procedere):
  - **Parallelismo**: `_fetch_api_day_fixtures` (`dashboard_service.py`) rifaceva il fetch per-lega in un loop Python sequenziale (fino a N leghe configurate, una richiesta HTTP alla volta) — sostituito con `ThreadPoolExecutor(max_workers=min(6, len(leagues)))` + `executor.map` (ordine dei risultati preservato), stessa quantita' di chiamate HTTP ma non piu' in serie: riduce direttamente la latenza percepita nelle "return lentissime" segnalate.
  - **Finestra oraria 12:30-00:30 (Europe/Rome)**: nuove costanti `_DASHBOARD_API_WINDOW_START=time(12,30)`/`_DASHBOARD_API_WINDOW_END=time(0,30)` (finestra che attraversa la mezzanotte, quindi confrontata con `now>=start OR now<end`, non un semplice range) + `_is_within_dashboard_api_window()`. Fuori da questa finestra, `_fetch_api_day_fixtures` (guardia scoped a `target_date==oggi`, bypassabile con `force_refresh` per i rari casi in cui serve comunque un refresh manuale) e `_fetch_api_live_fixtures` (nessun bypass, nessun bottone forza un fetch live fuori orario) saltano del tutto la chiamata esterna, risparmiando quota nella fascia in cui tipicamente non ci sono partite europee in corso. Fuso orario: `zoneinfo.ZoneInfo("Europe/Rome")` usato ESCLUSIVAMENTE per questa decisione di orario locale (stessa richiesta esplicita dell'operatore "voglio l'orario italiano proiettato" — poi ristretta via chiarimento esplicito al SOLO scope Dashboard) — i timestamp interni dell'applicazione restano SEMPRE UTC (`datetime.now(timezone.utc)`, invariato ovunque nel resto del codice), stesso principio gia' seguito da `BlockingScheduler(timezone="Europe/Rome")` in `src/jobs/scheduler.py` (che gia' usava un fuso solo per il proprio trigger, mai per i timestamp salvati). Nuovi test: 2 per il parallelismo (conteggio chiamate + ordine risultati preservato), `TestDashboardApiWindow` (9 casi: confini esatti 12:30/00:30, notte profonda, pomeriggio, bypass `force_refresh`, nessun bypass per `_fetch_api_live_fixtures`, date diverse da oggi mai bloccate) — piu' un fix ad un test pre-esistente (`test_fetch_api_live_fixtures_still_calls_when_quota_not_exhausted`) che dipendeva dall'ora reale di esecuzione, risolto mockando esplicitamente `_is_within_dashboard_api_window`.

- **Orario/intervallo dei job automatici editabile da Impostazioni, senza restart (2026-09-09, NEW)** — richiesto esplicitamente dall'operatore ("mettere l'orario editabile così che io possa avere maggiori controllo, è fattibile?"), con scelta esplicita tra due opzioni proposte (effetto immediato via self-gating vs piu' semplice ma con restart del container `scheduler` necessario) - scelta l'opzione ad effetto immediato.
  - **Redesign scheduler** (`src/jobs/scheduler.py`): i 7 job registrati NON hanno piu' un trigger APScheduler calcolato una volta da `cfg.*` all'avvio (vecchi `CronTrigger`/`IntervalTrigger` per-job) — condividono ora un unico heartbeat (`IntervalTrigger(seconds=30)`, `_HEARTBEAT_SECONDS`) e si auto-valutano ad ogni tick tramite il nuovo `_is_job_due(job_id, cfg)`: confronta lo schedule EFFETTIVO corrente (`job_settings.resolve_job_schedule`, override salvato se presente altrimenti default `AppConfig`) con l'ultima esecuzione TERMINATA nello storico gia' esistente (`JobHistory`/`jobs_history.jsonl` — riusato cosi' com'e', nessun nuovo tracking parallelo introdotto). Per i job "daily" (`data_daily_refresh`/`data_future_sync`/`ml_training`): dovuto se l'ora locale Europe/Rome ha superato `hour:minute` di OGGI e l'ultima esecuzione risale a un giorno locale precedente (mai due volte lo stesso giorno anche con molti tick di heartbeat). Per gli "interval" (`data_sync_today`/`data_settlement`/`data_quality_report`/`data_sync_live`): dovuto se e' trascorso l'intervallo configurato dall'ultima esecuzione (confronto in UTC). Nuovo `_run_if_due` sostituisce `_run_if_enabled`, combinando enabled (gia' esistente, invariato) + due (nuovo) — entrambi ricontrollati ad OGNI tick, non solo alla registrazione, quindi un cambio di orario da Impostazioni ha effetto entro 30s, mai un restart.
  - **Estensione `job_settings.py`**: ogni `JOB_DEFINITIONS[job_id]` dichiara ora `schedule_kind` ("daily"/"interval_minutes"/"interval_seconds") e `cfg_fields` (mappa parametro-schedule -> attributo `AppConfig`, il default quando non c'e' un override). Nuovo file di stato dedicato `best_models/job_schedule.json` (stessa scrittura atomica file-temp+`os.replace` di `job_settings.json`/`quota_pause_state.json`/`api_quota_state.json`), con `get_job_schedule_overrides`/`update_job_schedule`/`reset_job_schedule`/`resolve_job_schedule`; validazione (`_validate_schedule`) rifiuta chiavi diverse da quelle attese per lo `schedule_kind` del job e valori fuori range (`_SCHEDULE_FIELD_BOUNDS`: ore 0-23, minuti 0-59, intervallo minuti 5-1440, intervallo secondi 30-3600 — mai un valore abbastanza aggressivo da rischiare di bombardare API-Sports/il DB). `list_job_definitions` esteso con `schedule_kind`/`schedule`/`schedule_is_default` per il frontend.
  - **API** (`src/api/main.py`/`schemas.py`): `JobSettingRow` esteso con gli stessi 3 campi; nuovi `PUT /settings/jobs/{job_id}/schedule` (valida e salva l'override — 400 su chiavi/valori non validi, 404 su job sconosciuto) e `DELETE /settings/jobs/{job_id}/schedule` (torna al default `AppConfig`) — verificati end-to-end via `TestClient` con path JSON isolati (nessun file reale del repo toccato durante il test).
  - **Frontend** (`SettingsPage.jsx`): nuovo componente `JobScheduleEditor` per riga job — `<input type="time">` per i job "daily" (salvataggio al blur), input numerico per gli "interval_minutes"/"interval_seconds", bottone "ripristina default" mostrato solo quando esiste un override attivo. Nuove `api.js::updateJobSchedule`/`resetJobSchedule` + stato/handler dedicati in `App.jsx` (`jobScheduleSavingId`/`saveJobSchedule`/`resetJobScheduleToDefault`, stesso pattern optimistic-poi-riallineato gia' usato per enabled/disabled). Build frontend verificata senza errori.
  - **Test**: `tests/service/scheduler_test.py` riscritto per intero (i vecchi test verificavano trigger per-job calcolati da `cfg`, non piu' veri sotto il nuovo design a heartbeat condiviso) — nuove `TestAllJobsShareHeartbeatTrigger`/`TestIsJobDue` (7 combinazioni schedule_kind x stato storico, `datetime.now()` reso deterministico con una sottoclasse fissa per evitare test time-dependent)/`TestRunIfDue`/`TestLastCompletedRun`/`TestParseHistoryTimestamp`, oltre alle classi invariate su target/indipendenza dei 7 job. Nuovo `tests/service/job_settings_test.py` (25 test, path isolati via monkeypatch di `_schedule_path`) per validazione/override/reset/resolve/list_job_definitions. Suite completa rieseguita: **780 passed** (era 734 prima di queste tre voci di questa stessa giornata).

- **Banca dati predizioni persistita + fix "sempre lentissimo" (2026-09-09, NEW)** — richiesto esplicitamente dall'operatore dopo una diagnosi congiunta di performance ("il sistema e' sempre lentissimo... mi spieghi cosa succede quando cambio la data?"): trovato che `DashboardService._model_cache` (i modelli `.pkl` caricati via `joblib.load`) era un attributo PER-ISTANZA, e ogni endpoint Dashboard in `main.py` crea una `DashboardService()` NUOVA - quindi tutti i modelli di produzione (5 mercati, alcuni decine/centinaia di MB) venivano ricaricati da disco ad OGNI singola richiesta, indipendentemente dalla data o dallo stato della quota API-Sports (gia' bloccata al 100% nello screenshot dell'operatore, escludendo il provider esterno come causa). Proposta dell'operatore, piu' ampia del semplice "sposta la cache a classe": persistere le predizioni su DB, non ricalcolare le partite storiche, ricalcolare quelle future/odierne SOLO se una feature cambia, e usare questa tabella anche come "banca dati da accumulare" per analisi future.
  - **Nuova tabella APPEND-ONLY `match_prediction_snapshot`** (`src/service_ia/model/match.py`, migration `7b1e0c85a98d` — revises `a7d92c4f18e6`, MAI ancora eseguita da questa sessione cloud per lo stesso blocco di rete al Postgres Railway gia' documentato 2026-09-07): `fixture_id`/`market`/`prediction`/`probability`/`model_name`/`model_run_id`/`feature_fingerprint`/`computed_at`, indice su (fixture_id, market, computed_at). Distinta deliberatamente da `prediction_ledger` (BET-06): quella logga SOLO le decisioni di scommessa loggate esplicitamente (stake/EV/settlement), questa logga OGNI predizione GREZZA calcolata per OGNI mercato — doppio uso, cache di serving (si legge l'ultima riga) E storico di ricerca (come si e' mossa la stima del modello prima del calcio d'inizio, richiesto esplicitamente dall'operatore).
  - **Nuovo `src/ml/serving/prediction_snapshot_service.py`** (`PredictionSnapshotService`, nuovo package `src/ml/serving/`): `resolve_predictions(fixture_id, markets, db_match=None, status=None)` con due regimi decisi dallo status:
    - **Partita CONCLUSA** (`FT`/`AET`/`PEN`/`ABD`/`CANC`/`PST`/`WO`, stesso insieme di `dashboard_service.FINAL_STATUSES` — duplicato localmente per non introdurre una dipendenza a ritroso di `src/ml/serving` verso `src/api`, dato che questo package e' usato ANCHE dai job in `src/jobs/`): se esiste gia' una riga salvata, viene servita DIRETTAMENTE — zero query di frame, zero caricamento modello. Se manca (es. un mercato promosso dopo la fine di quella partita), viene calcolata UNA VOLTA e **CONGELATA per sempre** — scelta esplicita dell'operatore ("le predizioni delle partite finite restano quelle di allora"): un retrain futuro NON tocca mai le righe storiche gia' salvate.
    - **Partita NON conclusa** (NS/live): il frame di feature (odds + mean_statistics, la stessa infrastruttura di `FilterMarketService.build_prediction_frames*`, invariata) viene comunque costruito (economico: query + calcolo pandas, nessun modello coinvolto) e se ne calcola un `feature_fingerprint` deterministico — nuova funzione pura `compute_feature_fingerprint(X)`: hash SHA-256 dei valori (arrotondati a 6 decimali, convertiti esplicitamente a `float` Python per evitare differenze di `repr()` tra versioni di numpy) ordinati per nome colonna. Generico per costruzione (richiesta esplicita dell'operatore, "per le partite di oggi o future, solo se cambia una delle feature" — NON solo le quote come nella proposta iniziale): qualunque feature cambi (odds, mean_statistics, o fonti future aggiunte in seguito) invalida la cache, senza bisogno di enumerare colonne specifiche. La riga salvata viene riusata SOLO se fingerprint E `model_run_id` sono IDENTICI all'ultimo calcolo; altrimenti si ricarica il modello, si ricalcola e si APPENDE una riga nuova (mai un overwrite — coerente con l'append-only "banca dati").
    - `_model_cache` e' ora a LIVELLO DI CLASSE (`PredictionSnapshotService._model_cache`, sopravvive tra le istanze create ad ogni richiesta — stesso principio gia' applicato a `DashboardService._api_cache`): FIX DIRETTO del bug "sempre lentissimo", un processo (API o scheduler) carica ciascun modello da disco UNA SOLA VOLTA per tutta la sua vita, non ad ogni richiesta. `clear_model_cache()` esposto per isolamento tra test.
  - **`DashboardService._predict_fixture` refactored**: non carica piu' modelli/frame da solo — delega interamente a `PredictionSnapshotService.resolve_predictions` (nuovo attributo `self._snapshot_service`), passando anche `status` (nuovo parametro, threaded attraverso tutti e 3 i call site: `_serialize_api_fixture`, `_serialize_match`, `get_match_detail`), poi applica la proiezione di coerenza monotona SEMPRE sul payload grezzo risolto — deliberatamente MAI persistita nello snapshot: e' una combinazione tra 4 mercati diversi, non una proprieta' di un singolo modello, va ricalcolata ad ogni lettura (costo trascurabile, un'operazione numpy in memoria) mantenendo la cache DB semplice "un mercato alla volta". Rimossi `_load_model`/`_latest_model_for_market`/`_model_meta_cache`/`_model_cache`/`_extract_probability`/`self.filter_service` da `DashboardService` (nessun consumatore esterno verificato via grep, tutta la logica traslocata in `PredictionSnapshotService`); rimosso anche l'import `joblib`, non piu' usato in questo file.
  - **Nuovo job schedulato `prediction_snapshot_refresh`** (`run_prediction_snapshot_refresh` in `scheduler.py`, 8° job — riusa DIRETTAMENTE l'infrastruttura heartbeat/self-gating appena costruita nella voce precedente di questa stessa giornata, nessuna duplicazione): interroga le fixture `status='NS'` nella finestra oggi -> oggi+`days_ahead` giorni (default `cfg.daily_refresh_days_ahead`, la STESSA finestra gia' sincronizzata da `data_daily_refresh`/`data_future_sync`) e chiama `PredictionSnapshotService.resolve_predictions` per ciascuna — un no-op economico (query + hash, nessuna inferenza) per la stragrande maggioranza, dato che la fingerprint di solito non e' cambiata dall'ultimo giro. Popola la banca dati PRIMA che un utente apra la Dashboard, cosi' il percorso di serving resta una PURA lettura da DB. Un fallimento su una singola fixture finisce in `errors` senza bloccare le altre (stesso principio "provider errors isolati" di LIVE-01). Nuovo `AppConfig.prediction_snapshot_interval_minutes` (default 30, env `PREDICTION_SNAPSHOT_INTERVAL_MINUTES`) + voce `JOB_DEFINITIONS["prediction_snapshot_refresh"]` (`job_settings.py`, `calls_api_sports=False`) — editabile da Impostazioni esattamente come gli altri 7 job (riuso diretto della feature appena consegnata).
  - **Nuova repository** `src/repository/match_prediction_snapshot_repository.py` (`MatchPredictionSnapshotRepository`): stesso pattern "sessione per operazione" di `OddsSnapshotRepository`/`PredictionLedgerRepository` — `save`/`get_latest`/`get_latest_bulk` (una query sola per N fixture, per il job di refresh)/`list_for_fixture` (storico completo, per un futuro consumo della "banca dati").
  - **Test**: 30 nuovi — `match_prediction_snapshot_repository_test.py` (7, CRUD + storico + bulk), `prediction_snapshot_service_test.py` (15: fingerprint deterministico/sensibile/insensibile a rumore float/indipendente da ordine colonne, i tre regimi conclusa-con-e-senza-snapshot/congelamento-anche-con-modello-cambiato/non-conclusa-riusa-o-ricalcola-su-feature-o-modello-cambiati, cache di classe condivisa tra istanze verificata mockando SOLO `joblib.load` — non l'intero `_load_model`, per esercitare davvero la logica di cache-hit), `prediction_snapshot_refresh_job_test.py` (4: finestra NS/data, isolamento errori per-fixture, nessuna fixture, default `days_ahead` da cfg). Aggiornati `dashboard_service_test.py` (le due vecchie unit test su "query condivisa vs dedicata" — logica ora traslocata in `PredictionSnapshotService` — sostituite da due nuove sul contratto di delega + cache per-richiesta; tutte le altre lambda che sostituivano `_predict_fixture` estese con `status=None`) e `scheduler_test.py` (8° job: conteggio, target function, `_cfg()` con il nuovo campo `AppConfig`). Suite completa in esecuzione al momento di questa voce (attesa: 780 + 30 = 810 passed).

**Nota per il deploy**: la migration `7b1e0c85a98d` (`match_prediction_snapshot`) va applicata (`alembic upgrade head`) PRIMA del rebuild dei container — non eseguibile da questa sessione cloud (nessun accesso di rete diretto al Postgres Railway).

- **Vista storica Dashboard sempre veloce + copertura completa della banca dati predizioni (2026-09-10, NEW)** — richiesto esplicitamente dall'operatore dopo aver segnalato che il cambio data restava lento ANCHE al secondo giro sulla stessa data storica (non solo la prima volta): la banca dati `match_prediction_snapshot` (introdotta il giorno prima) copriva solo le fixture viste in Dashboard o intercettate dal job mentre erano ancora `NS`, mai lo storico gia' passato ne' le partite finite fuori da quella finestra — quindi la prima apertura di una data storica MAI vista prima ricalcolava comunque tutto al volo. Piano a 4 punti documentato in `docs/soccer_oracle_v2_detailed/PROMPT_fast_historical_predictions.md` (aggiornato dopo ogni punto, su richiesta esplicita dell'operatore), tutti e 4 completati dal lato codice nella stessa giornata:
  1. **Niente ricalcolo al volo per le date storiche** (commit `ad50648`): nuovo `allow_compute: bool = True` su `PredictionSnapshotService.resolve_predictions` — quando `False`, un mercato senza riga gia' salvata viene semplicemente saltato (nessun frame costruito, nessun modello caricato) invece di calcolare+salvare. Threaded fino a `DashboardService.get_day_matches`, attivo SOLO quando `is_historical_date` (mai per oggi/future, mai per `get_match_detail`/`get_live_matches` — un click deliberato su una singola fixture resta sempre a calcolo pieno). `cache_key` di `_predict_fixture` esteso con `allow_compute` per non mischiare le due modalita'. Frontend: `PredictionBadges.jsx` mostra ora un chip placeholder "In coda" (nuova classe CSS `.prediction-pending`) per ogni mercato registrato (`model_markets`, gia' nella risposta di `/dashboard/day`) assente dal payload — propagato `DashboardPage` -> `MatchTable` -> `PredictionBadges`.
  2. **Job esteso alle partite appena concluse** (commit `caf12bb`): `run_prediction_snapshot_refresh` (`src/jobs/scheduler.py`) ora interroga ANCHE le fixture con status finale concluse negli ultimi 3 giorni (`_RECENTLY_FINISHED_WINDOW_DAYS`, nuovo parametro opzionale `recently_finished_days`) prive di una riga salvata — non piu' solo le `NS` in finestra futura. Anti-join preventivo via `MatchPredictionSnapshotRepository.get_latest_bulk` (query bulk sull'intero batch) per scartare le fixture gia' completamente coperte, prima di chiamare `resolve_predictions` (che farebbe comunque una query di verifica per mercato). Lo storico OLTRE questa piccola finestra resta deliberatamente scoperto da questo job — e' lo scope del punto 3.
  3. **Script di backfill storico una tantum** (commit `d72b4f5`): nuovo `scripts/backfill_prediction_snapshots.py` — itera TUTTE le fixture concluse via keyset pagination su `id_fixture` (mai `OFFSET`, costo costante ad ogni pagina anche su tabelle di decine/centinaia di migliaia di righe), stesso anti-join del job schedulato, chiama `resolve_predictions(allow_compute=True)` solo per le fixture che ne hanno davvero bisogno. Idempotente, interrompibile (Ctrl+C) e riprendibile senza duplicare righe (regime "congelato" per le finali). Lanciato una volta contro il DB reale (sessione bridge locale), interrotto volontariamente dall'operatore a meta' e **deciso di NON completarlo**: chiarimento esplicito, "il progetto e' nuovo... tutte le partite storiche dovevano servire solo per addestrare... da oggi in poi mi interessano che le partite vengano salvate con predizioni" — lo storico profondo serve al training, che legge `match`/`statistics`/`odds` DIRETTAMENTE, mai `match_prediction_snapshot` (zero impatto). L'esigenza reale ("qualche giorno indietro + da oggi in poi") e' interamente coperta, per sempre e in automatico, dal job del punto 2. Lo script resta comunque disponibile per un eventuale uso futuro one-off.
  4. **Bottone "Ricalcola previsione" nel dettaglio partita** (commit `5ceba8b`): nuovo parametro `force: bool = False` su `resolve_predictions` — quando `True`, ignora QUALUNQUE riga esistente (anche una partita conclusa "congelata") e ricalcola+salva sempre una riga nuova; ha priorita' su `allow_compute`. Nuovo `DashboardService.recompute_predictions` + endpoint `POST /dashboard/matches/{fixture_id}/recompute-predictions` (schemi `RecomputePredictionsRequest`/`RecomputePredictionsResponse` in `schemas.py`) + bottone dedicato nel pannello di dettaglio partita (`MatchDetailPanel.jsx`, stato di caricamento/errore propri, ricarica il dettaglio dopo il ricalcolo). Uso ESPLICITO e manuale, mai automatico.
  - **Test**: 34 nuovi/estesi in totale tra i 4 punti (`prediction_snapshot_service_test.py`, `dashboard_service_test.py`, `prediction_snapshot_refresh_job_test.py`, nuovo `backfill_prediction_snapshots_test.py`). Suite completa verificata dopo ogni punto: **829 passed** a fine lavoro (era 813 prima di questa giornata). Build frontend verificata senza errori dopo i punti 1 e 4.
  - **Deploy (2026-09-10)**: nessuna nuova migration (nessuna modifica di schema, solo nuovi parametri/endpoint/job logic). Mergiato `feature/soccer-oracle-v2` in `main` (merge commit `06b1405`, merge pulito senza conflitti — `main` e `feature/soccer-oracle-v2` condividono ormai una storia comune dal merge del 2026-09-09, niente piu' bisogno della tecnica `-s ours --allow-unrelated-histories` usata in precedenza) — deploy Railway innescato dal push su `main`. Nessun passo operativo residuo: il job del punto 2 e' ora attivo in produzione.

## Migrazioni applicate (locale + docker)
- locale: `alembic stamp 55bbb5f0a367` + `alembic upgrade head` (ora include anche `f3a9c1d8e2b7`, vedi sotto)
- docker: nessuna piu' necessaria, l'API Docker riusa lo schema del DB locale (nessun Postgres containerizzato)

- **Schedine ufficiali e metriche decisionali complete (2026-09-10)**:
  esteso il generatore esistente senza introdurre un secondo motore.
  `CandidatePick` trasporta le metriche già prodotte dalla Decision Policy;
  `GeneratedSlip` espone quota combinata, probabilità ingenua e corretta,
  quota void combinata, edge, expected ROI, rischio, situazione e versioni.
  I profili mantengono le soglie tecniche precedenti ma sono versionati
  `*_v2_play_only`: solo selezioni `PLAY`, una fixture per schedina di
  default. Aggiunte le tabelle additive `betting_slips` e
  `betting_slip_picks` (migration `8f7d3c2a1b09`) con capture key
  deterministica, snapshot pre-partita, settlement `PENDING/WON/LOST/VOID`,
  quota effettiva con VOID a fattore 1, ritorno e PnL. Il job ufficiale
  cattura le schedine dopo le singole PLAY; il job settlement aggiorna
  anche le schedine. Nuove API GET read-only per lista e statistiche
  ufficiali. La pagina Schedina mostra profili Prudente/Bilanciata/Spinta,
  metriche aggregate e tutte le metriche di ogni selezione, distinguendo
  quota void modello da esito VOID.

- **Ottimizzazione end-to-end Dashboard (2026-09-11)**:
  eliminato il collo di bottiglia N×M degli snapshot con
  `MatchPredictionSnapshotRepository.get_latest_bulk`, che usa una window
  function per leggere soltanto l'ultima riga di ogni coppia
  fixture/mercato. `DashboardService.get_day_matches` carica quella mappa
  una volta e la riusa per tutte le righe senza consentire inferenza inline.
  La vista è DB-first per passato, oggi e futuro; API-Sports viene consultata
  soltanto da refresh esplicito o dai job dedicati.

  Aggiunto `GET /dashboard/bundle`: overview, live e day derivano dallo
  stesso dataset anziché eseguire fino a quattro letture del giorno. La
  query ORM usa il giorno esatto, limita le colonne di `statistics` e non
  carica `odds_snapshots`. Le fetch API concorrenti con la stessa cache key
  sono serializzate in single-flight e il recupero multi-lega è parallelo.
  Il frontend usa una sola richiesta bundle, filtra fase/mercato in memoria,
  protegge il mount iniziale duplicato e sospende il polling quando la
  Dashboard non è attiva.

  Migration additiva `c3a7e2f91d44_dashboard_query_indexes.py`: indici su
  `statistics.id_match`, `odds.id_match`,
  `prediction_ledger(cohort, fixture_id)` e
  `prediction_ledger.created_at`. Nessun dato o contratto API precedente è
  stato rimosso. Misura comparativa sullo stesso carico (7 fixture × 7
  mercati): da 54 query SQL totali, incluse 49 query snapshot, a 5 query
  totali con una sola query snapshot bulk. Sul database SQLite isolato il
  percorso consolidato ha impiegato 0,0126 s; il precedente rilievo sul
  percorso remoto era 6,438 s, quindi i tempi assoluti non sono direttamente
  confrontabili ma il numero di round-trip sì. Validazione: 111 test mirati
  Dashboard/snapshot/frontend passati; suite completa 904 test passati;
  build Vite riuscita; migration verificata con ciclo
  upgrade/downgrade/upgrade su database isolato.

- **Persistenza proposte e statistiche betting unificate (2026-09-11)**:
  introdotta la migration additiva
  `d9b4f6a21c73_betslip_proposal_snapshots.py`. Ogni proposta generata viene
  salvata come snapshot append-only con chiave idempotente, lineage logica,
  indicatore di revisione corrente, metriche combinate, versioni e payload
  completo delle selezioni. Quote o metriche immutate non producono righe
  duplicate; una variazione reale crea una nuova revisione senza modificare
  la precedente.

  Aggiunti `POST /betslip/generate/snapshot` per l'azione manuale esplicita
  e `GET /betting/statistics` per la vista aggregata. Il job
  `prediction_snapshot_refresh` salva automaticamente le proposte delle
  giornate future dopo aver aggiornato le prediction. La GET storica
  `/betslip/generate` resta read-only.

  La Schedina Oracle espone ora le viste Panoramica, Mercati e Schedine:
  tabella giornaliera per mercato, attività generata e performance
  giornaliera delle schedine ufficiali. Proposte e performance ufficiale
  sono deliberatamente separate: ROI, profitto e bankroll continuano a
  derivare soltanto dalle schedine ufficiali congelate pre-kickoff.

  Correzione anti-hindsight: `POST /betslip/generate/snapshot` rifiuta date
  passate e, per la giornata corrente, salva solo schedine con tutti i
  kickoff futuri e validi. `GET /betslip/proposals` permette di consultare
  gli snapshot storici senza rigenerarli; il frontend passa automaticamente
  alla modalità “Solo consultazione” quando viene selezionata una data
  precedente a oggi.

## Connessione DB runtime
- **AGGIORNATO 2026-09-04**: sorgente runtime ora fissata sul DB dev remoto Railway: `DATABASE_URL=postgresql://postgres:...@sakura.proxy.rlwy.net:18862/railway` (credenziali complete in `properties/config.env`), unica per locale/Docker/Alembic - vedi entry INFRA sopra. Il valore storico sotto (`localhost:5432/match_db`) e la narrazione del fix Docker restano come riferimento della situazione PRECEDENTE al cambio Railway.
- vecchio runtime locale (fino al 2026-09-03): `DATABASE_URL=postgresql://postgres:postgres@localhost:5432/match_db`
- verifica rapida: tabella `match` letta con volume storico (>47k righe)

**RISOLTO (2026-09-03):** rimosso il servizio Postgres containerizzato (`soccer_db`, immagine `postgres:16-alpine`) da `docker-compose.yml`. Causa del problema precedentemente annotato in questa sezione: sulla macchina di sviluppo le porte `5432`/`5433` dell'host sono contese tra istanze Postgres NATIVE Windows (`postgresql-x64-17` su 5432, `postgresql-x64-18` su 5433, entrambe servizi Windows sempre attivi) e container Docker di ALTRI progetti (`fantappero-postgres` prova a pubblicare la 5432) — il DB Docker di QUESTO progetto (porta pubblicata 5433) non era percio' mai raggiungibile dall'host (la 5433 risultava "vinta" dal Postgres nativo 18), mentre i container `soccer_api`/`soccer_scheduler` (rete Docker interna, hostname `db`) si connettevano correttamente al Postgres Docker interno — ma quel volume (`ia_predict_soccer_postgres_data`) conteneva un dataset separato e quasi vuoto (poche righe in `match`, creato 2026-09-01), disallineato dal DB storico reale (`localhost:5432`, Postgres 17 nativo, >47k `match`). Risultato pratico: l'API/scheduler in Docker (quindi anche il frontend che ci parla) lavoravano su un DB vuoto, mentre solo l'esecuzione locale (test/script/training) vedeva i dati veri — causa root del gap "nessuna previsione visibile sul FE" nonostante il lavoro ML/backend completato. Fix: `docker-compose.yml` non definisce piu' il servizio `db`; `api`/`scheduler` puntano ora a `postgresql://postgres:postgres@host.docker.internal:5432/match_db` (hostname speciale di Docker Desktop che risolve all'host Windows, reso portabile anche su Linux con `extra_hosts: host.docker.internal:host-gateway` gia' incluso nel compose); applicata su `localhost:5432` la migration mancante `f3a9c1d8e2b7` (BET-06 `prediction_ledger`, additiva, nessun impatto su tabelle esistenti) cosi' da allinearlo alla head del codice; container Docker `soccer_db` fermato e rimosso, volume `ia_predict_soccer_postgres_data` lasciato su disco ma non piu' referenziato (nessuna cancellazione distruttiva, pulizia manuale opzionale con `docker volume rm` se non serve piu'). Verificato con `GET /health/database` dal container `soccer_api` rebuilded: `host=host.docker.internal`, `database=match_db`, `counts={match: 47228, statistics: 81244, odds: 20402}` — stessa sorgente dati per locale, Docker e training.

Note: gli stati sopra sono riferiti all'implementazione tecnica nel branch corrente; la validazione finale dipende dall'esecuzione acceptance/test su ambiente dati reale.








